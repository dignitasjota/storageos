# CLAUDE.md

Este archivo proporciona contexto persistente a Claude Code para este proyecto. Léelo siempre al inicio de cada sesión.

## Resumen del proyecto

**Nombre:** StorageOS (provisional)
**Tipo:** SaaS multi-tenant para la gestión integral de locales de self-storage.
**Cliente objetivo:** empresas propietarias de uno o varios locales de trasteros que necesitan gestionar trasteros, contratos, inquilinos, facturación, accesos y operativa diaria.

### Jerarquía de usuarios

1. **Super Admin** (nosotros): gestiona la plataforma y los tenants.
2. **Tenant** (empresa cliente): tiene N facilities (locales físicos) y varios usuarios internos con roles (owner, manager, staff, readonly).
3. **Customer** (inquilino final): alquila trasteros; opcionalmente tiene acceso a un portal propio.

## Stack tecnológico

### Backend

- **Node.js 20 LTS + NestJS** (TypeScript estricto)
- **Prisma ORM** sobre PostgreSQL 16
- **Redis** para caché, sesiones y colas
- **BullMQ** para tareas en background (facturación recurrente, emails, generación de PDFs)
- **Zod / class-validator** para validación
- **Passport + JWT** (access + refresh tokens) para autenticación, con 2FA TOTP

### Frontend

- **Next.js 15 (App Router) + React 19 + TypeScript**
- **Tailwind CSS + shadcn/ui**
- **TanStack Query** para data fetching y caché
- **Zustand** para estado global ligero
- **react-konva** para el editor visual de planos
- **Recharts** para dashboards
- **react-hook-form + Zod** para formularios

### Infraestructura

- **Docker + docker-compose** para todos los servicios
- **Despliegue**: VPS con Portainer y Nginx Proxy Manager (SSL vía Let's Encrypt)
- **Almacenamiento de archivos**: MinIO (S3-compatible, autohospedado)
- **Email transaccional**: Resend o Brevo (NO autohospedar SMTP)
- **Pagos**: Stripe (tarjeta) + GoCardless (SEPA); preparar abstracción para añadir Redsys
- **Observabilidad**: Sentry (errores), Uptime Kuma (uptime), Loki + Grafana o Better Stack (logs)

### Estructura del repositorio (monorepo)

Usamos **pnpm workspaces + Turborepo**. El estado actual es:

```
storageos/
├── apps/
│   ├── api/           # Backend NestJS (existente)
│   ├── web/           # Frontend Next.js (existente; aloja panel tenant, portal y admin como rutas hasta que se separen)
│   └── worker/        # Worker NestJS standalone (Fase 13A.1) — processors BullMQ + crons fuera del proceso API
├── packages/
│   ├── database/      # Prisma schema + cliente + migraciones + seed + tests Vitest
│   ├── shared/        # Schemas Zod + tipos compartidos (auth, users, invitations, 2fa, ...)
│   └── ui/            # Componentes UI compartidos (placeholder, shadcn vive en apps/web/src/components/ui/)
├── docker-compose.yml    # Servicios dev: postgres, redis, minio, mailpit, createbuckets
├── docker-compose.prod.yml  # Placeholder (Fase 8)
├── docs/              # ARCHITECTURE, DATA_MODEL, ROADMAP, API, DEPLOYMENT
├── CLAUDE.md          # Este archivo
├── README.md
├── package.json
├── pnpm-workspace.yaml
├── turbo.json
└── tsconfig.json
```

Los apps separados `portal/` (inquilino final) y `admin/` (super admin) están planificados pero todavía conviven como rutas dentro de `apps/web` hasta que el alcance lo justifique. Los configs compartidos (eslint, tsconfig, tailwind) viven en cada paquete y en la raíz; cuando crezcan se extraerán a `packages/config`.

## Multi-tenancy

- Estrategia: **shared database, shared schema con `tenant_id`** en todas las tablas.
- Refuerzo de aislamiento: **Row-Level Security (RLS) de PostgreSQL**.
- En el backend, todo request autenticado debe inyectar `tenant_id` en el contexto vía un guard/middleware. Las consultas Prisma DEBEN filtrar siempre por `tenant_id` (usar una extensión Prisma o un repositorio base).
- NUNCA exponer endpoints que devuelvan datos sin filtrar por tenant.

## Convenciones de código

- **TypeScript estricto** (`strict: true`, `noUncheckedIndexedAccess: true`).
- Imports absolutos con alias (`@/`, `@api/`, `@shared/`).
- Naming:
  - Tablas y columnas en **snake_case** (Postgres).
  - Modelos Prisma y propiedades TypeScript en **camelCase** (Prisma hace el mapping con `@map`).
  - Componentes React en **PascalCase**.
  - Hooks en **camelCase** con prefijo `use`.
- DTOs separados por capa: `CreateXxxDto`, `UpdateXxxDto`, `XxxResponseDto`.
- Validación con **Zod en ambos lados**; los schemas viven en `packages/shared` y se reutilizan en el backend vía `nestjs-zod` (`createZodDto`) y en el frontend vía `@hookform/resolvers/zod`. No usamos class-validator.
- Errores: usar excepciones de NestJS (`BadRequestException`, etc.) con mensajes traducibles.
- Tests: Jest para unit, Supertest para e2e backend, Playwright para e2e frontend.
- Commits: **Conventional Commits** (`feat:`, `fix:`, `chore:`, `refactor:`...).
- Branches: `main` está protegida (branch protection) — solo avanza vía **Pull Request** con el gate `Smoke E2E (Playwright)` en verde. El flujo por cambio es rama `feat/...`|`fix/...`|`chore/...` → push de la rama → PR → merge cuando el gate pase (cheat-sheet en `README.md` → "Flujo de contribución"). El merge a `main` es lo que dispara el pull de Portainer (automatizado vía webhook, ver siguiente punto).
- **Despliegue automático (2026-06-25):** workflow `.github/workflows/deploy.yml` (push a `main`) hace `POST` a un **webhook de redeploy de Portainer** (secret `PORTAINER_WEBHOOK_URL`; sin él es no-op con aviso). El webhook hace `git pull` + redeploy del stack tipo Repository → al traer commits nuevos, la capa `COPY apps/web` del Dockerfile se invalida y **el web se reconstruye con el código nuevo**. **Causa raíz que resuelve**: un "redeploy" en Portainer **sin pull efectivo** daba _cache hit_ del `COPY` → servía la imagen **vieja** del frontend (síntoma: páginas/rutas recién añadidas devolvían 404, cambios que no aparecían). Setup en `docs/DEPLOYMENT.md §6C` (crear el webhook en Portainer + guardar la URL como secret de GitHub Actions). Si en algún caso Portainer reutilizara la imagen sin reconstruir, el follow-up determinista es construir las imágenes en CI y publicarlas en un registry (GHCR) usando `image:` en vez de `build:`.

## Seguridad

- **Passwords** hasheadas con argon2id (`@node-rs/argon2`).
- **JWT** access HS256 de 15 min + **refresh opaco** `<tenantId>.<sessionId>.<secret>` en cookie httpOnly/secure/sameSite. Rotación + detección de reuso paranoid (un refresh ya rotado revoca todas las sesiones del user).
- **2FA TOTP** opt-in con secret cifrado AES-256-GCM en BD (`MASTER_ENCRYPTION_KEY`). Login con `pendingToken` corto firmado con secret independiente. 10 recovery codes hashed argon2id, single-use. La política de **forzar 2FA** para roles `owner`/`manager` se introducirá en Fase 8 como flag de tenant.
- **Rate limiting** con `@nestjs/throttler` (60/min default + presets por endpoint sensible).
- **CSP** estricta en frontend (pendiente afinar tras Fase 2).
- Sanitización de inputs de texto enriquecido (pendiente, llega cuando aparezcan editores ricos).
- **Audit logs** (`audit_logs`) para toda acción crítica.
- **RLS de Postgres** como segunda línea de defensa contra fugas entre tenants.
- **RGPD**: exportación y borrado de datos del inquilino bajo demanda (esquema definido, implementación llega en Fase 8).

## Internacionalización

- Locale por defecto: `es-ES` (España, euros, IVA 21%).
- Preparar i18n desde el inicio con `next-intl` y formato de fechas/monedas según el locale del tenant.
- Fechas siempre almacenadas en UTC, mostradas en la timezone del facility correspondiente.

## Documentación que debes leer y mantener actualizada

- `docs/ARCHITECTURE.md` — decisiones de arquitectura.
- `docs/DATA_MODEL.md` — modelo de datos completo.
- `docs/ROADMAP.md` — fases del proyecto y MVP.
- `docs/API.md` — convenciones de la API REST.
- `docs/DEPLOYMENT.md` — cómo desplegar en el VPS.

## Cómo trabajar conmigo (el desarrollador)

1. **Antes de escribir código nuevo**, comprueba si ya existe algo similar; reutiliza.
2. **Antes de cambios grandes**, propón un plan y espera mi confirmación.
3. **Después de cada feature**, actualiza la documentación relevante.
4. **Tests**: añade tests para lógica de negocio crítica (facturación, pricing, control de accesos, multi-tenancy).
5. **Migraciones Prisma**: nunca edites una migración ya aplicada; crea una nueva.
6. **Variables de entorno**: documenta cada nueva variable en `.env.example`.
7. **Idioma**: responde y comenta en **español**, código y nombres de variables en **inglés**.

## Estado actual

**🗓️ Sesión 2026-07-03 (PRs #233–#234) — Motor de add-ons facturables del SaaS (en 2 PRs):** extras recurrentes (dominio propio, usuarios/locales extra, IA…) que se suman a la suscripción del tenant. **v1 DESACOPLADO de Stripe** (decisión de Jota): el importe efectivo (plan + add-ons) alimenta el MRR/ARPU y el importe sugerido del pago manual; NO se tocan subscription items de Stripe (v2). Monetiza el white-label y futuros extras de forma estructurada.

- **PR1 #233 — modelo + service + endpoints**: migración `subscription_addons` (catálogo GLOBAL, como subscription_plans: slug único, name, price_monthly, description, `feature` opcional, is_active) + `tenant_subscription_addons` (RLS; add-on por tenant con **precio congelado** + quantity; único `(tenant, addon)`). `SaasAddonsService` (en billing-saas): CRUD catálogo + `assign`/`remove` por tenant + `billingSummary` (plan + add-ons + `effectiveMonthly`) + `addonsMonthlyByTenant` (MRR global). **Asignar un add-on con `feature` activa el override de esa feature del tenant** (reusa `tenant_feature_overrides` #159; `upsert` al asignar por `(tenantId, feature)`, `deleteMany` al quitar) → vender «Dominio propio» activa `custom_domain` de un clic (decisión de Jota). Endpoints admin (`AdminGuard`, `@Public()` para saltar el JwtAuthGuard de tenant): `GET/POST /admin/addons` + `PATCH /admin/addons/:id` + `GET /admin/tenants/:id/billing-summary` + `POST /admin/tenants/:id/addons` + `DELETE /admin/tenants/:id/addons/:assignmentId`. **MRR**: `AdminMetricsService` inyecta `SaasAddonsService` y suma los add-ons de los tenants **active** al MRR total y ARPU (no a la distribución por plan; se añadió `tenantId` al select de subscriptions). e2e `saas-addons` 3/3. **Gotcha**: olvidé `git checkout -b` → el commit cayó en `main` local (ahead 1); se detectó al fallar el push, se movió a la rama con `git branch` + `git reset --hard origin/main`.
- **PR2 #234 — UI**: página `/admin/addons` (grupo Facturación, icono `PackagePlus`): catálogo con crear/editar (nombre, slug, precio, feature vinculada, activo). Card «Add-ons facturables» en la pestaña **Pagos** del detalle del tenant (`tenant-addons-card`): add-ons contratados + desglose plan + add-ons + **TOTAL EFECTIVO** mensual + asignar/quitar. Hooks `useAdminAddons`/`useUpsertAddon`/`useTenantBillingSummary`/`useAssignAddon`/`useRemoveAddon`. **Pendiente v2**: subscription items reales en Stripe (proration + sync por webhook); reflejar los add-ons como líneas en la platform_invoice (hoy la factura es de una sola línea sobre el total del pago).

**🗓️ Sesión 2026-07-04 (análisis del PORTAL DEL INQUILINO + fixes) — 3 agentes (seguridad/UX/flujos):** análisis exhaustivo del portal del cliente final. Seguridad SÓLIDA (sin IDOR, todo scoped por customer+tenant del JWT, magic link single-use, precios/stock server-side). Hallazgos por severidad: **GRAVES** (dinero/estado): G1 doble cobro en pagos SEPA/GoCardless `processing` (la factura sigue `issued` → botones activos → 2º adeudo); G2 contrato «zombi» si no se paga la 1ª factura del trastero adicional (unidad ocupada + facturación + dunning sin acceso ni pago, sin cron de limpieza); G3 tienda/pase nocturno entregan el producto antes de cobrar + su factura impaga entra al dunning que suspende accesos. **ALTOS** (UX sesión): A1 pantalla error sin CTA a `/portal/login`; A2 expiración 401 silenciosa sin redirigir; A3 `Promise.all` de 8 fetches todo-o-nada (un fallo parcial borra la sesión). **MEDIOS**: seguridad — register de documentos no valida el prefijo del `fileUrl`, sesión no re-valida tenant suspendido (48h); loops sin cerrar — cambio/incidencia resueltos sin notificar ni mostrar `resolutionNote`, solo 2 eventos generan push; precios sin IVA en seguro/trastero adicional; no cancelar baja; recargo sin etiqueta; sin desglose de factura; estados en inglés; recompensa de referido invisible. **BAJOS**: login pide slug + genérico sin marca; método de pago no explicado; Redsys no gated; badges móvil solo en «Más»; window.prompt; email no editable; docs sin descarga; push sin desactivar; iOS sin instrucciones PWA; sin QR offline; sin i18n; throttle genérico en 4 mutaciones; `totalStock` expuesto. **Plan de PRs**: PR1 anti-doble-cobro · PR2 booking sin zombi · PR3 sesión robusta · PR4 endurecimiento seguridad · PR5 cerrar loops · PR6 política dinero tienda/pase (decisión de Jota) · PR7 pulido UX.

- **PR1 HECHO (`fix/portal-double-charge`)**: `PaymentsService.assertNoPaymentInFlight(tenantId, invoiceId)` → 409 `payment_in_progress` si ya hay un `payment` `processing`/`pending` sobre la factura; llamado en `chargeInvoice` (staff + portal) y en `redsys.createRedirect`. `PortalInvoiceDto.paymentInProgress` (computado con `groupBy` de payments in-flight en `listMyInvoices`) → el portal muestra badge «Pago en curso» + oculta los botones «Pagar»/«Pagar con tarjeta»; `handlePay` recarga las facturas también en el caso `processing`. Cierra G1. e2e `portal-payments` 5/5 (nuevo: processing → charge 409 + `paymentInProgress:true`). Nota: las órdenes Redsys `pending` NO cuentan como en-vuelo (son intentos abandonables); solo `payment` processing/pending (cobro real).
- **PR2 HECHO (`feat/portal-booking-expiry`)**: cierra G2 (contrato «zombi»). Migración `contracts.first_payment_deadline`; `bookForExistingCustomer` lo fija a **now + 72h** (`BOOKING_PAYMENT_DEADLINE_HOURS`); `SignaturesService.expireUnpaidBookings()` (cross-tenant): por cada contrato con `firstPaymentDeadline < now` y status active/draft → si su factura está `paid`, limpia el deadline; si no, `ContractsService.cancel` (libera la unidad vía `syncUnitStatus`) + `InvoicesService.cancel` de las facturas issued/overdue/draft del booking. Cron `BookingExpiryCron` (`0 */6 * * *`, gated `WORKERS_ENABLED_IN_API`) en `MoveInModule`; **el worker ahora importa `MoveInModule` + `PlanLimitsModule`** (MoveIn arrastra `SaasAddonsService`→`PlanLimitsService` global; verificado con worker-bootstrap 2/2). `ContractsService.cancel`/`InvoicesService.cancel` relajados a `userId: string | null`. e2e `portal-self-booking` 2/2 (impagado → cancela+libera+anula; pagado → intacto + deadline null). Constante 72h (configurable por tenant = follow-up); precio del trastero adicional aún sin IVA → PR7.
- **PR3 HECHO (`fix/portal-session-robustness`, frontend puro)**: cierra A1/A2/A3 del portal. (A1) la pantalla de error de `/portal/consume` gana botón **«Pedir un enlace nuevo»** → `/portal/login` (antes callejón sin salida). (A2) `portalFetch` intercepta el **401**: limpia la sesión de localStorage + `setSession(null)` + muestra la pantalla de re-login (antes toasts crudos en bucle). (A3) los 8 fetches core de la carga se envuelven en `safe(p, fallback)` → un fallo transitorio de UNA sección cae a su valor por defecto en vez de tumbar todo el portal; solo el 401 se re-lanza. Sin backend; verificado typecheck+lint+build (el portal se prueba a mano, sin cobertura e2e).
- **PR4 HECHO (`fix/portal-security-hardening`)**: endurecimiento de seguridad del portal. **MEDIA-A**: `CustomerDocumentsService.register` valida que `fileUrl` empieza por `buildPublicUrl('uploads', '<tenant>/customers/<customer>/')` → 400 `invalid_file_url`. **MEDIA-B**: `requireCustomer` re-valida el tenant (deletedAt/suspended/cancelled) → **403 `account_unavailable`** (la sesión de 48h no sigue operando si el tenant se suspende). **BAJA-C**: `consumeMagicLink` con `findFirst deletedAt:null` + status → no emite sesión para customer/tenant borrado/suspendido (401). **BAJA-D**: `@ThrottleLogin()` en messages/purchases/insurance/profile. **BAJA-E/F**: `push.unsubscribe` scopea por `customerId`. e2e `portal-documents` 3/3 (fileUrl inválido 400 + tenant suspendido 403). El gate por-request de tenant cubre `requireCustomer` (pagos/add-ons/mandatos); los GET delegados (contracts/invoices) siguen 48h → follow-up.
- **PR5 HECHO (`feat/portal-resolution-notifications`)**: cierra los loops de notificación al inquilino. `domain-events.ts` añade `incident_resolved` + `unit_change_resolved` (NO son triggers de automations; solo los oye el push) + tipo `CustomerNotifyPayload` (ligero, no el `DomainEventPayload` pesado). `IncidentsService.transition` emite `incident_resolved` al pasar a resolved/dismissed **si el incidente tiene `customerId`** (las de staff no); `UnitChangesService.resolve` (ahora inyecta `EventEmitter2`) emite `unit_change_resolved` con la nota. `PushService` gana `@OnEvent onIncidentResolved` + `onUnitChangeResolved` → `sendToCustomer` (patrón `onInvoicePaid`; no-op sin VAPID). **Frontend**: `PortalUnitChangeRequestDto` gana `resolutionNote` (+ `toPortalDto`); el portal muestra la **nota de resolución** bajo el badge del cambio de trastero (antes el inquilino no veía por qué se rechazó/aprobó). e2e `unit-change-requests` (el inquilino ve status `handled` + `resolutionNote` en su portal) + worker-bootstrap 2/2 (UnitChanges inyecta EventEmitter2, global). Push del `contract_ending_soon` = follow-up (no crítico).
- **PR6 HECHO (`feat/portal-charge-on-purchase`)**: **cobro en el acto** de la tienda de accesorios y el pase nocturno (antes entregaban dejando factura pendiente → el pase, PIN digital usable ya, se usaba GRATIS). `PaymentsService.assertChargeableDefaultMethod(tenantId, customerId)` → 400 `no_payment_method` si no hay PM por defecto cobrable (card/sepa_debit). **Tienda** (`purchaseProducts`): valida PM **antes** de crear la venta → crea venta+factura → `chargeInvoice` → `succeeded`/`processing` entrega OK; `failed`/error → `revertSale` (`productSales.cancel` = repone stock vía `restoreInTx` + anula factura) + 402 `payment_failed`. **Pase nocturno** (`NightPassService.buy`, ahora inyecta `PaymentsService`): pase gratuito (precio 0) se emite sin cobro; con precio valida PM antes → emite PIN → factura (ahora `invoiceNightPass` devuelve `invoiceId` y **lanza** si no hay serie) → cobra → falla → `revertNightPass` (revoca el PIN + anula factura) + 402. `ProductSalesService.cancel` relajado a `userId: string | null`. **Decisión: sin pasarela configurada NO se degrada a «factura pendiente»** (reabriría el agujero) → sin PM = `no_payment_method`. Frontend: `shop-card`/pase muestran «se cobra en el acto» + manejan `no_payment_method` y `payment_failed`. e2e `portal-shop`/`portal-night-passes` (sin método → 400 y NO se entrega; pase gratuito → entregado) + `portal-payments` 10/10 + worker-bootstrap 2/2. **Gotcha**: el cambio de dinero rompió 2 specs viejos que compraban un pase de PAGO sin método (asumían PIN+factura) → `night-pass.e2e` + `staff-night-passes.e2e` adaptados a **pase gratuito** (precio 0, sin ingresos; el caso de pago sin método = 400 lo cubre `portal-night-passes`) — sin Stripe en test no se puede ejercitar un cobro con éxito.
- **PR7 HECHO (`feat/portal-ux-polish`)**: pulido UX del portal (cierra el análisis del portal, 7/7). (1) **Precios con IVA**: `insurance-card` (usa `taxRate` real del DTO) y `additional-unit-card` (21%, el `AvailableUnitDto` no trae taxRate) muestran «€/mes (IVA incl.)». (2) **Estados de factura traducidos**: helper `invoiceStatusLabel` en el portal (draft→Borrador, issued→Emitida, paid→Pagada, overdue→Vencida…). (3) **Cancelar baja**: `ContractsService.cancelMoveOutByCustomer` (valida propiedad + estado `ending` → 400 `contract_not_ending`; revierte a `active` limpiando `endDate`/`endingRequestedAt` + `contractEvent 'resumed'` payload `move_out_cancelled`; **reusa el enum `resumed` para evitar migración**) + `POST /portal/me/contracts/:id/cancel-move-out` + botón «Cancelar baja». e2e `move-out` 3/3. (4) **Ocultar Redsys si no configurado**: `RedsysService.isEnabled` + `GET /portal/me/redsys/enabled` (patrón `gocardless/enabled`) → el botón «Pagar con tarjeta» solo si está activo. (5) **`window.prompt`/`confirm` → diálogos** (acceso adicional con `Input`, pase nocturno con confirmación). (6) badges móvil (`ring-2`). (7) **Documentos descargables**: `CustomerDocumentsService.getDownloadUrl` (valida propiedad + presigned GET 300s, patrón del PDF de contrato) + `GET /portal/me/documents/:id/download` + botón de descarga. typecheck+lint+build+e2e move-out 3/3+worker 2/2. **ANÁLISIS DEL PORTAL CERRADO: 7 PRs (#264–#269 + PR7)** — G1 doble cobro · G2 booking zombi · A1-A3 sesión · seguridad · loops de notificación · cobro en acto · pulido UX.

**🗓️ Sesión 2026-07-04 noche — ANÁLISIS EXHAUSTIVO DEL PANEL SUPER ADMIN (3 agentes: seguridad/UX/rendimiento) → plan de 5 PRs (fixes) + valor.** Hallazgos: rol `support` sin restricción (podía anonimizar/impersonar/cobrar), cookie de refresh mal scopeada (`/admin` vs `/v1/admin` → refresh admin siempre roto), `tenants` sin índices, escrituras en el GET de métricas, invalidaciones de cache rotas, «Hoy» incompleto, listas sin paginar. Plan: PR1 seguridad · PR2 rendimiento · PR3 correctitud · PR4 «Hoy»+triaje · PR5 pulido · luego valor (emails de ciclo de vida, digest KPIs).

- **PR1 HECHO (`fix/admin-security-hardening`)**: (1) **separación de roles** — decorador `@RequireSuperadmin()` + `AdminGuard` lo evalúa (Reflector); aplicado a las acciones destructivas/de dinero/seguridad: anonymize, suspend/reactivate tenant, change-plan, saas-payments/manual, impersonate, disable-2fa/deactivate usuarios, CRUD add-ons (assign/suspend/reactivate/remove), CRUD planes, addon-charges/charge, announcements → un `support` recibe **403 `insufficient_super_admin_role`** (antes podía TODO; `support` es el default al crear). La lectura sigue abierta a support. (2) 🐛 **cookie de refresh** `COOKIE_PATH '/admin'→'/v1/admin'`: el API sirve bajo `/v1` (URI versioning) → por RFC 6265 la cookie **nunca** se adjuntaba a `/v1/admin/auth/refresh` → el refresh silencioso del panel admin fallaba siempre (cada recarga expulsaba al login; era la causa real del gotcha del smoke test 04). (3) 🐛 **impersonación sin rastro**: el token usaba un `sub` UUID sintético que no existía en `users` → toda escritura violaba la FK de `audit_logs.userId` y el audit se descartaba en silencio → ahora el token se emite en nombre del **owner activo real** del tenant (las acciones quedan auditadas; el `ImpersonationLog`+`superAdminId` identifican al impersonador). No se puede impersonar un tenant `cancelled` (suspended sí, a propósito). (4) **revocación efectiva**: `AdminGuard` hace un SELECT por PK de `superAdmin.isActive` por request → desactivar un super admin **invalida su access token vivo de inmediato** (antes seguía hasta 8h). e2e `admin-role-separation` 2/2 (support 403 en destructivas + lee 200 + superadmin 200; desactivar → 401 inmediato).
- **PR2 HECHO (`perf/admin-queries`)**: rendimiento de las agregaciones cross-tenant (pensado a 500 tenants). (1) **Índices** (migración `20260704160000_admin_query_indexes`): la tabla `tenants` no tenía **NINGÚN** índice (solo PK) → seq scan en cada query admin; añadidos `[deletedAt,status]`, `[status,trialEndsAt]`, `[deletedAt,createdAt]`, `[status,updatedAt]` + `invoices[tenantId,issueDate]` + `payments[tenantId,status,paidAt]`. (2) **MRR sin escrituras en el GET**: `getMovements` hacía `captureMonth` (~500 upserts) + `backfillFromPayments` (full-scan + creates en bucle) en **cada** GET, y Métricas lo dispara 2× (movements + forecast lo reusa) → ahora `getMovements` solo LEE + `ensureCurrentMonthSnapshot` (guard: captura el mes en curso solo si falta); el cron pasó de mensual a **diario** (`0 3 * * *`) y hace capture + backfill fuera del path de request; `backfillFromPayments` usa `createMany skipDuplicates`. (3) **`getAtRisk`** (corre cada 60s vía el badge del sidebar): el `user.groupBy(lastLoginAt)` se **acota a los tenants activos** (antes escaneaba toda la tabla `users`). (4) **Frontend**: 12 hooks con `refetchInterval` ganan `refetchIntervalInBackground: false` + los de 15s suben a 30s. e2e admin-mrr/at-risk/today 9/9 + worker-bootstrap 2/2 + db 16/16.
- **PR3 HECHO (`fix/admin-correctness`)**: correctitud del admin. (1) **Idempotencia del pago manual**: `recordManualPayment` no deduplicaba → un doble clic extendía el periodo 2× + ingreso duplicado; ahora si hay un pago idéntico (mismo provider+importe, `paid`) en los últimos **60s** lo devuelve en vez de duplicar. (2) **Invalidaciones de cache rotas** (`hooks.ts`): 🐛 `useAssignAddon`/`useRemoveAddon` invalidaban `['admin','tenant',id]` (singular) pero la ficha es `['admin','tenants',id]` (plural) → nunca refrescaba; ahora invalidan detalle + límites + features; `useChangePlan` invalida detalle+features+límites+billing-summary (antes solo la lista → features/precio stale); `useRunDunning` invalida tenants/at-risk/today (antes nada). (3) **MRR de add-ons unificado**: `admin-metrics` contaba solo tenants con suscripción `active`, `admin-finance` **todos** → dos números con el mismo rótulo; nuevo `SaasAddonsService.addonsMrrForActiveTenants()` (única definición) usado por finance. e2e `admin-saas-manual-payment` 4/4 (añade doble submit → mismo pago, periodo extendido 1×). **Pendiente (a PR5)**: auditar en `super_admin_audit_logs` las mutaciones de add-ons/planes (colisionaba con los decoradores de PR1 en el mismo controller).
- **PR4 HECHO (`feat/admin-today-triage`)**: el «Hoy» del super admin pasa a cubrir TODO lo urgente (antes solo cobros/renovaciones/past_due/seguimientos). `getToday` añade **tickets de soporte sin responder** (status `open`, ordenados por prioridad + antigüedad, con `waitingDays`) y **jobs BullMQ en `failed`** (suma cross-cola; `AdminTodayService` inyecta las 9 colas —QueuesModule es `@Global`— + un `openTickets()` sobre `supportTicket`); ambos suman a `urgentCount` (tickets por count, colas +1 si hay alguno). `AdminTodayDto` gana `openTickets: AdminOpenTicketDto[]` + `failedJobs: number`. **Enlaces cruzados**: la página `/today` lista los tickets con enlace al ticket **y** al tenant + aviso «Colas con fallos → Ver colas»; el detalle del ticket (`/admin/support/[id]`) enlaza el nombre del tenant a su ficha (antes texto plano). e2e `admin-today` 3/3 (+ aserción de `openTickets`/`failedJobs`). **Pendiente (a PR5)**: pestaña de tickets en el detalle del tenant; webhooks fallando en el «Hoy».
- **PR5 HECHO (`feat/admin-polish`)**: pulido del panel admin (cierra el análisis, 5/5). (1) **Auditoría de add-ons/planes** (aparcado de PR3): `saas-addons.controller` (create/update/assign/remove/suspend/reactivate) y `subscription-plans.controller` (create/update/deactivate) ahora registran en `super_admin_audit_logs` vía `SuperAdminAuditService` (`@CurrentSuperAdmin('sub')` + `extractMeta(req)`; actions `admin.saas_addon.*`/`admin.plan.*`); `BillingSaasModule` provee el service directamente —solo depende de `PrismaAdminService` global— para no crear ciclo con `AdminModule`. (2) **Badges de estado traducidos** (`lib/admin/labels.ts`: `TENANT_STATUS_LABELS`/`SUBSCRIPTION_STATUS_LABELS`) en la lista + detalle del tenant (antes inglés crudo); prioridad del ticket traducida. (3) **Estados de error con reintento**: `<AdminError onRetry>` en at-risk/adoption/today/health/trials (antes error = spinner infinito o «vacío» engañoso). (4) **Búsqueda de tenants server-side** (debounce 300ms → `?search=&status=`, antes filtraba en cliente sobre la lista completa; paginación cursor = follow-up). (5) **Padding global** (menor riesgo: solo AÑADE `px-4 py-4 sm:px-6 sm:py-6` a las 9 páginas sin él —today/health/at-risk/adoption/announcements/custom-domains/queues/addons/followups—, contenido pegado al borde en móvil). (6) dark-mode del badge «Tibio» + aria-label del botón de editar add-on. typecheck+lint (api/web) + e2e saas-addons 5/5 + worker 2/2. **ANÁLISIS DEL PANEL ADMIN CERRADO: 5 PRs (#271–#274 + PR5)** — seguridad · rendimiento · correctitud · «Hoy»+triaje · pulido. Follow-ups documentados: paginación cursor de tenants/soporte, pestaña de tickets en el detalle del tenant, caché Redis de las agregaciones + acotar groupBy de health/adoption, y las mejoras de VALOR (emails de ciclo de vida al tenant, digest KPIs, LTV/cohortes de ingresos, benchmarking anónimo).

**🗓️ Sesión 2026-07-05 — MEJORAS DE VALOR del SaaS (del análisis del panel admin).** Tras cerrar los 5 PRs de fixes del admin, se atacan las mejoras de valor propuestas por el agente de rendimiento (por ratio valor/esfuerzo). **V1 HECHO (`feat/tenant-lifecycle-emails`) — emails automáticos de ciclo de vida al TENANT** (antes TODO el seguimiento era manual → a 50+ tenants se pierden conversiones de trial y recuperaciones de past_due): migración `20260705120000` tabla `tenant_lifecycle_emails` (idempotencia: UNIQUE `(tenant_id, type)`) + 4 flags en `platform_alert_settings` (`lifecycle_enabled`/`send_welcome`/`send_trial_reminders`/`send_past_due`). `TenantLifecycleEmailsService.run()` (idempotente) encola al OWNER del tenant: **bienvenida** (alta <48h), **trial por expirar** (hitos 7/3/1 días, ventana de 1 día por hito para que el cron diario capture cada uno una vez), **past_due** (una vez por episodio). Reutiliza el patrón de `admin-comms` (`recipientsFor` owners verificados + `emailQueue.addBulk` a `QUEUE_EMAIL`); `dispatch` registra la fila ANTES de encolar (catch P2002 → sin duplicados entre réplicas) y NO registra si no hay destinatarios (reintenta cuando haya owner verificado). Plantillas en español con CTA a `${WEB_BASE_URL}/settings/saas-billing` (solo interpolan el nombre del tenant con `escapeHtml`). Cron `TenantLifecycleEmailsCron` (`0 8 * * *`, `claimDailyCronRun`, corre en el API sin gatear como platform-alerts). `POST /admin/tenant-lifecycle/run` (`@RequireSuperadmin`, «ejecutar ahora»). Config reutiliza el singleton `platform_alert_settings` + la página `/admin/platform-alerts` (card «Emails automáticos al tenant» con 4 toggles + botón). e2e `tenant-lifecycle-emails` (trial +1d → run encola + registra + 2ª llamada no duplica). **Mejoras de valor pendientes** (por orden): digest de KPIs por email al admin · LTV/cohortes de ingresos · cupones SaaS + trials por lote · playbooks de retención · segmentación por tags · export contable facturas SaaS · benchmarking anónimo entre tenants.

- **V2 HECHO (`feat/admin-kpi-digest`) — digest SEMANAL de KPIs por email al super admin**: para ver MRR/churn/altas había que entrar al panel (6 queries pesadas); ahora un email los lunes 08:00 + botón «Enviar ahora». `WeeklyDigestService.sendWeeklyDigest()` reutiliza `getOverview()` + `getMovements(1)` + `getAtRisk()` (top 5 a vigilar), renderiza HTML/text en español (MRR + variación, net new, churn, ARPU, nuevos, trials por convertir, tickets abiertos), envía a `alertEmail` del singleton `platform_alert_settings`. Migración `20260705140000` (`weekly_digest_enabled`). Cron `WeeklyDigestCron` (`0 8 * * 1`, `claimDailyCronRun`). `POST /admin/weekly-digest/run` (`@RequireSuperadmin`). Toggle + botón en `/admin/platform-alerts`. e2e `admin-weekly-digest` (off → `disabled_or_no_email`; on → `sent:true`).
- **V3 HECHO (`feat/admin-ltv-cohorts`) — LTV + cohortes de ingresos** (analítica admin, sin migración): `AdminMetricsService.getLtv(months)` agrega cross-tenant sobre `tenant_subscription_payments` (un `groupBy` por tenant con `_sum`/`_count`/`_min`, acotado). KPIs: `avgLtv` (= `avgArpa × avgLifespanMonths`, modelo prospectivo) + `realizedLtv` (Σ pagos / nº pagadores) + `avgLifespanMonths` (alta→baja aprox por `updatedAt` de suspended/cancelled, mismo criterio que `getRetention`, mín 1) + `avgArpa`. **Top 10 tenants por LTV realizado** + **cohortes de INGRESO** por mes de alta (ingreso acumulado + €/tenant, últimos N meses clamp 1-24). `GET /admin/metrics/ltv?months=` + card «LTV y cohortes de ingresos» en `/admin/metrics`. e2e `admin-ltv` (tenant con 2 pagos 29€ → totalPaid 58 + estructura; 401).
- **V4 HECHO (`feat/saas-coupons-trials`) — cupones/descuentos SaaS + extender trials por lote**: migración `20260705160000` tabla `platform_coupons` (code único, `discount_type` percentage/fixed, `discount_value`, `valid_until`, `max_uses`, `used_count`, `is_active`). `PlatformCouponsService`: CRUD + `validateAndComputeDiscount(code, amount)` (400 `coupon_invalid`/`coupon_expired`/`coupon_exhausted`; percentage = `amount×%`, fixed = `min(value, amount)`) + `incrementUsage` (SQL crudo `used_count+1 WHERE used_count < max_uses` → atómico anti-carrera). **Aplicar en el pago manual**: `recordManualPayment` acepta `couponCode` → calcula el descuento SERVER-SIDE (no confía en el cliente) e incrementa el uso tras el guard de dedup (no lo consume en un doble-submit). CRUD `GET/POST/PATCH /admin/coupons` (`@RequireSuperadmin`, auditado) + página `/admin/coupons` + campo «Código de cupón» en el pago manual del tenant. **Extender trials por lote**: `POST /admin/tenants/extend-trials {tenantIds, days}` (declarado ANTES de `:id`) → suma días al `trialEndsAt` de N tenants; selección múltiple en `/admin/trials`. e2e `admin-coupons` 5/5 (cupón % en pago manual + used_count; max_uses:1 → 2º uso 400; inexistente 400; lote extiende N trials; 401). **Deuda pre-existente (no de V4)**: los unit `payments.service.spec`/`portal.service.spec` fallan por mocks desactualizados (les falta `tx.payment.count`/`findFirst` de `assertNoPaymentInFlight`/`assertChargeableDefaultMethod` de PRs del portal) — el CI no corre unit de api, así que no bloquea; arreglar los mocks es follow-up.
- **V5 HECHO (`feat/admin-retention-playbooks`) — playbook de retención (1 clic)** (sin migración): botón «Playbook» por tenant en `/admin/at-risk` que con un clic hace 3 gestiones a la vez. `AdminCommsService.launchRetentionPlaybook(tenantId, superAdminId, meta)`: valida tenant → crea **followup** (dueDate +3d, «Retención: contactar al cliente») → encola **email** de retención a los owners verificados (reusa `recipientsFor` + `enqueue` a `QUEUE_EMAIL`, best-effort: si no hay owner verificado se crean followup+interacción igual, `emailRecipients:0`) → registra **interacción** `note` («Playbook de retención lanzado») → audita `admin.tenant.retention_playbook`. Decisión: NO reusa `emailTenant` (lanza `no_recipients` + duplicaría la interacción) sino sus primitivas → una sola interacción. `POST /admin/tenants/:id/retention-playbook` (`@RequireSuperadmin`). e2e `admin-retention-playbook` (playbook → followupId + emailRecipients≥1 + fila en tenant_followups + tenant_interactions; 401).
- **V6 HECHO (`feat/admin-tag-segmentation`) — segmentación por tags** (sin migración): los `tenant.tags String[]` (junto a ltvTier/strategicNotes, editables por `PUT /admin/tenants/:id/notes`) ahora sirven para filtrar y dirigir comunicaciones. Filtro `?tag=` en `GET /admin/tenants` (`list` añade `tags: { has: tag }`) + `GET /admin/tenants/tags` (tags distintos en uso, antes de `:id`) → selector en `/admin/tenants`. Broadcast dirigido: `AdminBroadcastSchema` gana `tag?` → `broadcast` restringe (AND con el `audience` de status) a los tenants con ese tag; selector en `/admin/announcements`. e2e `admin-tag-segmentation` (tag a 1 de 2 → filtro devuelve 1 + broadcast alcanza 1; 401).
- **V7 HECHO (`feat/saas-invoices-csv-export`) — export contable CSV de facturas SaaS** (sin migración): `GET /admin/platform-billing/export?year=` (`@RequireSuperadmin`, `@Res()`) → `PlatformInvoicesService.exportCsvForYear(year)` genera el CSV en SERVIDOR (BOM `String.fromCharCode(0xfeff)` + `;`, escape de `;`/`"`/saltos, importes `toFixed(2)`; columnas: nº factura, fecha emisión, concepto, tenant, base, IVA, total, estado; filtra por `issuedAt` del año, clamp 2020-2100) con `Content-Type text/csv` + `Content-Disposition attachment`. Front: `adminApiFetchBlob` (descarga autenticada con token admin + refresh transparente) + selector de año + botón en `/admin/platform-billing`. **Unifica** el export previo (#249, que construía el CSV en cliente por rango, sin gating) → una sola vía servidor por año. e2e `admin-saas-invoices-export` (200 text/csv + BOM + cabecera de columnas; 401).
- **V8 HECHO (`feat/tenant-benchmark`) — benchmarking anónimo entre tenants** (feature de valor del panel del TENANT, sin migración): `BenchmarkService.getBenchmark(tenantId)` (usa `PrismaAdminService` cross-tenant SOLO para agregados) calcula, sobre operadores activos con ≥1 unit, mediana/p25/p75 de **ocupación** (units occupied/total), **precio medio** y **€/m²** (basePriceMonthly/areaM2, areaM2>0) + la posición del propio tenant (`mine` + `myPercentile`). **Anonimato estricto**: solo agregados, nunca ids/nombres/valores de otros; **mínimo 5 operadores** o `{available:false}`. Funciones puras `percentile`/`median`/`percentileRank`/`computeMetric` en `benchmark.util.ts` (7 unit tests). `GET /analytics/benchmark` (`@RequirePermission(analytics:read)`, JwtAuthGuard de tenant, NO admin). Pestaña «Mercado» en `/analytics` (barra p25→p75 + marcador mediana/tenant + percentil; «datos insuficientes» si <5). e2e `tenant-benchmark`. **LAS 8 MEJORAS DE VALOR DEL SaaS COMPLETAS: V1 emails ciclo de vida · V2 digest KPIs · V3 LTV/cohortes · V4 cupones/trials lote · V5 playbooks retención · V6 tags · V7 export contable · V8 benchmarking anónimo.**
- **HECHO (`feat/admin-cursor-pagination`) — paginación por cursor de la lista de tenants del admin**: `AdminTenantsService.list` devuelve `{ items, nextCursor }` (take limit+1, cursor por `(createdAt desc, id desc)`, limit clamp 1-100 default 30) en vez del array completo con 4 `_count` por fila (caro a 500+ tenants). `GET /admin/tenants?cursor=&limit=` + DTO `AdminTenantsListResponseDto`. Front: `useAdminTenants` pasa a `useInfiniteQuery` + botón «Cargar más» en `/admin/tenants` (`rows = data.pages.flatMap(items)`). e2e `admin.e2e` (limit=1 → 1 item + nextCursor; 2ª página con cursor no repite) + `admin-tag-segmentation` actualizado al nuevo contrato.
- **HECHO (`fix/trial-to-active-on-payment`) — el pago saca de trial + botón «Finalizar prueba»**: 🐛 dos estados separados (`tenant.status` trial/active/suspended vs `subscription.status`); todos los `updateMany` que reactivaban al pagar filtraban solo `status:'suspended'` → un tenant en **trial que pagaba** (Stripe o pago manual) dejaba la suscripción `active` pero **seguía en `trial`** (banner de prueba, salía en «Trials por expirar», podía expirar aun pagando). Fix: `syncSubscriptionFromStripe` (webhook) y `recordManualPayment` (extendsPeriod=true, ahora **incondicional**, ya no solo `wasPastDue`) cambian el filtro a `status: { in: ['suspended','trial'] }` → al pagar la suscripción el tenant pasa a `active`. **Botón «Finalizar prueba»** (duda 1): `AdminTenantsService.endTrial` (valida `trial`→400 `tenant_not_in_trial`; pone `active` + limpia `trialEndsAt`) + `POST /admin/tenants/:id/end-trial` (`@RequireSuperadmin`, auditado `admin.tenant.trial_ended`) + botón en el detalle del tenant (solo si `status===trial`) con diálogo. e2e: `admin-saas-manual-payment` (trial + pago → active) + `admin.e2e` (end-trial → active + trialEndsAt null; no-trial → 400).
- **HECHO (`feat/audit-followups`) — 3 follow-ups de la auditoría en 1 PR: prorrateo del traslado + cierre de caja por local + facilityScope en mutaciones de facturas.** (1) **facilityScope en las mutaciones de facturas**: el assert por local se movió al `InvoicesService.findOrThrow(tenantId, id, facilityScope?)` (un solo punto) → **todas** las mutaciones por id (update/issue/cancel/mark-paid/refund/rectify/late-fee/revertPayment) asertan el local del contrato → un usuario restringido recibe **403 `facility_not_in_scope`** al mutar una factura de un local fuera de su scope (antes solo las listas + detalle). Cada método pasa `args.facilityScope`; el controller pasa `user.facilityScope`. Las facturas sin contrato (F2/productos) siguen permitidas. (2) **Cierre de caja POR LOCAL**: migración `20260708150000` `cash_closures.facility_id` (nullable = caja global; con valor = arqueo de un local) + **dos índices únicos parciales** (global `WHERE facility_id IS NULL` + por-local `WHERE facility_id IS NOT NULL`) → un cierre global **y** uno por cada local el mismo día. `getDaySummary(tenantId, date, facilityId?, scope?)` filtra los pagos por `invoice.contract.unit.facilityId`; `closeDay`/`listClosures` respetan el `facilityScope` (el encargado solo ve/cierra la caja de sus locales + la global); catch P2002 → 409 `day_already_closed`. Selector «Caja: Global / <local>» + columna «Caja» en `/cash`. (3) **Prorrateo del traslado de trastero**: `changeUnit` gana `prorate?` → si la cuota nueva es MAYOR, emite una factura de ajuste F1 (IVA 21) por la **diferencia prorrateada por los días que restan del mes natural** (`(newPrice−oldPrice)×díasRestantes/díasDelMes`, en céntimos); si baja o igual no factura (el ahorro llega en la recurrente). `ContractsModule` importa `BillingModule` (sin ciclo: Billing no importa Contracts) → `ContractsService` inyecta `InvoicesService`; best-effort (si falla la emisión no bloquea el traslado). Checkbox «Emitir factura de ajuste prorrateada» en el diálogo de traslado. shared `ChangeUnitSchema.prorate`. e2e: `facility-scope` (staff de A: mark-paid de factura de B → 403 `facility_not_in_scope`) + `cash-closure` (caja de local independiente de la global, ambas cerrables el mismo día, re-cierre 409) + `contract-renew-move` (traslado a trastero más caro con prorate → factura «Ajuste por cambio de trastero» emitida). typecheck 8/8 + build web.
- **HECHO (`feat/booking-conversion`) — PR6 de la auditoría: transparencia de precios con IVA en el embudo del inquilino** (frontend puro). Hallazgo M3: el inquilino **firmaba sin ver cuánto pagaría** — el importe con IVA solo aparecía DESPUÉS de firmar, en el paso de pago → sorpresa/abandono. Fix: (1) **firma** (`/sign/[token]`): bloque «Resumen del precio» ANTES de firmar con **cuota mensual (IVA 21% incl.)** + **fianza** (una vez, sin IVA) + **a pagar al activar** + nota de prorrateo si entra a mitad de mes (el `ContractSignViewDto` ya traía `priceMonthly`/`depositAmount`). (2) **landing pública** (`/s/[slug]` + `/s/[slug]/[facility]`, B2C → precio con IVA por ley): «desde X€/mes · IVA incl.» (× 1.21). (3) **booking** (`/book/[slug]`): el `<option>` y un resumen del tipo elegido muestran la cuota con IVA incl. + `min` en el date input (arregla B4: permitía fechas pasadas). Verificado typecheck+lint+build web (las públicas se prueban a mano, sin e2e headless). **Pendientes del embudo (PRs aparte)**: hold/reserva temporal de la unidad durante el booking+firma (A4, carrera sobre el último trastero) · captura email-first + lead si abandona (A3) · widget con precios/disponibilidad reales en vez de solo lead (A2).
- **HECHO (`feat/booking-unit-hold`) — hold de la unidad en el booking self-service (A4 del embudo).** 🐛 Carrera del último trastero: `BookingService.createBooking` elegía la primera unidad `available` y creaba el contrato **draft** SIN marcar la unidad → quedaba `available` hasta firmar (ventana de 72 h) → otro booking (o dos POST simultáneos) se llevaban el mismo trastero. Fix: (1) la selección+reserva de la unidad se serializa por `(tenant, tipo)` con **`pg_advisory_xact_lock`** (se libera al commit; la 2ª tx espera y elige otra libre) y la unidad pasa **`available → reserved`** + `unitStatusHistory` → sale de la disponibilidad; (2) el `sign` ya aceptaba `reserved→occupied` (verificado); (3) `ContractsService.cancel` ahora libera también la unidad **`reserved`** (antes solo `occupied` de contratos active/ending) → un booking cancelado sin firmar la devuelve a `available`; (4) `createBooking` fija `firstPaymentDeadline = now+72h` → el `BookingExpiryCron`/`expireUnpaidBookings` cancela los bookings **abandonados sin firmar** y libera el hold (sin esto la unidad quedaría `reserved` para siempre). `bookForExistingCustomer` (portal) crea+firma inmediato y el guard del `sign` (occupied) ya lo protege → no necesita hold. e2e `booking-unit-hold` 2/2 (reserva→fuera de disponibilidad→2º booking 409→firma occupied; abandonado→cron→available). **Gotcha local**: `move-out.e2e` timeoutea en mi máquina (argon2 lento), falla idéntico en main → no es de este PR, en CI pasa.
- **HECHO (`feat/booking-email-first`) — captura email-first en el booking (A3 del embudo).** El booking `/book/[slug]` solo persistía algo al pulsar «Continuar a la firma»: si el visitante abandonaba tras teclear su email, no quedaba **nada** (ni lead, ni remarketing). Fix: `BookingService.captureLead(slug, {email, firstName?, facilityId?, unitTypeId?})` guarda un **lead** (source `widget` + metadata `origin:'booking'`, sin migrar el enum `LeadSource`) en cuanto se deja el email; **idempotente** (si ya hay un lead `new` del mismo email en las últimas 24 h lo enriquece con local/tipo preferidos en vez de duplicar); **best-effort** (nunca lanza, no rompe el formulario) + honeypot. `POST /public/move-in/book/:slug/lead` (`@Public`, throttle 10/min). Front: `onBlur` del campo email en `/book/[slug]` → captura el lead (guardado en estado `leadCaptured` para no repetir). El operador ve el lead abandonado en `/leads`. shared `CaptureBookingLeadSchema`. e2e `booking-email-first` (captura → el staff lo ve en /leads → reintento no duplica → honeypot no captura).
- **HECHO (`feat/widget-prices`) — widget embebible con precios + reservar + consentimiento (A2/B1 del embudo).** El widget `/widget/[slug]` era un «formulario de contacto disfrazado»: titulaba «Reservar trastero» pero solo captaba lead, e **ignoraba los datos que el backend ya traía** (`WidgetService.listFacilities` devuelve `unitTypes` con `defaultPriceMonthly`, `availableUnits`, `color`, `description` → el front solo usaba `name`/`city`). Fix (frontend): (1) sección **«Trasteros disponibles»** que renderiza los tipos con **precio (IVA incl., ×1.21)** + plazas disponibles + descripción + punto de color; (2) botón **«Reservar online»** → `/book/[slug]` (`target="_blank"`, sale del iframe al booking self-service real) + el formulario de lead queda como «o te llamamos»; (3) **B1 consentimiento RGPD**: `acceptsTerms` pasó de `true` por defecto (¡sin casilla!) a **casilla obligatoria desmarcada** — el schema `WidgetLeadSchema.acceptsTerms` cambió de `z.literal(true)` a `z.boolean().refine(===true)` (permite la casilla desmarcada en el form; el envío sigue exigiendo true) y el submit se deshabilita sin marcarla. Verificado typecheck+lint+build web + e2e `communications` 9/9 (el widget con `acceptsTerms:true` sigue OK). **ANÁLISIS DEL EMBUDO DEL INQUILINO CERRADO**: precios IVA (PR6) · hold de unidad (A4) · email-first (A3) · widget con precios (A2).
- **HECHO (`fix/facility-scope-billing`) — el alcance por local (facilityScope) ya cubre facturas y pagos (M2 del panel staff).** El `facilityScope` (un usuario restringido a ciertos locales, `user_facilities`) estaba en units/contracts/collections/access pero **NO en facturas ni pagos** → un «encargado de UN local» **veía y gestionaba la caja de todos los locales**. Fix: (1) `InvoicesService.list` acepta `facilityScope` → filtra por `contract.unit.facilityId ∈ scope` (las facturas SIN contrato —F2/ventas de producto, no ancladas a local— se incluyen vía `OR contractId:null`); `invoices.controller` pasa `user.facilityScope`. (2) `InvoicesService.detail(tenantId, id, scope?)` hace `assertFacilityAllowed` sobre el local del contrato → **403** al pedir por id una factura de un local fuera del scope (las sin local se permiten). (3) `PaymentsService.list` filtra igual vía `invoice.contract.unit.facilityId`. e2e `facility-scope` extendido (staff de local A ve la factura de A, NO la de B, detalle de B → 403, owner ve ambas). **Follow-up**: las mutaciones directas por id (issue/charge/refund) aún no asertan el scope (vector menor: exige conocer el id de una factura de otro local); las lecturas —lo que expone «la caja»— ya están cubiertas.
- **HECHO (`feat/cash-closure`) — cierre de caja diario / arqueo de efectivo (M7 del panel staff).** El efectivo se cobra factura a factura (`mark-paid` methodType `cash` → crea `payment`) pero **no había agregación diaria ni cuadre de caja**. Fix (feature nueva): migración `20260708140000` tabla `cash_closures` (RLS; único `(tenant, closure_date)` = un cierre por día). `CashService`: `getDaySummary(date)` agrega los `payment` `succeeded` con `paidAt` en el día por método (`groupBy methodType` → efectivo/tarjeta/SEPA/transferencia/otros + total + nº) e incluye el cierre si existe; `closeDay({date, countedCash, notes})` → **esperado** = efectivo del día, **diferencia** = contado − esperado, registra el arqueo (409 `day_already_closed` si ya cerrado) + audit `cash.day_closed`; `listClosures` (historial, take 90). `GET /cash/summary?date=` + `GET /cash/closures` (`payments:read`) + `POST /cash/close` (`payments:charge`). Shared `packages/shared/src/cash` (`CloseCashSchema` + `CashDaySummaryDto`/`CashClosureDto`). Front: página `/cash` (resumen por método + arqueo con efectivo contado/diferencia en vivo + historial), item «Cierre de caja» en el grupo Facturación (icono `Wallet`, `payments:read`). Caja **global del tenant** v1 (sin desglose por local). e2e `cash-closure` (cobro cash 121€ → summary → cierre contando 120 → diferencia -1 → 2º cierre 409 → historial).
- **HECHO (`feat/backlog-closeout`) — cierre del backlog de la auditoría: renovar + traslado de trastero + onboarding + backups en el stack (4 piezas en 1 PR).** (1) **Renovar contrato** (`ContractsService.renew`, `POST /contracts/:id/renew {months}` `contracts:manage`): extiende `endDate` N meses (helper `addMonths` con ajuste de fin de mes; base = fin actual o hoy); si estaba `ending` vuelve a `active` + limpia `endingRequestedAt`/`endingSoonNotifiedAt`; evento `note_added` payload `renewed` + audit. Botón «Renovar» en el detalle del contrato. (2) **Traslado de trastero** (`changeUnit`, `POST /contracts/:id/change-unit {newUnitId, newPrice?}`): valida destino `available` + mismo tenant + facilityScope (assert en la unidad de destino), libera el trastero viejo (→available) y ocupa el nuevo (→occupied) vía `syncUnitStatus`, actualiza `unitId` + `priceMonthly` opcional; guardas `same_unit`/`unit_not_available`; evento `unit_changed` + audit. La próxima recurrente sale con el trastero/precio nuevos (**prorrateo de una factura de ajuste = follow-up**, hoy no se genera factura intermedia). Botón «Trasladar» (selector de trasteros disponibles) en el detalle. shared `RenewContractSchema`/`ChangeUnitSchema`. (3) **Onboarding del operador** (`OnboardingService.getOnboarding`, `GET /dashboard/onboarding`): checklist derivado de counts (primer local/tipo/trastero/serie de facturación/inquilino/contrato) con progreso; `OnboardingCard` en el dashboard (se auto-oculta al completarse). shared `OnboardingDto`. (4) **Backups DENTRO del stack** (M de infra): servicio `backup` (perfil `backup`, opt-in) en `docker-compose.prod.yml` + `docker-compose.portainer.yml` — **autónomo** (conecta directo a `postgres` por la red interna, sin docker socket ni crontab del host): `pg_dump` custom → cifrado GPG AES-256 → subida a Backblaze B2, retención local; loop diario (`BACKUP_INTERVAL_SECONDS`). Vars en `.env.example`, doc en `DEPLOYMENT.md §10.1` (respalda Postgres; MinIO sigue por `scripts/backup.sh` del host). e2e `contract-renew-move` 2/2 (renovar +12m → endDate; traslado reasigna + estados de unidad + `same_unit` 400; onboarding refleja pasos). **CIERRA la auditoría del panel staff + embudo del inquilino** (los otros trabajos están en la cola de PRs #291–#297).
- **HECHO (`feat/deposit-settlement`) — PR3 de la auditoría: ciclo de vida de la fianza.** 🐛 El enum `ContractDepositStatus {none,held,returned,partially_returned}` existía pero `depositStatus` **nunca se escribía** (solo se leía) y al finalizar el contrato NO se liquidaba la fianza (sin devolución/retención). Fix: (1) migración `20260708120000` añade `contracts.deposit_returned_amount`/`deposit_settled_at`/`deposit_retention_reason`; (2) `ContractsService.sign` marca `depositStatus:'held'` al activar si `depositAmount>0` (sin pisar un estado ya avanzado); (3) `settleDeposit(contractId, {returnedAmount, retentionReason})` liquida al finalizar → valida `held` (400 `deposit_not_held`), rango del importe (400 `invalid_return_amount`), motivo obligatorio si retiene (400 `retention_reason_required`); transiciona held→`returned` (devolución total) / `partially_returned` (parcial), guarda importe/fecha/motivo + `contractEvent note_added` payload `deposit_settled` (reusa el enum, sin migrar) + audit `contract.deposit_settled`. La devolución NO es factura (la fianza es garantía sin IVA). `POST /contracts/:id/settle-deposit` (`contracts:manage`). `ContractDto` gana `depositReturnedAmount`/`depositSettledAt`/`depositRetentionReason`; shared `SettleDepositSchema`. Front: `DepositCard` en el detalle del contrato (estado traducido + botón «Liquidar fianza» si held → diálogo con importe a devolver + motivo + **muestra la deuda pendiente del contrato** para ayudar a retener). e2e `deposit-settlement` 3/3 (firma→held; parcial→partially_returned+retención+guardas; total→returned).
- **HECHO (`feat/data-retention-crons`) — PR4 de la auditoría: retención de tablas de crecimiento ilimitado.** Hallazgo ALTO de arquitectura: `audit_logs`, `access_logs`, `communications` y `notifications` crecían **sin cleanup** (a diferencia de `security_events`/`webhook_deliveries`/`stripe_events` que sí lo tienen) → riesgo de disco lleno / Postgres OOM en el VPS modesto. Fix: `DataRetentionModule` + `DataRetentionService.runCleanup()` (cross-tenant vía `PrismaAdminService`, `deleteMany` por antigüedad: audit_logs/access_logs por `occurredAt`, communications/notifications por `createdAt`) + `DataRetentionCron` (`@Cron 0 4 * * *`, gated `WORKERS_ENABLED_IN_API` → corre en el worker en prod). Plazos configurables por env (`RETENTION_AUDIT_LOGS_DAYS` default **730** = 24 meses por compliance, `RETENTION_ACCESS_LOGS_DAYS`/`RETENTION_COMMUNICATIONS_DAYS` **180**, `RETENTION_NOTIFICATIONS_DAYS` **90**; documentadas en `.env.example`). NO toca tablas con obligación fiscal (invoices/payments). Registrado en app.module + worker.module (worker-bootstrap 2/2 cierra el grafo). unit `data-retention.service.spec` (poda las 4 tablas por su plazo + suma conteos). **Pendientes del PR4 original** (separados): backups dentro del stack (compose/cron) + facilityScope en facturas/pagos.
- **HECHO (`feat/bulk-invoicing`) — PR5 de la auditoría: facturación en lote + emisión automática.** Hallazgo A5: la recurrente genera **borradores** que había que emitir **una a una** (200 contratos = 200 clics cada mes) y no había acciones masivas. Fix: (1) **flag `auto_issue_recurring`** por tenant (migración `20260708130000`) → si activo, `billing-jobs.processGenerateRecurring` **emite** cada factura tras crearla (best-effort: si el issue falla queda en draft, no tumba el lote); toggle «Emisión automática de recurrentes» en `/settings/billing`. (2) **Acciones en lote** con selección múltiple en `/invoices`: `POST /invoices/bulk/issue` (`InvoicesService.bulkIssue`) + `POST /payments/invoices/bulk/charge` (`PaymentsService.bulkCharge`), ambos `{ids:[]}` (máx 200) → procesan cada factura **independientemente** y devuelven `{succeeded, failed:[{id,error}]}` (un fallo no tumba el resto; un cobro SEPA `processing` cuenta como OK). Front: columna de checkbox (+ select-all de los seleccionables) + barra «N seleccionadas → Emitir (draft) / Cobrar (issued/overdue)» con toast del resumen. **Gotcha**: `bulk/issue` y `bulk/charge` deben declararse **ANTES** de `:id/issue`/`:invoiceId/charge` o Nest interpreta `bulk` como el `:id` → ParseUUIDPipe 400. e2e `bulk-invoicing` 2/2 (bulk issue con una ya emitida → succeeded+failed; auto_issue → la recurrente emite en vez de draft). shared `BulkInvoiceActionSchema`/`BulkInvoiceActionResultDto`. **Pendiente (follow-up)**: bulk «enviar recordatorio a N» (el dunning ya recuerda automáticamente).
- **HECHO (`fix/billing-money-bugs`) — PR1 de la auditoría del panel staff: 2 bugs de dinero.** 🐛 **(A2) reembolso falso**: `InvoicesService.refund` solo tocaba la BD; `gateway.refund()` estaba implementado pero **nadie lo llamaba** → el botón «Reembolsar» marcaba la factura pero el dinero NO salía de Stripe/SEPA. Fix: `InvoicesService` inyecta `PAYMENT_GATEWAY` (BillingModule ya importa PaymentsModule, sin ciclo); si la factura tiene un `payment` con `gatewayPaymentId` reembolsable → `gateway.refund({gatewayPaymentId, amountCents, reason})` (rechazo → 400 `gateway_refund_failed`, sin tocar BD) **y** actualiza `payment.refundedAmount` en la misma tx → el webhook `charge.refunded` (que sincroniza por delta contra ese campo) ya no **doble-cuenta**. Cobro manual (sin gateway) → solo abono contable en BD (como antes). Guarda `over_refund_gateway`. unit `invoices-refund.service.spec` 3/3 (gateway+anti-doble-cómputo · manual · rechazo). 🐛 **(A4) doble facturación del 1er mes**: la 1ª factura del move-in iba por **aniversario** (`startDate`+1 mes) y la recurrente por **mes natural** (`[día1, últimoDía]`), y el dedup exigía coincidencia **exacta** de periodo → nunca casaba → el primer mes se cobraba dos veces (solape). Fix: (1) `signatures.maybeIssueFirstInvoice` factura **`[alta, fin de mes natural]` con el alquiler PRORRATEADO** por días (día 1 = mes completo; la fianza no se prorratea); (2) `billing-jobs.processGenerateRecurring` deduplica por **SOLAPAMIENTO** (`periodStart <= nuevoEnd && periodEnd >= nuevoStart`) en vez de coincidencia exacta. e2e `billing-proration` (alta 15-mar precio 31€ → alquiler 17€ prorrateado [15-31 mar]; recurrente de marzo NO duplica; abril sí crea). Front: nota en el diálogo «Reembolso» (pasarela vs abono manual).
- **HECHO (`fix/seed-transactional-templates`) — PR2 de la auditoría: los emails transaccionales del ciclo de vida ya se envían.** 🐛 `MessageTemplatesService.seedBuiltins` existía pero **no lo llamaba nadie** y `communications.enqueue` **lanzaba `message_template_not_found`** si la plantilla no estaba en BD → un tenant real que se registraba tenía **rotos** todos los emails del embudo (bienvenida, contrato firmado, factura vencida del dunning, PIN de acceso, review). Fix por 3 capas: (1) **seed en el alta** (`AuthService.register` → `tx.messageTemplate.createMany({skipDuplicates})` de `BUILTIN_TEMPLATES` en la $transaction), (2) **fallback en `enqueue`** (helper `resolveTemplateSource` cae a `BUILTIN_TEMPLATES.find(code)` si falta la fila), (3) **backfill** en `bootstrap.ts` para tenants existentes. e2e `communications` (registro siembra + enqueue con plantillas borradas cae al built-in sin lanzar).

**🗓️ Sesión 2026-07-04 (análisis SaaS-tenant + endurecimiento self-service, PRs #259+) — la relación tenant↔plataforma:** análisis exhaustivo (3 agentes) de cómo el TENANT gestiona su suscripción/add-ons/facturación con StorageOS. Hallazgos: **C1 CORREGIDO (PR `feat/tenant-saas-invoices`)**: el tenant ya ve/descarga sus `platform_invoices` — `GET /settings/saas-billing/invoices` + `/invoices/:id/pdf` (`getPdfUrlForTenant` con scoping por tenantId, 404 si es de otro) + `/payments` (historial de pagos SaaS, reusa `listSaasPayments`); sección «Facturas y pagos» en `/settings/saas-billing` (facturas con botón PDF + historial de pagos, con nota de que NO son las facturas a inquilinos); el email de factura corregido («Ajustes → Suscripción → Facturas y pagos»). e2e `tenant-saas-invoices` 2/2 (scoping cross-tenant); **A1 CORREGIDO (PR `feat/tenant-plan-change`)**: cambio de plan self-service real — `POST /settings/saas-billing/change-plan {planId}` (`changePlanSelfService`): si el tenant tiene `stripeSubscriptionId` → `stripe.subscriptions.retrieve` + `subscriptions.update({items:[{id,price}], proration_behavior:'create_prorations'})` (NO crea suscripción nueva) + actualiza `planId` en BD directamente (el webhook `subscription.updated` no trae el plan) + audit `saas_billing.plan_changed`; si es pago manual (sin Stripe) → 400 `manual_plan_change` («contacta con soporte»); guardas `plan_not_available`/`already_on_plan`. **Guard anti-duplicado** en `createCheckoutSession`: si ya hay suscripción `active` con `stripeSubscriptionId` → 400 `already_subscribed` (evita doble cobro). Front: catálogo de planes **SIEMPRE visible** (antes solo en trial) con **comparativa de features** (`tenantFeatures` + `FEATURE_LABELS`) + botón contextual (`PlanCta`: Plan actual / Suscribirse / Cambiar a este plan / Contactar soporte) + **estados del badge traducidos** (`STATUS_LABELS`). e2e `tenant-plan-change` 2/2 (plan inexistente/mismo/manual → 400 + checkout duplicado → 400). **Proration**: `create_prorations` (Stripe ajusta la diferencia en la próxima factura, tanto upgrade como downgrade); decisión de v1. **abusos del self-service de add-ons — CORREGIDOS (PR2, `fix/saas-selfservice-abuses`)**: (H4/M1) re-contratar un add-on ya poseído caía en el UPDATE del upsert y **pisaba precio congelado/cantidad/notas** del admin → ahora **409 `addon_already_assigned`**; (M2) **fuga**: las `notes` internas del admin sobre la asignación se devolvían al tenant → `selfServiceView` las **sanitiza a null** (el admin sí las ve vía `billingSummary`); (H2) **exploit de límites**: contratar add-on de capacidad → crear recursos → cancelar (los recursos persisten, enforcement solo-al-crear) → ahora `selfRemove` llama `assertCapacityRemovable` (si `uso > límite_sin_addon` para units/facilities/users → **409 `addon_capacity_in_use`**); (H7) cancelar un add-on con **config especial** (precio ≠ catálogo, quantity>1 o notas) → **notifica al super admin** (`superAdminNotification` type `saas_addon.self_removed`). e2e `self-service-addons` 4/4 (añade re-contratar 409 + notes null + capacidad en uso 409). **PR4 CORREGIDO (`feat/platform-invoice-concept`)**: la factura de plataforma ganó `concept` (migración `platform_invoices.concept`); `issueForPayment` lo fija = `payment.description` (p. ej. «Add-on: X»); el PDF (`renderHtml`) y el DTO usan `concept ?? «Suscripción {plan}»` → las facturas de cobros de add-on ya no salen mal etiquetadas como «Suscripción {plan}». `PlatformInvoiceDto.concept` + fila del tenant lo muestra. e2e `tenant-saas-invoices` 3/3. **PR5 pulido UX HECHO (`feat/saas-ux-polish`)**: pestañas de Ajustes renombradas para diferenciar «Mi suscripción» (lo que pagas a StorageOS) vs «Facturación a inquilinos» (lo que cobras a tus clientes) en `es.json settings.nav`; **CTA «Regularizar el pago»** (enlace a `/settings/saas-billing`) en los emails de dunning del plan (`PlatformDunningService` inyecta `ConfigService` para `WEB_BASE_URL`); **plan de pago manual impagado**: `TenantBillingStatusDto.planManual` (`past_due && !stripeSubscriptionId`) → el `PaymentAlertBanner` muestra «He realizado el pago» (ticket, cubre plan y/o add-ons) en vez del enlace de método de pago que no aplica sin Stripe. e2e `tenant-billing-status` 2/2. **PLAN DE 5 PRs SaaS-tenant COMPLETO: #259 abusos add-ons · #260 facturas tenant · #261 cambio de plan · #262 concepto factura · PR5 UX.** **H1 (escape de cobro de feature pura) queda documentado**: el cobro manual desacoplado permite usar una feature un rato sin pagar; se cierra del todo con el cobro automático de add-ons (v2 Stripe subscription items, ver [[v2-addons-stripe-billing]]).

**🗓️ Sesión 2026-07-03 tarde (auditoría de monetización + panel admin, PRs #245–#253 + M#4 en curso) — bugs corregidos + mejoras del admin:** auditoría en profundidad de pagos/add-ons/cambios de suscripción (3 agentes) + mapa del panel admin. **Bugs corregidos:** #245 críticos (escape de deuda con add-on suspendido → 400 `addon_suspended`; dunning doble padding de `manualExtensionDays`; `chargeAddon` re-chequea `suspendedAt`; reactivar `tenant.status` al pagar), #246 estructurales (`manualExtensionDays` solo acumula si el tenant ya tiene `stripeSubscriptionId`; overrides de feature con `source` `manual`/`addon` + `reconcileFeatureOverride` → quitar/suspender un add-on no borra cortesías ni features de otros add-ons), #247 medios (`changePlan` valida `plan.isActive`; **preview de cambio de plan**; `updateAddon` bloquea cambiar feature si asignado; webhook Stripe sin tenant → `super_admin_notification`). **Mejoras S:** #248 «Hoy» admin (renovaciones manuales por expirar + add-ons suspendidos >30d), #249 export CSV de `platform_invoices`, #250 item «Suscripción» en el menú de Ajustes del tenant. **Mejoras M:** #251 dashboard financiero `/admin/finance` (cobrado por fuente Stripe/manual + serie + MRR add-ons), #252 analítica global de add-ons cross-tenant (card «Adopción del catálogo» en `/admin/addons`), #253 gestión de trials `/admin/trials` (ordenados por expiración + filtro «nunca accedió» + «+14 días» inline). **M#4 (rama `feat/admin-tenant-notes`):** notas estratégicas/LTV del tenant (migración `20260703230000_tenant_notes`: `ltv_tier`/`strategic_notes`/`tags[]` + `GET/PUT /admin/tenants/:id/notes` auditado en `super_admin_audit_logs` + card «Notas y valor» en la pestaña General del detalle; enum LTV low/medium/high/enterprise + tags sugeridos). e2e `admin-tenant-notes` 2/2. **BAJOS de la auditoría CORREGIDOS (rama `fix/audit-monetization-lows`):** (1) **race de límites** (TOCTOU) — el count + `assertCanCreate` estaban en una tx separada del create en units/facilities/invitaciones → nuevo `PlanLimitsService.lockForCreate(tx, tenantId, resource)` (`pg_advisory_xact_lock(hashtext(tenant), key::int4)`, se libera al commit) tomado DENTRO de la tx del create, con el count movido dentro; e2e de concurrencia (6 altas simultáneas con límite 1 → exactamente 1 crea). **Gotcha**: Prisma manda el number JS como bigint → `pg_advisory_xact_lock(int, bigint)` no existe → castear `::int4`. (2) **reactivar usuario** (`admin-support.service.setUserActive`) ahora inyecta `PlanLimitsService` y re-chequea `maxUsers` (activos + invites pendientes) al reactivar → 403 `users_limit_reached` si topa. (3) **`nextChargeAt` residual** — `saas-addons.suspend` limpia `nextChargeAt: null` (el filtro del «Hoy» ya excluía `suspendedAt`, pero se limpia por coherencia). (4) **serie de `platform_invoices`** por año de EMISIÓN (`new Date()`), no por `payment.paidAt` (un pago manual retroactivo no debe numerarse en la serie de un año anterior). **Backlog M (en curso):** **churn por razón HECHO** — migración `tenants.churn_reason`/`canceled_at`; `SuspendTenantSchema` (reason + `churnReason` opcional: price/missing_features/business_closure/competitor/nonpayment/other); `suspend`/`reactivate` capturan/limpian el motivo + `canceledAt`; el dunning setea `churnReason:'nonpayment'` al suspender por impago; `AdminMetricsService.getChurnByReason(months)` agrupa las bajas (suspended/cancelled en la ventana por `canceledAt`, fallback `updatedAt`) por motivo capturado o **inferido** (past_due→nonpayment, cancelAtPeriodEnd→voluntary, resto unknown) con count + MRR perdido + nº capturados; `GET /admin/metrics/churn-by-reason`; selector de motivo en el diálogo de suspender + card «Churn por razón» en `/admin/metrics`; labels en `lib/admin/churn.ts`. e2e `admin-churn-by-reason` 2/2. **retry analysis HECHO** — migración `tenant_subscription_payments.failed_attempts`/`first_failed_at`/`recovered_at`; el webhook `invoice.payment_failed` ahora **persiste** el fallo (antes solo audit): `recordInvoicePaymentFailed(invoice)` hace upsert por `(provider, externalId)` con `status:'failed'` + `failedAttempts++` + `firstFailedAt`; al cobrarse luego, `recordStripeInvoice` marca `recoveredAt` sobre la misma fila; `AdminMetricsService.getPaymentRetryAnalysis(months)` = de las facturas con `firstFailedAt` en la ventana, cuántas `recoveredAt` (tasa de recuperación, importe recuperado vs en riesgo, intentos medios); `GET /admin/metrics/payment-retries` + card «Recuperación de cobros» en `/admin/metrics`. e2e `admin-payment-retries` 2/2 (simula el ciclo con `app.get(BillingSaasService)` + `metadata.tenantId` como hint). **forecast MRR HECHO** — `MrrSnapshotService.getForecast(historyMonths=12, horizonMonths=6)` sobre `getMovements`: `currentMrr` = último `endingMrr`; `avgNewMrr` = media(newMrr+reactivation) de los meses con actividad; `avgNrr` = media de los NRR no-null; proyecta el horizonte con `mrr[i] = mrr[i-1]×(avgNrr/100) + avgNewMrr` (aditivo si no hay NRR), `warmingUp` si sin histórico; expone los supuestos. `GET /admin/metrics/mrr-forecast?months=` + card «Previsión de MRR» (AreaChart, actual vs previsto a 6 meses + supuestos) en `/admin/metrics`. e2e `admin-mrr-forecast` 2/2. **Backlog M COMPLETO (3/3):** churn por razón + retry analysis + forecast MRR. **Falsos positivos descartados:** booking/importador CSV SÍ respetan límites; forzar `active` al cobro manual es intencional. **Falsos positivos descartados:** booking/importador CSV SÍ respetan límites; forzar `active` al cobro manual es intencional.

**🗓️ Sesión 2026-07-03 (PRs #235–#243) — SUITE DE MONETIZACIÓN del SaaS (ciclo de vida completo del add-on manual, 9 PRs):** amplía el motor de add-ons (#233/#234) hasta un ciclo cerrado: contratar → cobrar → recordar → suspender si no paga → avisar al tenant → reactivar. Sigue **v1 desacoplado de Stripe** (cobro manual; el cobro automático por Stripe queda como v2, ya anotado con su gotcha del webhook `items.data[0]`).

- **#235 — docs** del motor de add-ons (#233/#234).
- **#236 — catálogo del grupo 1 (features premium como add-ons vendibles)**: `DEFAULT_SAAS_ADDONS` en `packages/shared/src/admin/default-addons.ts` (las 9 features premium: IA 12€, SEPA 9€, conciliación N43 6€, control de accesos 15€, ECRI 10€, seguros 6€, automatizaciones 8€, impagos/overlock 12€, dominio propio 15€; precios orientativos). El **seed** dev y el **bootstrap** prod los siembran idempotente por slug (`update:{}` → no pisa ediciones del super admin). ROI inmediato: el desarrollo ya está hecho, solo se define el precio, y da a un `free`/`starter` motivo para subir gasto sin cambiar de plan. Test `seed.test` (database).
- **#237 — enforcement de límites del plan + add-ons de capacidad**: los planes ya tenían `maxUnits/maxFacilities/maxUsers` pero eran **informativos**. Migración `subscription_addons.grants_units/grants_facilities/grants_users` (capacidad que aporta el add-on por unidad de `quantity`). `PlanLimitsModule` (global, ligero, para no arrastrar billing-saas en facilities) + `PlanLimitsService`: `resolveLimits` (= `plan.maxX + Σ(add-on.grants × quantity)`; **`null` en el plan = ilimitado**, no se toca) + `assertCanCreate` (**403 `<resource>_limit_reached`** con `{limit, current}`) en el create de **units** (cuenta), **facilities** (activas) e **invitaciones** (users activos + invitaciones pendientes). `GET /admin/tenants/:id/limits` (uso vs límite). **El enforcement SOLO actúa al CREAR** (no oculta ni borra lo existente → bajar un límite = «no puedes crecer», nunca «te quito lo que sobra»). UI: inputs de capacidad en `/admin/addons` + badges de uso vs límite (ámbar al topar) en la card de add-ons del tenant. Habilita el pricing que escala con el cliente (trasteros/locales/usuarios extra). e2e `plan-limits` 2/2.
- **#238 — catálogo self-service del tenant**: el **owner contrata/cancela extras** desde `/settings/saas-billing` (sección «Extras de tu cuenta»), en vez de depender del super admin → sube la conversión. `BillingSaasController` (`billing:configure`): `GET /settings/saas-billing/addons` (resumen + catálogo disponible), `POST` (contratar; solo add-ons ACTIVOS, 400 `addon_not_available`; activa la feature al instante), `DELETE /:assignmentId` (cancelar). `SaasAddonsService.selfServiceView/selfAssign/selfRemove` con el tenantId del token. Invalida `auth/me` al contratar/cancelar (cambian las features). e2e `self-service-addons` 2/2.
- **#239 — fix del pago manual (raíz de 2 bugs de facturación con add-ons)**: (1) **cobro sin extender periodo** — `recordManualPayment` gana `extendsPeriod` (default true); con `false` registra el ingreso sobre el periodo VIGENTE + factura, **sin tocar `current_period_end` ni `manual_extension_days`**. Caso: un add-on cobrado en mano a un tenant que paga el plan **por Stripe** (el periodo lo lleva Stripe, el add-on no debe estirarlo — y el acumulador `manual_extension_days` lo estiraría de forma PERMANENTE). Checkbox «Solo cobro (no extender el periodo)» en el diálogo, **activado por defecto si `hasStripe`**. (2) **sugerencia de duración por importe efectivo** — antes dividía el importe por el precio del PLAN a secas, inflando los meses con add-ons (49€ plan + 50€ add-on = 99€ → sugería `round(99/49)=2` meses en vez de 1); ahora divide por el `effectiveMonthly` (plan + add-ons) del `billing-summary`. e2e `admin-saas-manual-payment` 3/3.
- **#240 — «Hoy» del super admin** (`/admin/today`, como el «Hoy» del tenant): bandeja de acciones del día. **Pieza central = cobros de add-ons pendientes** (add-ons con `next_charge_at<=hoy` de tenants con `stripeSubscriptionId` — los 100% manual cobran el add-on con el plan, sin recordatorio aparte; migración `tenant_subscription_addons.next_charge_at`, se programa al asignar y avanza +1 mes por cobro). `POST /admin/today/addon-charges/:id/charge {provider}` cobra sin extender periodo + reprograma (un clic). Agrega también trials por expirar + past_due (reusa `getAtRisk`) + seguimientos vencidos (`listPending`). Item «Hoy» **primero** en el nav admin con **badge ámbar de urgentes** (refetch 60s). Resuelve el hueco: un add-on manual de tenant-con-Stripe no disparaba ningún aviso (Stripe renueva solo). e2e `admin-today` 2/2. **Gotcha**: el controller admin nuevo necesita `@Public()` (salta el JwtAuthGuard de tenant) además de `@UseGuards(AdminGuard)`, o el token de super admin da 401 (lo cazó el e2e).
- **#241 — suspender/reactivar un add-on por impago (reversible, no destructivo)**: alternativa a «quitar» (que borraba el add-on + su historial). Migración `tenant_subscription_addons.suspended_at`. `SaasAddonsService.suspend`/`reactivate`: suspender **retira el override de la feature** (se desactiva; `FeatureGuard`/UI la bloquean) + no cuenta al **MRR** (`addonsMonthly`/`addonsMonthlyByTenant` filtran activos) ni al total efectivo (se sigue listando, tachado + badge «Suspendido») + su **capacidad deja de ampliar el límite** (`resolveLimits` filtra activos) + **sale del «Hoy»** (`addonChargesDue` filtra activos); **NO toca los datos del tenant**. Reactivar re-aplica el override + resetea `next_charge_at`. 400 si ya está en ese estado; reasignar reactiva. `POST /admin/tenants/:id/addons/:assignmentId/{suspend,reactivate}`; `TenantAddonDto.suspended`. UI: badge + botones en la card del tenant y botón «Suspender» en el «Hoy». **Nota de diseño**: como el enforcement solo actúa al crear, suspender un add-on de capacidad **NO oculta los trasteros existentes** (solo impide crear más); y suspender una feature **NO corta la operación física ya configurada** — `/access/verify` es `@Public` (device key) sin gating → las puertas siguen abriendo con las credenciales emitidas; cortar eso sería un paso más agresivo, **deliberadamente fuera de alcance**. e2e `saas-addons` 4/4.
- **#242 — aviso de pago pendiente en el panel del tenant**: `GET /settings/billing-status` (**sin `@RequirePermission`** → lo consulta cualquier usuario del tenant para el banner; el `JwtAuthGuard` global ya exige sesión) → `TenantBillingStatusDto` (`pastDue` = suscripción del plan `past_due`, `suspendedAddons`, `suspendedFeatures`, `hasIssue`). Banner rojo `PaymentAlertBanner` en el layout del panel (visible en todas las páginas cuando `hasIssue`). `FeatureGate`: si la feature está en `suspendedFeatures`, muestra «… está suspendida por un pago pendiente» en vez del genérico «no está en tu plan». **Coherencia**: como el cobro de add-ons es manual, «add-on no pagado» = «add-on suspendido» (el super admin lo suspende → el aviso aparece automáticamente); el plan sí es automático (Stripe → `past_due` → banner solo). e2e `saas-addons` 5/5.
- **#243 — regularización del pago pendiente por caso**: como el add-on es de cobro manual (offline), el banner ofrece la vía correcta. **Plan** impagado (Stripe): enlace «Actualizar método de pago» → `/settings/saas-billing` (portal de Stripe, online). **Add-ons** suspendidos (manual): botón **«He realizado el pago»** que abre un **ticket de soporte** al super admin (asunto «Regularización de pago», priority high, con los add-ons afectados) → le llega con el **badge de `/admin/support`**; él confirma el cobro desde el «Hoy» y reactiva. `FeatureGate` suspendida → CTA «Contactar para regularizar» → `/support`. Reutiliza el sistema de tickets tenant↔admin. Frontend puro. **Circuito de impago manual cerrado**: no paga → suspendes desde el «Hoy» → banner + mensaje en la feature al tenant → «He realizado el pago» abre ticket → registras el cobro y reactivas.

**🗓️ Sesión 2026-07-03 (PRs #230–#232) — Overlock + impago físico → disposición (backlog operativo del negocio, en 3 PRs):** gestión del impago físico del self-storage. **Principio rector (marco legal ES)**: en España NO hay lien law → el software **NO ejecuta** la subasta; **orquesta el expediente** (plazos, avisos, evidencias, liquidación) con **compuertas manuales** en cada paso legalmente sensible. La validez del procedimiento depende del contrato + asesoría del operador → todo opt-in con plazos configurables + descargo legal en la UI.

- **PR1 #230 — modelo + máquina de estados**: `CollectionsModule`. Migración `delinquency_cases` (RLS; contract/customer/unit/facility denorm p/ scope, status, `debt_snapshot` céntimos, fechas por hito + `final_notice_deadline`, disposal_type; **índice único parcial: 1 expediente NO cerrado por contrato**) + `delinquency_case_events` (timeline inmutable) + `delinquency_case_files` (evidencias MinIO privado `uploads`) + enum `delinquency_case_status` + config del tenant (`collections_enabled` default OFF, `_open_after_days` 30, `_notice_days` 15, `_clause_ref`). **Máquina de estados** `open → overlocked → final_notice → resolution_pending → disposal → closed_disposed`; desde cualquiera `closed_paid` (AUTO por pago) y `closed_cancelled`; `TRANSITIONS` valida cada paso (400 `invalid_transition`). `CollectionsService`: apertura manual (exige deuda viva → 400 `no_debt`) + `openFromDunning` (opt-in, idempotente por el índice único; enganchado en el paso `legal_notice`/+30 del dunning vía `@Optional` CollectionsService, sin ciclo) + transiciones con evento+audit + deuda viva recalculada de las facturas issued/overdue en **céntimos** (`toCents`) + evidencias (presigned PUT/GET patrón inspection photos) + `onInvoicePaid` (listener `invoice_paid` → cierra el expediente cuando la deuda llega a 0 + aviso de retirar candado; el acceso ya lo reactiva `AccessIntegrationsService`). Permisos `collections:read` (todos) / `collections:manage` (owner+manager) + feature `collections` (starter+pro; free por override) + `@RequireFeature`. **Gotchas**: el plan `pro` data-driven exige que el seed/bootstrap sincronice `tenantFeatures` (ya lo hacía desde el custom_domain) + añadir la feature a starter+pro; el catálogo pasó a 9 features (actualizar `admin-adoption` toHaveLength); en el e2e `/auth/me` da `tenant.id` (no `tenantId`) y `mark-paid` exige `amount`. e2e `collections` 4/4.
- **PR2 #231 — UI staff**: página `/collections` (lista con filtro por estado, deuda viva, badge de plazo vencido, candado + diálogo de ajustes con descargo legal) + `/collections/[id]` (cabecera + **acciones contextuales según el estado** de la máquina: candado → requerimiento con plazo → marcar plazo vencido → disposición con tipo → completar con importe → cancelar → nota + subida de evidencias + timeline). Hooks `lib/collections/hooks.ts`. Item «Impagos» en el sidebar (grupo Cartera, `collections:read` + feature `collections`, icono `Lock`).
- **PR3 #232 — «Hoy» + docs**: sección **«Impagos: plazo vencido»** en `/today` (`TodayService` añade `collectionsDeadlines` = expedientes `final_notice` con `finalNoticeDeadline` vencido, suma a `urgentCount`; `TodayDto` gana el campo). Docs: ROADMAP (feature completa + descargo legal), CLAUDE.md/memoria. **Pendiente (v2)**: PDF del requerimiento (Puppeteer), liquidación fina (producto+fianza→facturas por antigüedad con `markPaidManually`), badges en contrato/portal.

**🗓️ Sesión 2026-07-03 (PRs #227–#229) — White-label por dominio propio por tenant (tier premium, en 3 PRs):** un tenant del plan `pro` (o con la feature `custom_domain` por override) sirve su web pública bajo **su propio dominio** (`trasteros-garcia.com`) en vez de `app.plataforma.com/s/garcia`. La landing, páginas por local y la reserva (`/reservar`) van bajo su marca; el panel del staff/portal siguen en el dominio de plataforma.

- **PR1 #227 — modelo + API + CORS + gating**: migración `tenants.custom_domain` (UNIQUE) + `custom_domain_verified_at` (null=pendiente, fecha=activo). Feature nueva **`custom_domain`** en el catálogo `TenantFeatures` (incluida en `pro` por el spread `[...TenantFeatures]`; `starter` no) + `FEATURE_LABELS`. Shared: `HOSTNAME_REGEX`/`isValidCustomDomain`; `customDomain` en `UpdateTenantBrandingSchema`; DTOs `AdminCustomDomainDto` + `ResolveDomainDto`. **Tenant**: `PATCH /settings/tenant/branding` acepta `customDomain` — **setear/cambiar requiere la feature** (403 `feature_not_in_plan`, gating EN EL SERVICE con `hasCustomDomainFeature` reusando `resolvePlanFeatures`+`effectiveFeaturesFromList`, NO `@RequireFeature` en el endpoint que también cambia color/logo; quitar con `''` siempre se permite), **unicidad** (409 `domain_taken`), cambiar dominio **resetea `verifiedAt`**. **Público**: `GET /public/landing/resolve-domain?host=` → `{tenantSlug}` solo si verificado (lo consume el middleware); la landing expone `customDomain` solo si activo (para el canonical). **Admin**: `GET /admin/tenants/custom-domains` (cola) + `POST /admin/tenants/:id/custom-domain/verify|revoke` (auditados, doble rastro; declarados antes de `:id`). **CORS dinámico** (`common/cors-origin.ts`): `ALLOWED_ORIGINS` + dominios verificados con caché en memoria (TTL 5 min). **Seed/bootstrap**: `custom_domain` en el plan `pro` + el upsert ahora **sincroniza `tenantFeatures`** (`update: { tenantFeatures: ... }`, antes `update: {}`) → un deploy actualiza el jsonb del plan con features nuevas (el jsonb del plan no se edita desde la UI). e2e `custom-domain` 4/4. **Gotchas**: (1) el plan `pro` es data-driven (jsonb `tenant_features`) → sin sincronizar el upsert, `resolvePlanFeatures` prioriza el jsonb viejo sin la feature nueva → gating falla con 403 aun en pro; (2) `exactOptionalPropertyTypes` obliga a `data.tenantFeatures as string[]` en el upsert del seed/bootstrap; (3) el test `admin-adoption` asevera el nº de features del catálogo (pasó de 7→8) → actualizar `toHaveLength`.
- **PR2 #228 — middleware + SEO + UI**: **función pura** `resolveCustomDomainRoute(pathname, slug)` en `packages/shared/src/routing` con **8 unit tests vitest** → mapea cada ruta de un dominio propio a `rewrite` (raíz→`/s/<slug>`, `/reservar`→`/book/<slug>`, `/<local>`→`/s/<slug>/<local>`), `redirectToPlatform` (panel/auth/admin) o `next` (portal/firma/pago/assets/landing directa). **`middleware.ts` reescrito**: detecta el `Host`; si ≠ plataforma (`NEXT_PUBLIC_SITE_URL`; localhost/IP = plataforma en dev), resuelve el slug vía `resolve-domain` con **caché en memoria TTL 60s** y aplica la función; host no resoluble → passthrough; el `matcher` pasa a `'/((?!_next/static|_next/image|favicon.ico).*)'` (corre en todo salvo assets, para leer el Host en cualquier ruta). **SEO**: canonical de `/s/[slug]` y `/s/[slug]/[facility]` apunta al dominio propio cuando está activo (evita contenido duplicado). **UI tenant**: card «Dominio propio» en `/settings/branding` (gated `useHasFeature('custom_domain')`) con estados sin-configurar/pendiente/activo + guía DNS. **UI admin**: página `/admin/custom-domains` (grupo Tenants, icono `Globe`) — cola de pendientes con «Activar» + activos con «Desactivar» (hooks `useAdminCustomDomains`/`useCustomDomainAction`). **CI**: nuevo step `pnpm -F @storageos/shared test` (vitest en shared, config nueva `packages/shared/vitest.config.mts` + devDep vitest + exclude `**/*.test.ts` del tsconfig.build). **Gotcha**: el web NO tenía runner de unit tests (solo Playwright) → la función pura + su test van en `packages/shared` (con su vitest), no en `apps/web`. **Config prod (DEPLOYMENT.md §18)**: el super admin crea el Proxy Host en NPM (`web:3000` + SSL Let's Encrypt) para el dominio del tenant (sin wildcard, un cert por dominio; **no** hace falta Proxy Host de API, el CORS ya permite el dominio) → activa en `/admin/custom-domains`. **Pendiente v2**: automatizar NPM (Proxy Host+cert vía su API) + verificación DNS por TXT + email saliente con dominio propio (SPF/DKIM por tenant) + upload del logo al bucket.
- **PR3 #229 — docs**: `DEPLOYMENT.md §18` (funcionamiento, flujo de alta por dominio, requisitos/infra, troubleshooting) + `ONBOARDING.md` (paso opcional + cobro del extra vía Stripe dashboard/pago manual) + `ROADMAP.md` (white-label completo). **Cobro del extra facturable**: la feature funciona por plan `pro` O por override del super admin (palanca ya existente #159); no hay motor de add-ons recurrentes aún (v2), así que el extra se cobra ajustando el precio de la suscripción en Stripe o como pago manual con nota.

**🗓️ Sesión 2026-07-02 tarde (PRs #218–#222) — follow-ups de features + reorganización del menú admin:**

- **PWA del panel staff**: el panel del tenant es ahora instalable (antes solo el portal del inquilino lo era). `public/manifest-staff.webmanifest` (id `/dashboard`, scope `/`, start_url `/dashboard`, standalone) enganchado en `(app)/layout.tsx` (`metadata.manifest` + `appleWebApp` + `viewport.themeColor` — el layout es server component, puede exportar metadata). **Reutiliza** el `sw.js`, `<PwaRegister>` y `<InstallPwaButton>` del portal (el SW es único, scope `/`, cubre toda la app). Botón «Instalar app» en el header del staff (`AppHeader`, aparece solo con `beforeinstallprompt`). Dos PWAs instalables distintas por scope: staff (`/`, start `/dashboard`) vs portal (`/portal`, start `/portal/login`). Sin e2e (las PWA no se testean bien headless; verificación manual en móvil, como el portal).
- **#224 Inbound de mensajes (WhatsApp + email → chat del inquilino)**: hasta ahora las comunicaciones eran solo salientes + el chat del portal; las respuestas del inquilino por WhatsApp/email se perdían. Ahora entran al MISMO hilo `customer_messages`, marcadas con su canal. Migración `customer_messages.channel` (portal|whatsapp|email, default portal); `sendFromCustomer(..., channel)`; `CustomerMessageDto.channel`. `InboundMessagesModule`: `InboundMessagesService` resuelve el customer por remitente (el WABA/email es GLOBAL, compartido por tenants) vía `PrismaAdminService` — teléfono por **sufijo de 9 dígitos** (`phone: { contains }`; desempata por la comunicación WhatsApp saliente más reciente si el nº está en varios tenants), email por coincidencia insensitive; best-effort (remitente no resoluble = log, no rompe el webhook). `WhatsAppInboundController` (`@Public`, `/webhooks/whatsapp` VERSION_NEUTRAL): GET verificación de Meta (`hub.challenge` con `WHATSAPP_VERIFY_TOKEN`) + POST con firma `X-Hub-Signature-256` (HMAC del app secret sobre raw body, como Stripe; sin app secret no se exige, dev) → parsea `entry[].changes[].value.messages[]` de texto → **siempre 200** (si no Meta reintenta en bucle). `EmailInboundController` (`/webhooks/email-inbound`): POST con `X-Inbound-Secret` == `EMAIL_INBOUND_SECRET`. Env nuevas `WHATSAPP_VERIFY_TOKEN`/`WHATSAPP_APP_SECRET`/`EMAIL_INBOUND_SECRET` + raw body para `/webhooks/whatsapp`. Frontend: badge «vía WhatsApp/email» en los mensajes entrantes del chat del staff. e2e `inbound-messages` 3/3. **Gotcha**: el ConfigModule tiene `cache: true` → los secrets de test van en `env-setup.ts` (el `beforeAll` llega tarde). **Config prod**: en Meta apuntar el webhook a `/webhooks/whatsapp` con verify token + app secret; email = routing entrante del proveedor al endpoint con el secret. **Con esto se cierra el backlog de follow-ups** (filtro por local en Hoy, ocupación competencia, menú admin agrupado, ventanas horarias, PWA staff, inbound).
- **#222 Ventanas horarias reales por credencial**: el `allowedHours` por credencial era un stub (solo `always !== false`); el toque de queda es POR LOCAL. Ahora una credencial define franjas propias. Modelo (sin migración, ya era `Json`): `allowedHours = { windows: [{ days: 0-6, start: HH:MM, end: HH:MM }] }`; `AccessWindowSchema` valida `start < end` (sin cruce de medianoche — para eso el curfew); helpers puros `accessWindowsFrom` (parseo defensivo, tolera `{}` viejo) + `isWithinAccessWindows(windows, weekday, minutes)`. Verify: `checkAccessWindows(credential, facility)` en la cadena `evaluateCredential ?? checkAccessWindows ?? checkCurfew`, evalúa «ahora» en la **TZ del local** (nuevo `nowWeekdayInTz` + el `nowMinutesInTz` existente); sin ventanas → sin restricción; se retiró el stub de `evaluateCredential`. **3 niveles de acceso horario**: toque de queda del local (a todos) · `bypassCurfew`/24h (exención) · ventanas por credencial (franja propia). Frontend: `AccessWindowsEditor` en el alta (`/access/credentials`): pills L-D + inputs `time`. e2e `access-windows` 1/1. **Gotcha**: al añadir `AccessAllowedHours` al DTO, faltó importarlo en `access/types.ts` → el typecheck local pasó (dist previo) pero el `tsc` limpio del CI del shared falló → cuando toques tipos de `packages/shared`, el build del shared en CI es más estricto que el typecheck local.

**🗓️ Sesión 2026-07-02 tarde (PRs #218–#220) — follow-ups de features + reorganización del menú admin:**

- **#218 Filtro por local en «Hoy»**: `GET /dashboard/today?facilityId=` acota las secciones ANCLADAS a un local (entradas/salidas, firmas, contratos que vencen, reservas por `unit.facilityId`; facturas por `contract.unit.facilityId`; tareas e incidencias por su `facilityId` propio); las de empresa (leads, seguimientos, mensajes, cambios) se dejan tenant-wide. Controller valida el uuid (400 `invalid_facility_id`). Frontend: selector «Todos los locales / …» en `/today` (solo si hay >1; respeta el facility scope porque `/facilities` ya viene filtrado) + nota de que leads/seguimientos/mensajes son de toda la empresa. e2e `dashboard-today` 3/3.
- **#219 Ocupación de la competencia** (punto 7 de #183): los `competitor_units` ya se fichaban con estado available/occupied; ahora `GET /competitors/occupancy` (`analytics:read`, antes de `:id`) infiere la ocupación de la competencia (ponderada por nº de trasteros, no por local) y la compara con la MÍA (units occupied / total). `MarketOccupancyDto`. Frontend `/competitors`: card «Ocupación de mercado» (2 barras tú vs competencia + insight: por encima → margen para subir precio, por debajo → revisar precio/captación) + badge «% ocupación» por competidor. e2e `competitors` 2/2.
- **#220 Menú del super admin en submenús desplegables**: el nav pasaba de 22 items planos a **6 grupos desplegables + «Soporte» suelto** (mismo patrón que el sidebar del tenant #187; rutas SIN tocar): Negocio (Métricas·Salud·Adopción·En riesgo), Tenants (Tenants·Seguimientos), Facturación (Planes·Facturación SaaS·Dunning SaaS), Comunicación (Anuncios·Banner·Alertas·Páginas legales), Soporte (suelto, badge de tickets), Seguridad (Dashboard·Eventos·Audit·Impersonaciones·Super admins), Sistema (Colas·Cleanup webhooks). Un grupo se abre al clic y automáticamente si contiene la ruta activa (`open = openGroups[label] ?? hasActiveChild`). Se retiró «Seguridad (2FA)» del nav (duplicada en el menú de usuario). `AdminNavLinks` refactorizado (helper `AdminNavLink` hoja + grupos con `ChevronDown`). **Gotcha smoke test 04**: «Tenants» pasó a ser hijo de un grupo → el test abre el grupo si el enlace no está visible antes de clicarlo (`getByRole('link', {name:'Tenants', exact:true})`).

**🗓️ Sesión 2026-07-02 — AUDITORÍA EN PROFUNDIDAD + fixes (PRs #204, #208–#210) + páginas legales (#205–#207):** análisis completo del proyecto con 4 auditorías paralelas (seguridad, dinero, frontend, infra) → informe vivo en **`docs/AUDITORIA.md`** (solucionado / pendiente / falsos positivos). **Solucionado:** (#204) **OOM crónico del e2e** — el job de CI corre ahora **3 shards secuenciales** (`jest --shard=N/3`, proceso nuevo por shard → la fuga acumulada no crece; mismo nombre de check). (#208) **Modo oscuro roto en ~20 sitios** (combos `bg-*-100 text-*-700` sin `dark:` → añadidos `dark:bg-*-950 dark:text-*-300`; los `bg-white` de QR se conservan) + **25 aria-label** en botones solo-icono (gotcha: insertar atributos JSX con regex se rompe con los `=>` de props — usar escáner que cuente llaves). (#209) **Dinero en céntimos** en SEPA/Redsys/N43/dunning/payments/late-fees (usaban floats a pelo; el core ya usaba `common/money.ts`) + **IVA por línea** (`computeTotals` redondeaba global vs items por línea → la cabecera podía diferir céntimos de Σ items = riesgo de rechazo AEAT; ahora `invoice.total ≡ Σ items.total`; e2e `invoice-rounding`) + `setUTCDate` en el due date. (#210) **Guardas de robustez** — 🐛 **bug latente cazado por el e2e**: el unique `(tenant, series, sequence_number)` con drafts a 0 solo permitía UN borrador por serie → **la recurrente con 2+ contratos habría fallado en el 2º** (fix: índice parcial `WHERE sequence_number > 0`, la numeración solo es única emitida; migración `20260702140000`); tabla global **`cron_runs`** + helper `claimDailyCronRun` (los 2 crons sin gatear del API —PlatformDunning/PlatformAlerts— reclaman su ejecución diaria por PK → sin duplicados con réplicas); índices parciales anti-TOCTOU **`invoices_recurring_period_unique`** (409 `duplicate_period_invoice` en el create manual; catch P2002→skip en el job) y **`dunning_actions_active_unique`**; claim atómico del uso de promociones (`updateMany` condicionado); **healthcheck del worker** (script que verifica `workers:heartbeat` en Redis, wired en ambos composes); `.env.example` del API completado (19 vars). **Pendiente** (ver `docs/AUDITORIA.md`): smoke Playwright del flujo navegador del portal, unit tests billing-saas/portal, secret dedicado del portal, raw middleware Redsys, `mem_limit`, alerta de memoria Redis. **Falsos positivos** (no tocar): `noeviction` es CORRECTO para BullMQ; `useQueryClient` es estable en deps; los `@Public()` están todos autenticados. **Legales (#205–#207)**: páginas `/terminos`, `/privacidad` y `/cookies` (RGPD/LSSI-CE, doble rol responsable/encargado) **editables desde `/admin/legal`** (Markdown + vista previa; tabla global `platform_legal_documents`, contenido por defecto en `packages/shared/src/legal` como fallback; `GET /platform-legal/:slug` público) + **banner de cookies** no bloqueante (localStorage) en el layout público.

**🗓️ Sesión 2026-07-01 madrugada (PRs #195–#202) — Ciclo SaaS del super admin + modo oscuro/menú/«modo app» del portal:**

- **Tanda «mejoras de admin» (4, cierra el ciclo de negocio SaaS del super admin):** (#195) **Features por plan data-driven** — migración `subscription_plans.tenant_features` (jsonb) + helpers `normalizePlanFeatures`/`resolvePlanFeatures`/`effectiveFeaturesFromList` en `packages/shared/src/features`; el `FeatureGuard` y `/auth/me` leen las features del **plan en BD** (fallback a `PLAN_FEATURES` en código); multiselect en `/admin/plans` (antes las features por plan vivían solo en código). (#196) **Facturación del SaaS** (StorageOS→tenant) — migración `platform_billing_settings` + `platform_invoices`; `PlatformInvoicesService` numera atómico por serie/año, deriva base/IVA del total, PDF Puppeteer→MinIO best-effort; el webhook `invoice.payment_succeeded` y el pago manual generan la factura; página `/admin/platform-billing` + card en el detalle del tenant. (#197) **Dunning del SaaS** (cobro de morosos) — migración `platform_dunning_settings` + `platform_dunning_events` (idempotencia `(tenant, step, period_end)`); `PlatformDunningService.run` (recordatorios a N días + suspensión); **cron diario 08:00 sin gatear** (ligero, corre en el API); `/admin/platform-dunning`. Gotcha: `SubscriptionStatus` no tiene `suspended` (solo `TenantStatus`) → suspender toca `tenant.status`, la sub queda `past_due`. (#198) **Banner global + notificaciones del super admin** — migración `platform_banner` (singleton) + `super_admin_notifications` (feed global); `GET /platform-banner` lo lee el **tenant autenticado** (`<PlatformBanner>` en su layout, color por nivel info/warning/critical), `admin/platform/*` gestiona; abrir un ticket de soporte genera una notificación; campana con badge en el header admin. **Cadena stacked**: rebasar+resolver conflictos en cada una (`schema.prisma` relaciones de `Tenant` + modelos, `hooks.ts`/`schemas.ts`/`types.ts` de admin, `admin/layout` nav); **gotcha**: hooks/DTOs suelen quedar **entrelazados** en el conflicto (comparten el `}` de cierre) → cerrar cada bloque a mano; **regenerar Prisma** tras rebasar o el typecheck falla.
- **Portal + «modo app» (frontend puro):** (#199) **Modo oscuro del portal** — era el único panel sin dark mode; `ThemeProvider` propio en `(public)/portal/layout.tsx` (clave `storageos-portal-theme`, claro por defecto) + `ThemeToggle` en `/portal/consume` (el theme es **por sección**: `(app)`/`admin`/`portal` cada uno con su provider+clave). (#200) **Menú del portal** — 10 pestañas apelmazadas (scroll en móvil) → **sidebar vertical en ordenador + barra de navegación inferior en móvil** (4 principales Inicio·Contratos·Facturas·Accesos + botón «Más» → `Sheet`); los `TabsContent` se mantienen (el `Tabs` sigue controlado por `tab`), solo se sustituye la fila de `TabsList` por la nav custom; badges de pendientes conservados. (#201) **Home «tipo app» del STAFF en móvil** — `useIsMobile()` → `DashboardContent` early-return a `<MobileHome>` (lanzador de acciones grandes: Hoy con badge de urgentes, Nuevo contrato, Cobros, Inquilinos, Trasteros, Accesos, Incidencias, Tareas), gated por `usePermissions()` + `useFeatures()` (`useHasPermission` NO va en `.filter` → usar el array `usePermissions()`); en escritorio no cambia. (#202) **Home «tipo app» del INQUILINO** (stacked sobre #200) — potencia la `OverviewCard`: accesos rápidos como **tarjetas grandes** (icono en círculo tintado), grid 2 col móvil / 3 escritorio, 6 acciones (Pagar, Mis trasteros, Mi acceso, Mensajes con badge, Incidencias, Contratar). Idea validada por Jota con AskUserQuestion (previews ASCII): menú = «sidebar + barra inferior», modo app = ambos paneles. **Sin app nativa** (web responsive + el portal ya es PWA); follow-up: activar la PWA también en el panel del staff. **⚠️ OOM del e2e API crónico** esta sesión (heap ya en 6 GB; es fuga acumulada entre las 100+ suites `--runInBand`, no falta de límite) → `gh run rerun --failed`; follow-up real: shards o reciclar worker por memoria.

**Última actualización:** 2026-06-22 · **MVP COMPLETO + DESPLEGADO EN PRODUCCIÓN + crecimiento/CRM + revenue/operativa + pagos España + IA + portal del inquilino completo** — Fases 1 a 17 cerradas + despliegue VPS + Planes 1-4 (importador CSV, WhatsApp real, move-in + firma electrónica, Holded + Redsys). **Bloque crecimiento/CRM (PRs #17-#21):** Reviews/NPS, Promociones, Referidos, Slug+imágenes de local, Campañas segmentadas. **Tanda revenue/operativa (PRs #24-#28):** ECRI (subidas de precio a cartera), Seguro/protección recurrente, Move-out self-service + encuesta de salida, Recargos por mora (late fees), Reseñas en Google Business Profile. **Pagos España (PRs #34-#35):** Remesas SEPA (fichero pain.008) + Conciliación bancaria Norma 43. **IA (PR #36):** Asistente IA para staff con tool-use read-only y conversaciones (provider Anthropic/stub). **Bloque "Retención y portal del inquilino" COMPLETO (PRs #37-#40):** el inquilino reporta incidencias, recibe campañas de win-back, activa notificaciones push (Web Push/PWA) y solicita cambio de trastero. **Incidencia resuelta:** un 502 en prod por `resolve.symlinks=false` aplicado al build standalone (PR #32, fix: gatear a dev). Cada feature con e2e + docs. (Notas detalladas por feature más abajo en este archivo.)

**🧹 Cierre de deuda técnica (2026-06-21):** (1) **`RolesGuard` + `@Roles` retirados** tras completar RBAC v2 (eran código muerto: ningún handler usaba `@Roles` y el guard devolvía `true` para todo) — borrados `roles.guard.ts` y `roles.decorator.ts`, cadena de guards ahora `Throttler → JwtAuth → Permissions`. (2) **Gating de UI por permisos** completado: componente `<Can permission="…">` (`apps/web/src/components/auth/can.tsx`) gatea los botones de acción de las páginas principales contra el permiso exacto del backend (cosmético; el `PermissionsGuard` sigue siendo la frontera real). (3) **Swagger arreglado de raíz**: subida `nestjs-zod 4.3.1 → 5.4.0`. El antiguo `patchNestJsSwagger()` (monkey-patch en boot que petaba contra `@nestjs/swagger@11.4.3` con `ERR_PACKAGE_PATH_NOT_EXPORTED`) se sustituye por `cleanupOpenApiDoc(doc)` sobre el documento generado; el arranque dev ya no necesita `try/catch` y `/api/docs` recupera los ~121 schemas Zod. La migración fue de bajo impacto: `createZodDto`/`ZodValidationPipe` no cambian de API y zod sigue en 3.25 (no se usa `@nest-zod/z`). Verificado: typecheck monorepo 8/8 + lint + unit API 123/123 + **e2e API 314/314** + `/api/docs` con schemas.

**🧩 Bloque de valor añadido (post-deploy, 2026-06-17):** sobre el MVP desplegado se han construido varias líneas de producto (detalle por feature en `docs/ROADMAP.md` → "Backlog de valor añadido"):

- **Plan 1 — Importador CSV** (`ImportsModule`): alta masiva de **inquilinos, trasteros y contratos** desde CSV (migración desde el sistema del competidor). Motor genérico `import-engine` (parseRaw papaparse + alias de cabeceras ES/EN + preview dry-run + commit con política de duplicados). Resuelve referencias por nombre (local/tipo) y email/documento/código (cliente/trastero). Contratos como borradores. Wizard `<ImportWizard>` en `/{customers,units,contracts}/import`. e2e 9/9.
- **Plan 2 — WhatsApp Business real**: `MetaWabaProvider` (Meta Cloud API, texto + plantilla aprobada) seleccionado por `WHATSAPP_PROVIDER=stub|meta_waba` (factory en `CommunicationsModule`). **Plantillas WABA** en `message_templates` (`whatsappTemplateName/Language/Variables`) → el outbox envía por plantilla en envíos proactivos (dunning). **Editor de plantillas** CRUD en `/message-templates`. Tests `meta-waba-provider` 4/4 + `whatsapp-template-util` 3/3.
- **Plan 3 — Move-in self-service + firma electrónica simple**: tabla `contract_signatures` (firmante, método drawn/typed, imagen, **hash SHA-256 del documento**, IP/UA, canal). Firma remota por enlace (`POST /contracts/:id/request-signature` → token TTL 7d → página pública `/sign/[token]`), **asistida en el local** (pad en tablet, canal `in_person`) y **self-service público** (`/book/[slug]`: disponibilidad → alta cliente+contrato draft+token → firma → acceso emitido por `contract.signed` + 1ª factura best-effort + pago inline por Redsys con token de portal). `ContractsService.sign` acepta firma opcional; `invoices.create/issue` + `contracts.create` aceptan `userId` nulo (flujos públicos). e2e move-in 4/4.
- **Plan 4 — Pagos España**: **Holded** (`AccountingModule`): export contable por tenant con API key cifrada (`holded_settings`), `@OnEvent(invoice_issued)` empuja la factura best-effort (contacto + documento) + backfill + sync manual + `invoices.holded_document_id`. **Redsys** (`RedsysModule`, TPV pasarela alojada): config por tenant cifrada (`redsys_settings`) + `redsys_orders`; firma `HMAC_SHA256_V1` (3DES-CBC + HMAC) en `redsys-signature.ts`; `createRedirect` genera el form firmado, webhook público `/webhooks/redsys` verifica la firma y marca la factura pagada (idempotente); pago desde portal del inquilino + panel + páginas `/pay/redsys/ok|ko`. Env nueva `API_BASE_URL` (URL de notificación). Tests `holded-client` 5/5 + `holded.e2e` 4/4 + `redsys-signature` 5/5 + `redsys.e2e` 4/4. Config de ambos por `/settings/billing`; docs en `DEPLOYMENT.md` §15-17.
- **Quick wins CRM** — (a) **Notificaciones in-app** (`NotificationsModule`): tabla `notifications` (feed del tenant, RLS) + listeners `@OnEvent` (lead.created, invoice.overdue, invoice.paid, incident.created) + endpoints list/read/read-all + `<NotificationBell>` en el header (badge no leídas + dropdown). e2e 3/3. (b) **KPIs de revenue** en el dashboard: `AnalyticsService.getRevenue` (RevPAU, length-of-stay medio, LTV medio) + `GET /analytics/revenue` + `<RevenueKpiCard>`. (c) El motor de **automations** y sus triggers (`invoice_paid`, `lead_created`…) ya son funcionales vía `/automations`. **Cron `contract_ending_soon`** (`ContractEndingSoonCron` en `ContractsModule`, gated `WORKERS_ENABLED_IN_API`, diario `0 7 * * *`): emite `domain.contract_ending_soon` para contratos `active|ending` con `endDate` dentro de 30 días no avisados (idempotente vía nueva columna `contracts.ending_soon_notified_at`, migración `20260617200000`); lo escuchan las automations de renovación y un listener nuevo en `NotificationsService` (notificación in-app "Contrato vence pronto" → `/contracts/<id>`). El worker importa ahora `ContractsModule` + `NotificationsModule` para que el cron y la notificación in-app corran en producción. e2e `contract-ending-soon` 1/1. Nota: `lucide-react@0.469` no incluye `bell.js` (sí `bell-dot`) → usar `BellDot`.

**🌱 Crecimiento/CRM — Reviews / NPS (2026-06-22):** `ReviewsModule` para medir satisfacción post-contratación. Tabla `reviews` (RLS, migración `20260621120000`: `customer_id`, `contract_id?`, `token` único + `token_expires_at`, `status` pending/submitted/expired, `nps_score` 0-10, `rating` 1-5, `comment`, ip/ua). **Solicitud**: staff con `POST /reviews/request` (permiso nuevo `reviews:write`, staff+) o **cron `reviews.auto-request`** (`ReviewRequestCron`, diario `0 8 * * *`, gated `WORKERS_ENABLED_IN_API`) que pide la valoración N días tras `signedAt` para contratos sin review — **opt-in por tenant** (flags nuevos `tenants.reviews_auto_request` + `review_request_delay_days`, default OFF; `GET/PATCH /settings/tenant/reviews`). **Envío** por `CommunicationsService.enqueue` con plantilla `review_request_email` (built-in nuevo) y **fallback a cuerpo inline** si el tenant no la tiene seedeada; WhatsApp con texto. **Público** (`@Public`, throttle, bajo `/v1`): `GET /public/reviews/:token` (contexto) + `POST /public/reviews/:token` (envío con honeypot) → marca submitted + emite `domain.review_submitted` (nuevos triggers `review_request`/`review_submitted` en el enum Zod + Prisma `automation_trigger` + whitelist de variables; listener en `AutomationsService`). **Stats**: `GET /reviews/stats` → NPS (= %promotores 9-10 − %detractores 0-6), media de estrellas, tasa de respuesta. Frontend: página pública `/review/[token]` (NPS + estrellas + comentario), panel `/reviews` (tarjetas NPS/media/respuesta + ajuste de auto-solicitud gated `settings:manage` + historial), item de sidebar `Star` + permiso `reviews:read` (todos los roles). El worker importa `ReviewsModule`. e2e `reviews` 4/4.

**🌱 Crecimiento/CRM — Promociones (2026-06-22):** `PromotionsModule` expone la tabla `promotions` (existía sin CRUD ni UI; la lógica de aplicar un código ya estaba en `PricingRulesService` pero no se usaba). CRUD `GET/POST/PATCH/DELETE /promotions` (permisos nuevos `promotions:read` —todos— / `promotions:manage` —owner+manager—) + `POST /promotions/validate` (previsualiza el descuento de un código sobre un precio, permiso `contracts:write`). **Aplicación en el alta de contrato**: `CreateContractSchema.promotionCode` opcional → `ContractsService.create` (que ahora inyecta `PromotionsService`) llama `applyToContractTx(tx, …)` DENTRO de la transacción del contrato: valida (activa/vigente/`maxUses`) y fija `discountAmount` recurrente + `discountReason='Promoción <code>'` + incrementa `usedCount` atómico; **solo `percentage`/`fixed`** (los `free_months` se crean/gestionan pero `validate`/apply los rechazan con `unsupported_type` —pendiente la mecánica de facturación). Los schemas/DTOs de promoción ya vivían en `packages/shared/src/billing` (no se creó módulo nuevo): se ampliaron con `UpdatePromotionSchema`, `ValidatePromotionSchema`, `ValidatePromotionResultDto` + `createdAt`/`isActive`. Frontend: página `/promotions` (CRUD, item sidebar `Ticket`, gating `promotions:manage`) + campo "Código promocional" en el wizard de contrato (`/contracts/new`, paso 3) que valida en vivo y aplica el descuento. **Sin migración** (reusa `contracts.discount_amount`). e2e `promotions` 2/2.

**🌱 Crecimiento/CRM — Referidos (2026-06-22):** `ReferralsModule` encima de promociones. Migración `20260622120000`: `customers.referral_code` (índice único parcial por tenant, autogenerado 8 chars sin ambiguos) + tabla `referrals` (RLS: referrer/referred + `status` pending/converted + `reward_promotion_id` FK a promotions, único por `referred_customer_id`) + flags de tenant `referral_enabled`/`referral_reward_type` (enum `promotion_discount_type`)/`referral_reward_value` (opt-in). **Registro**: `CreateCustomerSchema.referralCode` (alta staff) y `PublicBookingSchema.referralCode` (self-service `/book/[slug]`) → `ReferralsService.registerInTx(tx,…)` DENTRO de la transacción del alta (best-effort: código inválido o propio no bloquea). **Conversión**: listener `@OnEvent(domain.contract_signed)` en `ReferralsService` → si hay un referral `pending` para el customer del contrato y el programa está activo, lo marca `converted` + genera una **promoción de un solo uso** (`REF-<hex>`, `maxUses=1`, recompensa del tenant; solo percentage/fixed) vía `PrismaAdminService` y la enlaza en `reward_promotion_id`. **Portal**: `GET /portal/me/referrals` (`@Public` + sesión de portal) devuelve el código (lazy-gen vía `ensureCode`), referidos y recompensas; solo si el programa está activo. **Staff**: `GET /referrals` + `/referrals/stats` (permiso nuevo `referrals:read`, todos los roles) + config `GET/PATCH /settings/tenant/referrals` (`settings:read`/`settings:manage`). El código del referidor es one-to-one con el customer (FK única en `referred_customer_id`). Frontend: panel `/referrals` (stats + ajustes + historial, item sidebar `Gift`), card "Recomienda y gana" en el portal (`/portal/consume`), campo "Código de referido" en el alta de cliente y en el booking. Módulos que importan `ReferralsModule`: customers, move-in, portal (+ app.module para el listener). e2e `referrals` 2/2.
**🌱 Crecimiento/CRM — Slug + imágenes de local (2026-06-22):** edición del `Facility.publicSlug` (ya aceptado por `Update/CreateFacilitySchema`) e **imágenes** del local para la landing pública. Migración `20260622130000`: `facilities.images String[]` (guarda **keys** de objeto MinIO, no URLs del cliente). `POST /facilities/:id/images/upload-url` (presigned PUT, patrón de los planos) + `PUT /facilities/:id/images` (fija la lista completa por keys, **valida que cada key empieza por `<tenant>/<facility>/images/`** → 404 `invalid_image_key` si no). `FacilityDto.images` = `{ key, url }[]` (la URL la construye `FilesService.buildPublicUrl`); la landing (`PublicLandingFacilityDto.imageUrls`) las muestra en `/s/[slug]/[facility]`. **Bucket público dedicado** `storageos-public` (nuevo en el `bucketMap` de `FilesService` + env `MINIO_BUCKET_PUBLIC`): se sirve con `mc anonymous set download` (createbuckets en los 3 composes) — **clave**: `uploads`/`invoices`/`plans` siguen PRIVADOS (guardan documentos de clientes y PDFs de contratos), por eso las imágenes públicas van a un bucket aparte. Frontend: pestaña "Ajustes" en `/facilities/[id]` (gated `facilities:manage`) con campo de slug + subida/preview/borrado de imágenes (sube por la URL firmada y luego `PUT` la lista de keys). e2e `facility-images` 2/2.

**🌱 Crecimiento/CRM — Campañas segmentadas (2026-06-22):** `CampaignsModule` + tabla `campaigns` (RLS, migración `20260622140000`: name, channel email, subject, body_text, `segment` jsonb, status draft/sending/sent/cancelled, audience_count, sent_count, scheduled_for). **Segmento** (discriminated union por `audience`): clientes (`contractStatus` active/none/any vía relación `contracts`, `overdueOnly` vía `invoices.status='overdue'`, `tag` vía `tags has`) o leads (`leadStatus`/`leadSource`). El resolver usa `withTenant` (RLS). `POST /campaigns/preview` cuenta la audiencia con email; `POST /campaigns` crea (borrador, congela audience_count); `POST /campaigns/:id/send` resuelve destinatarios → **renderiza subject/body por destinatario** con `renderTemplate` + whitelist `TEMPLATE_VARIABLES_BY_TRIGGER.manual` → encola una `communications` por cada uno vía `CommunicationsService.enqueue` (`source=campaign:<id>`, `scheduledFor` opcional, `customerId`/`leadId` vinculado) → marca sending→sent + sent_count (idempotente: 409 `campaign_not_sendable` si no es draft). Permisos `communications:read`/`communications:send` (sin permiso nuevo). El mensaje es **inline** (subject+body con Handlebars), no por plantilla de BD. Frontend: página `/campaigns` (lista + botón "Enviar" en drafts) + diálogo de creación (audiencia → filtros condicionales → asunto/cuerpo → "Calcular audiencia" que llama a preview → crear borrador), item sidebar `Megaphone`. e2e `campaigns` 1/1. v1 solo email (WhatsApp pendiente por requerir plantilla WABA aprobada).

**💰 Revenue — Subidas de precio a clientes en cartera / ECRI (2026-06-22):** `RentIncreasesModule` + tablas `rent_increases` (la tanda) + `rent_increase_items` (por contrato, old/new price congelados) (RLS, migración `20260622150000`). **La palanca de ingresos del self-storage**: revisa la cuota de los inquilinos activos con preaviso legal + aplicación programada. Scope: contratos `active`/`ending` + **antigüedad mínima** (`minMonthsSinceSigned` vía `signedAt <= now-N meses`) + por local/tipo. `POST /rent-increases/preview` → afectados + precio nuevo (% o € fijo, `round2`) + **delta de MRR** sin persistir; `POST /rent-increases` → congela los items, status `scheduled`, **envía el preaviso por email** (inline, best-effort, `source=rent_increase:<id>`) a cada inquilino; `POST /rent-increases/:id/apply` (manual) y el **cron `rent-increases.apply`** (diario `0 6 * * *`, gated `WORKERS_ENABLED_IN_API`, busca tandas `scheduled` con `effectiveDate<=hoy` vía `PrismaAdminService` cross-tenant y delega en `apply(tenantId,id)` con `withTenant`) → por cada item pendiente: re-chequea que el contrato sigue activo (si no, `skipped`) → `contract.priceMonthly = newPrice` + `contractEvent 'price_changed'` (payload `reason: rent_increase:<id>`) → item `applied`. **Idempotente** (solo items `pending`). La **facturación recurrente lee `priceMonthly`**, así que la siguiente factura sale al precio nuevo sin más. `POST /rent-increases/:id/cancel` (solo `scheduled`). Permiso `contracts:manage` (mutaciones) / `contracts:read` (GET), igual que change-price. El **worker importa `RentIncreasesModule`** para que el cron corra en prod. Frontend: página `/rent-increases` (lista + "Aplicar"/"Cancelar" + detalle con items) + diálogo (tipo % / € + valor + antigüedad mín. + local → "Calcular afectados" con preview de contratos y MRR → fecha efectiva → "Programar + avisar"), item sidebar `TrendingUp` (gated `contracts:manage`, filtro del sidebar generalizado). e2e `rent-increases` 1/1 (preview → programar + preaviso → aplicar → precio cambiado + evento + idempotencia).

**💰 Revenue — Seguro / protección de contenido recurrente (2026-06-22):** `InsuranceModule` + tabla `insurance_plans` (RLS, migración `20260622160000`: name, monthly_price, coverage_amount, tax_rate default 21, description, is_active) + `contracts.insurance_plan_id` (FK SET NULL) + `contracts.insurance_price` (**snapshot** congelado al asignar). Ingreso recurrente de alto margen: un plan se asigna a un contrato y la prima se factura como **una línea más en la factura mensual del alquiler**. CRUD `/insurance-plans` (permisos nuevos `insurance:read` —todos— / `insurance:manage` —owner+manager—). **Asignación** (permiso `contracts:write`, staff): en el **alta** vía `CreateContractSchema.insurancePlanId` (snapshot en `ContractsService.create` dentro de la transacción) y en la **ficha** vía `PUT /contracts/:id/insurance {planId|null}` (`ContractsService.setInsurance`). El snapshot hace que subir la tarifa del plan **no** afecte a contratos ya vinculados. **Facturación recurrente** (`billing-jobs`): si el contrato tiene `insurancePlanId` + `insurancePrice>0`, añade una línea "Protección de contenido — <plan>" con `taxRate` del plan (reusa el array `items` del job; la query ya incluye `insurancePlan`). `ContractDto` gana `insurancePlanId`/`insurancePlanName`/`insurancePrice` (include `insurancePlan` añadido a las 4 queries de contrato). Se vende como **servicio de protección** (IVA 21%), no seguro regulado (el IPS 8% queda como opción vía `tax_rate`). Frontend: página `/insurance-plans` (CRUD, sidebar `Shield`) + card "Seguro / protección" en la ficha del contrato (selector asignar/quitar, gated `contracts:write`) + selector en el paso 3 del wizard de alta (`/contracts/new`, solo si hay planes activos). e2e `insurance` 1/1 (plan → contrato con seguro → snapshot inmutable al subir tarifa → línea en la factura recurrente vía `processGenerateRecurring` → quitar seguro).

**🚪 Operativa — Move-out self-service + encuesta de salida (2026-06-22):** el inquilino solicita la baja desde el portal, cerrando el ciclo del local desatendido (el move-in ya era self-service). **Sin migración** (reusa `contracts.status/endDate/endingRequestedAt/cancellationNoticeDays`). Backend: `GET /portal/me/contracts` (contratos active/ending del customer) + `POST /portal/me/contracts/:id/request-move-out {endDate}` (`@Public` + `requirePortalSession` + throttle login) → `ContractsService.requestEndByCustomer`: valida propiedad (404 si no es suyo) + **preaviso** (`endDate ≥ hoy + cancellationNoticeDays`, 400 `notice_period_not_met`) → contrato a `ending` + `endDate` + `contractEvent 'ending_requested'` (payload `channel: portal`, `createdByUserId: null`) → emite `DOMAIN_EVENTS.contract_move_out_requested` (nuevo). **Dos listeners** (event-driven, sin acoplar módulos): `NotificationsService.onMoveOutRequested` → notificación in-app al staff ("Baja solicitada — <contrato>" → `/contracts/<id>`); `ReviewsService.onMoveOutRequested` → **encuesta de salida NPS** best-effort (reusa `request()`). `PortalModule` importa `ContractsModule` (exporta `ContractsService`); `requestEndByCustomer` + `listForCustomer` devuelven `PortalContractDto`. **Decisión**: la baja deja el contrato en `ending` (el staff hace el check-out/cierre en la fecha; sin cron de auto-finalización). Frontend: card "Mis contratos" en `/portal/consume` + diálogo "Solicitar baja" (date input nativo con `min` = hoy+preaviso). e2e `move-out` 2/2 (lista + preaviso 400 + baja → ending + encuesta creada + cross-customer 404). **Gotcha**: `reviews.list` devuelve `{ items, nextCursor }` sin `total` (el `total` del DTO es vestigial); en tests usar `items.length`.

**💸 Cobro — Recargos por mora / late fees (2026-06-22):** activa el tipo `late_fee` del dunning (ya existía en el enum pero **sin implementar**). Migración `20260622170000`: flags de tenant `late_fee_enabled` (opt-in) + `late_fee_type` (percentage/fixed) + `late_fee_value` (default 5) + `late_fee_grace_days` (default 7) + `invoices.late_fee_for_invoice_id` (FK self, **único parcial** → idempotencia). El recargo es una **factura SEPARADA** (no se puede modificar la original ya emitida con hash Veri\*Factu encadenado): `InvoicesService.createLateFee` crea+emite una F1 con una línea "Recargo por mora — factura `<num>`" e **IVA 0** (es indemnizatorio, no servicio), importe = % del `total` vencido o € fijo según config; idempotente (409 `late_fee_already_applied` si ya existe, garantizado por el índice único). **Automático**: `DunningService.scheduleActions` agenda el paso `late_fee` a `grace_days` **solo si el tenant lo activó**; `executeAction('late_fee')` re-chequea el opt-in y llama `createLateFee` (`userId: null`). `DunningModule` importa `BillingModule` (DunningService inyecta `InvoicesService` — sin ciclo, BillingModule no importa Dunning). **Manual**: `POST /invoices/:id/late-fee` (`invoices:manage`). Config en `GET/PATCH /settings/tenant/billing` (extendido; `autoChargeOnIssue` pasó a opcional + audit `tenant.billing.settings_changed`). `InvoiceDto` gana `lateFeeForInvoiceId` (si es un recargo) + `lateFeeInvoiceId` (si la vencida ya tiene recargo; vía relación inversa en `includeRelations`). Frontend: card "Recargo por mora" en `/settings/billing` (toggle + tipo + valor + días) + botón "Aplicar recargo" / "Ver recargo" en la factura vencida (`/invoices/[id]`, gated `invoices:manage`). e2e `late-fees` 1/1 (config → emitir recargo 6.05€=5% de 121 sin IVA → enlace bidireccional → reaplicar 409) + unit `dunning.service` 5/5 (constructor con el nuevo arg `invoices`). **Gotcha**: la respuesta de `/settings/tenant/billing` ahora trae los 5 campos (auto-charge + late fee); el e2e `auto-charge` pasó de `toEqual` exacto a `toMatchObject`.

**🌟 Crecimiento/SEO — Reseñas en Google Business Profile (2026-06-22):** tras una valoración NPS positiva (promotor), invita al inquilino a dejar la reseña en Google → mejora el ranking local y la captación orgánica. Migración `20260622180000`: `tenants.google_review_url` (nullable). Config en `GET/PATCH /settings/tenant/reviews` (extendido con `googleReviewUrl`; los otros campos pasaron a opcionales). `ReviewsService.submitByToken` ahora devuelve `SubmitReviewResultDto = { status, googleReviewUrl: string | null }`: si `npsScore >= 9` (promotor) **y** el tenant configuró su link, lo devuelve; detractores/pasivos reciben `null` (se gestionan en privado — patrón de review-gating estándar). Frontend: campo "Enlace de reseña en Google" en la card de ajustes de `/reviews` + **CTA "Reseñar en Google"** en la página pública `/review/[token]` (solo si la respuesta trae `googleReviewUrl`). Sin permiso nuevo (reusa `settings:read`/`settings:manage`). e2e `google-reviews` 1/1 (config → promotor NPS 10 recibe el link → detractor NPS 3 recibe `null`).

**🇪🇸 Pagos España — Remesas SEPA (adeudos directos, fichero bancario) (2026-06-22):** `SepaModule` para cobrar las facturas domiciliadas generando el **XML pain.008.001.02** (esquema CORE, AEB 19.14) que el operador sube a su banco — **sin comisiones de pasarela**. Migración `20260622190000`: 4 tablas (RLS) — `sepa_settings` (acreedor: nombre, identificador del acreedor, **IBAN cifrado AES-GCM** como redsys/holded, BIC, enabled; única por tenant), `sepa_mandates` (por cliente: IBAN cifrado + last4 + referencia única autogenerada `MND-…` + fecha de firma + `sequence_type` FRST/RCUR + status; índice parcial **único mandato activo por cliente**), `sepa_remittances` (lote: message_id, collection_date, status generated/confirmed/cancelled, xml, totales) + `sepa_remittance_items` (uno por factura, `invoice_id` **único** → una factura en una sola remesa). Validación **IBAN mod-97** (`isValidIban` en `packages/shared/src/sepa`). **Builder `buildPain008`** (`sepa-pain008.ts`, función pura): agrupa por `SeqTp` (FRST/RCUR) en bloques `PmtInf`, `CdtrSchmeId` con el identificador del acreedor, `MndtRltdInf` por adeudo, BIC o `NOTPROVIDED`, importes EUR 2 decimales + `CtrlSum`/`NbOfTxs`. Flujo: `GET/PUT /sepa/settings` (`settings:read`/`billing:configure`; IBAN opcional al actualizar → conserva el actual), `GET/POST/DELETE /sepa/mandates` (`payments:read`/`payments:charge`; crear cancela el mandato activo previo), `POST /sepa/remittances/preview` (facturas issued/overdue con mandato activo y sin remesa + lista `withoutMandate`), `POST /sepa/remittances` (genera el XML + items, **decisión: facturas quedan "en remesa / cobro en curso"**, no se marcan pagadas), `GET /sepa/remittances/:id/xml` (devuelve `{filename, xml}`; el front descarga el blob), `POST /sepa/remittances/:id/confirm` (marca cada factura pagada vía `InvoicesService.markPaidManually` methodType `sepa_debit` + pasa los mandatos FRST→RCUR). `SepaModule` importa `BillingModule` (InvoicesService) + `CryptoService` (global). Permisos: remesas (preview/create/confirm/xml) `invoices:manage`; lecturas `payments:read`. Frontend: card "Remesas SEPA (acreedor)" en `/settings/billing`, sección "Mandato SEPA" en la pestaña Pagos del cliente (`payment-methods-tab`), página `/sepa-remittances` (lista + diálogo nueva remesa con preview/selección de facturas + descargar XML + confirmar cobro), item sidebar `Landmark`. e2e `sepa-remittances` 2/2 (config + mandato → remesa → XML pain.008 validado → confirmar → factura pagada + mandato RCUR; + factura sin mandato en `withoutMandate`). **Gotcha**: `confirm` (nombre de la mutación en el front) tapa a `window.confirm`; usar `window.confirm`.

**🏦 Pagos España — Conciliación bancaria Norma 43 (2026-06-22):** `BankReconciliationModule` para casar los cobros con el extracto del banco — complementa las remesas SEPA. Migración `20260622200000`: `bank_statements` (extracto: cuenta, fechas, saldos en céntimos) + `bank_statement_transactions` (movimiento: fechas operación/valor, `amount` céntimos **con signo** (+ abono / − cargo), conceptos, referencias, `status` pending/matched/ignored, `matched_invoice_id` FK SET NULL) (RLS). **Parser `parseN43`** (`n43-parser.ts`, función pura): registros AEB 80 chars — `11` cabecera de cuenta (fechas AAMMDD, saldo inicial), `22` movimiento (debe `1`/haber `2` → signo, importe 14 díg = céntimos, refs), `23` concepto complementario (texto libre → `description`), `33` final de cuenta (saldo final), `88` fin; tolera líneas cortas (padEnd 80) y un `22` sin `11` previo se ignora. Flujo (**decisión: auto-sugerencia + confirmación manual, v1 solo abonos**): `POST /bank-statements/import {filename, content}` (el front lee el fichero como texto y lo manda JSON; 400 `invalid_n43`) crea un `bank_statement` por bloque de cuenta + movimientos; `GET /bank-statements` (matchedCount) + `GET /bank-statements/:id` (por cada **abono pendiente** calcula `suggestions` = facturas issued/overdue con `amountPending` exacto, la referencia/concepto que contiene el nº de factura desempata); `POST /bank-statements/transactions/:id/match {invoiceId}` marca la factura pagada vía `markPaidManually` (methodType `bank_transfer`, importe = pendiente) + tx `matched`; `POST .../ignore`. Los **cargos** se muestran informativos (no se concilian en v1). Permisos: import/match/ignore `invoices:manage`; lecturas `payments:read`. `BankReconciliationModule` importa `BillingModule`. Frontend: página `/bank-reconciliation` (subir N43 + lista de extractos + detalle con botón "Conciliar <nº factura>" sobre la sugerencia top + "Ignorar"), item sidebar `ArrowLeftRight`. unit `n43-parser` 2/2 + e2e `bank-reconciliation` 2/2. **Gotcha**: los unit specs del API van en `__tests__/` (jest `testMatch` = `**/__tests__/**/*.spec.ts`), no sueltos en `src/modules`. **(2026-06-22) Devoluciones SEPA (R-transactions):** los **cargos** ya no son solo informativos — por cada cargo pendiente, `getStatement` calcula `returnSuggestions` = facturas **`paid`** con `amountPaid` == importe del cargo (referencia desempata); `POST /bank-statements/transactions/:id/mark-return {invoiceId}` (`invoices:manage`) llama `InvoicesService.revertPayment` (nuevo; mismo patrón que el revert de disputas Stripe: resta `amountPaid`, marca los `payment` succeeded como `failed`, devuelve la factura a `overdue`/`issued`, `paidAt=null`, audit `invoice.payment_reverted`) y marca la tx con el nuevo status **`returned`** (añadido al union de status de `BankTransactionDto`). El dunning reabre solo al estar la factura `overdue`. Frontend: en los cargos, botón rojo "Devolución <nº factura>" (sobre `returnSuggestions`) + badge "Devuelta". e2e `bank-reconciliation` ahora **3/3** (cobro → pagada → cargo del mismo importe sugiere la factura → mark-return → vuelve a vencida + amountPaid 0).

**🤖 Asistente IA para staff (2026-06-22):** `AiModule` — chat con acceso a datos (tool-use read-only) y conversaciones persistidas. Migración `20260622210000`: `ai_conversations` (tenant + **user** + title) + `ai_messages` (role user/assistant, content, `tools_used` jsonb) (RLS; conversaciones **por usuario**). **Provider abstracto** `AiProvider` (forma = Messages API de Anthropic) + `StubAiProvider` (determinista: según el texto del usuario decide invocar herramienta y al recibir el resultado responde; dev/test/CI **sin coste ni API key**) + `AnthropicAiProvider` (Claude real vía **`fetch`** a `api.anthropic.com/v1/messages`, **sin SDK** para no añadir dependencia). Factory por env nueva `AI_PROVIDER=stub|anthropic` (+ `ANTHROPIC_API_KEY`, `AI_MODEL` default `claude-sonnet-4-6`). **`AiToolsService`** — 5 herramientas read-only ejecutadas con `withTenant` (RLS, nunca cruzan tenant): `get_business_metrics` (MRR/contratos/ocupación/pendiente), `get_occupancy` (por local), `list_overdue_invoices`, `search_customers`, `get_customer_summary`. **`AiService.chat`** corre el **loop agéntico** (máx 5 iter): persiste el mensaje del usuario, reconstruye el histórico (solo texto) como contexto, llama al provider; si `stop_reason=tool_use` ejecuta las herramientas → `tool_result` → repite; al terminar persiste la respuesta + `tools_used` (transparencia). Si el provider no está disponible (anthropic sin key) → 503 `ai_not_configured`. Endpoints (`@RequirePermission('ai:use')` — **permiso nuevo**, owner/manager/staff): `POST /ai/chat {conversationId?, content}`, `GET /ai/conversations`, `GET /ai/conversations/:id`, `DELETE /ai/conversations/:id` (todo **scoped por userId** además de RLS). Frontend: página `/assistant` (chat con lista de conversaciones + sugerencias iniciales + burbuja que muestra "Consultó: …" con las herramientas usadas), item sidebar `Bot` gated `ai:use`. e2e `ai-assistant` 2/2 (con stub: crea conversación → usa herramienta → persiste 4 mensajes → borra; + aislamiento cross-user 404). **Gotcha**: en tests, `registerVerifiedUser(app, suffix)` usa el suffix en el slug → solo minúsculas/dígitos.

**🎫 Portal — Reportar incidencias (2026-06-22):** el inquilino abre incidencias desde su portal (antes solo staff). **Sin migración** (reusa `incidents`). `IncidentsService.createFromPortal({tenantId, customerId, input})` crea la incidencia con `reportedByUserId: null` + `reportedByExternal: 'Portal: <nombre>'` + `customerId` + severity `medium`, y **emite `incident_created` siempre** (no solo en severity alta) → `NotificationsService` avisa al staff in-app. `IncidentsService.listForCustomer` devuelve un `PortalIncidentDto` slim (id/título/descr/status/severity/fecha). Endpoints portal (`@Public` + `requirePortalSession`): `GET /portal/me/incidents` + `POST /portal/me/incidents {title, description?}` (throttle login). `PortalModule` importa `OperationsModule` (exporta `IncidentsService`). Frontend: card "Incidencias" en `/portal/consume` (form asunto+detalle + lista con estado traducido Recibida/En curso/Resuelta/Cerrada). e2e `portal-incidents` 1/1 (401 sin token → lista vacía → reportar → el inquilino la ve + el staff la encuentra en `/incidents` → título corto 400). Primera pieza del bloque "Retención y portal del inquilino" (siguen: win-back, cambio de trastero, push PWA).

**🔁 CRM — Win-back de bajas (2026-06-22):** segunda pieza del bloque. Nuevo valor **`former`** en `contractStatus` del segmento de clientes de `CampaignsModule` (`packages/shared/src/campaigns`): ex-clientes = `where.AND` de `contracts.some(status in ended/cancelled)` + `contracts.none(status in active/ending)`. Permite lanzar una campaña de recuperación a quienes se dieron de baja, desde `/campaigns` (opción "Ex-clientes (win-back)" en el selector de contrato). Sin migración. e2e `campaigns-winback` 1/1 (contrato finalizado=former, activo excluido, sin-contrato excluido).

**🔔 Portal — Notificaciones push (Web Push / PWA) (2026-06-22):** cuarta pieza del bloque. El inquilino activa push en su dispositivo (PWA del portal) y recibe avisos de sus facturas. Migración `20260622220000`: `push_subscriptions` (tenant + customer + `endpoint` único + p256dh + auth) (RLS). Dependencia **`web-push`** (en `apps/api` y `apps/worker`). **`PushService`** (en `PushModule`): si hay claves **VAPID** (`VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`) activo, si no **no-op** (dev/test/CI sin claves). `getPublicKey()` (null si no configurado), `subscribe`/`unsubscribe` (upsert/delete por endpoint), `sendToCustomer` (envía a todas las subs; **borra las caducadas** ante 404/410). **Listeners `@OnEvent`** `invoice_overdue` + `invoice_paid` → push al `p.customerId`. Endpoints portal (`@Public` + `requirePortalSession`): `GET /portal/me/push/public-key`, `POST /portal/me/push/subscribe {endpoint, keys}`, `POST /portal/me/push/unsubscribe {endpoint}`. `PushModule` en **app.module (API)** y **worker.module** (el dunning que emite `invoice_overdue` corre en el worker en prod). SW `public/sw.js`: handlers `push` + `notificationclick`. Frontend: card "Notificaciones" + botón "Activar notificaciones" en `/portal/consume` (solo si `public-key` ≠ null). e2e `portal-push` 1/1. **Activar en prod**: `npx web-push generate-vapid-keys` → variables de Portainer (mismas en api y worker). Cierra el bloque "Retención y portal del inquilino" salvo (b) cambio de trastero (pendiente).

**📦 Portal — Solicitar cambio de trastero (2026-06-22):** última pieza del bloque (queda **completo**). El inquilino pide un cambio/upgrade de trastero desde el portal y el staff lo gestiona. Migración `20260622230000`: `unit_change_requests` (tenant + customer + `contract_id?` + note + status pending/handled/rejected + resolution_note + handled_by/at) (RLS). `UnitChangesModule`: `createFromPortal` (valida que el contrato indicado es del cliente; **notifica al staff** vía `NotificationsService.create` type `unit_change.requested` → `/unit-change-requests`), `listForCustomer`, `list` (staff, filtro status), `resolve` (pending→handled/rejected, 400 `already_resolved`). Endpoints staff `GET /unit-change-requests?status=` (`contracts:read`) + `PATCH /unit-change-requests/:id {status, resolutionNote?}` (`contracts:write`). Endpoints portal (`@Public` + `requirePortalSession`): `GET/POST /portal/me/unit-change-requests`. `NotificationsModule` ahora **exporta** `NotificationsService`. Frontend: card "Cambiar de trastero" en `/portal/consume` (selector de contrato + nota + lista) + página staff `/unit-change-requests` (cola con filtro + Gestionar/Rechazar, sidebar `Replace`, gated `contracts:read`). e2e `unit-change-requests` 1/1. **Gotcha**: el narrowing de `input.contractId` se pierde dentro del callback de `withTenant` (closure) → capturar en const antes.

**🎨 Rediseño del panel (2026-06-22):** lavado de cara minimalista/moderno del panel staff (frontend-only, sin tocar backend). (1) **Sidebar reorganizado en 7 grupos** con cabeceras (`Principal`/`Operativa`/`Inventario`/`Facturación y cobros`/`Accesos`/`Crecimiento (CRM)`/`Análisis`) — `app-sidebar.tsx` pasa de una lista plana de 26 items a `GROUPS: NavGroup[]`. **Filtrado genérico por permiso**: cada item tiene su `permission?` y se filtra con `usePermissions().includes(...)` (antes solo miraba 2 permisos ad-hoc); **un grupo se oculta entero si no quedan items visibles** → fácil ocultar opciones a usuarios/tenants que no tengan el permiso (i18n en `sidebar.groups.*`). (2) **Tokens de tema** (`globals.css`): `--radius` 0.5→0.75rem, **sidebar blanco** con item activo = **píldora azul tenue** (`--sidebar-accent` azul claro + `--sidebar-accent-foreground` azul), nuevo `--app` (gris muy claro) para el fondo de la zona de contenido → tarjetas blancas "flotando". `SidebarGroupLabel` a mayúsculas + tracking. `Card` a `rounded-xl` + `shadow-card`. tailwind: color `app` (con `<alpha-value>`) + sombras `card`/`soft`. (3) **Layout**: `SidebarInset` con `bg-app`; **header sticky + backdrop-blur**. (4) **Dashboard** con `StatCard` (icono en círculo tintado + label + valor). Acento **azul corporativo** (no violeta), blanco y gris; tipografía **Geist** ya existente (solo se ajustaron pesos/tracking). Verificado: typecheck 8/8 + lint + build web.

**🔒 Gating por plan del tenant / entitlements (2026-06-22):** vender planes que muestran u ocultan módulos. **Catálogo de features** en `packages/shared/src/features`: `TenantFeature` (7 premium: `ai_assistant`, `sepa`, `bank_reconciliation`, `rent_increases`, `insurance`, `access_control`, `automations`) + `PLAN_FEATURES` (mapa **por slug**: free=∅, starter=+ECRI/seguros/accesos/automatizaciones, pro=todas) + `featuresForPlan(slug)` (**slug desconocido → todas**, default seguro) + `FEATURE_LABELS`. **Sin migración** (aunque `subscription_plans.features` jsonb existe, v1 usa el mapa en código por slug; data-driven editable queda como follow-up). Backend: `/auth/me` (`MeResponse.features: TenantFeature[]`) lo expone vía `featuresForPlan(subscription.plan.slug)` en `AuthService.me`. Frontend: `useFeatures()`/`useHasFeature()`; el **sidebar** añade `feature?` a los 7 items premium y los filtra (`can(permission) && hasFeature(feature)`); cada **ruta premium tiene un `layout.tsx`** que envuelve en `<FeatureGate feature="…">` (`apps/web/src/components/auth/feature-gate.tsx`) → si el plan no la incluye, muestra upsell "… no está en tu plan" + CTA a `/settings/saas-billing` en vez del contenido (evita montar los hooks de la página). v1 = gating de **menú + página (frontend)**; **el backend sigue respondiendo** (no infranqueable — `@RequireFeature` en endpoints queda como follow-up). e2e `auth.me` extendido (starter trae rent_increases/insurance/access_control/automations, no ai_assistant/sepa/bank_reconciliation). **Nota operativa**: un tenant en `starter` no ve IA/SEPA/conciliación; para acceso total ponerlo en `pro`. **Gotcha**: `MeResponse` ahora exige `features` → los `setQueryData(meQueryKey, …)` optimistas de login/register lo rellenan con `featuresForPlan(data.subscription.planSlug)`. **Cambio de plan desde super-admin**: `POST /admin/tenants/:id/change-plan {planSlug, reason}` (`AdminGuard`) → `AdminTenantsService.changePlan` busca el plan por slug (404 `plan_not_found`), actualiza `tenant_subscription.planId` (no toca Stripe) + audita (`audit_logs` + `super_admin_audit_logs`, action `admin.tenant.plan_changed`); shared `ChangePlanSchema`. UI: selector inline "Cambiar plan" en la card Suscripción de `/admin/tenants/[id]` (`useChangePlan` + `useAdminSubscriptionPlans` que lista `/subscription-plans`). Así el super-admin mueve un tenant entre free/starter/pro → cambia qué módulos premium ve. e2e `admin` extendido (starter→pro + plan inexistente 404).

**🧾 Fiscalidad España — Libro de IVA + Modelo 303 + Modelo 347 (2026-06-22):** `FiscalModule` — informes fiscales **solo lectura** derivados de las facturas emitidas (no incluye IVA soportado/compras, que el sistema no gestiona). **Sin migración.** Estados que cuentan a efectos fiscales (`FISCAL_STATUSES`): todo salvo `draft`/`cancelled`; el devengo se mide por `issue_date`; base por línea = `item.total − item.taxAmount`, cuota = `item.taxAmount` (las rectificativas R netan en negativo). `GET /fiscal/vat-book?from=&to=` (libro registro de facturas expedidas: filas por factura + desglose por tipo + totales), `GET /fiscal/model-303?year=&quarter=` (IVA devengado por tipo del trimestre), `GET /fiscal/model-347?year=` (clientes con operaciones > **3.005,06 €/año** —`MODEL_347_THRESHOLD` en `packages/shared/src/fiscal`— con NIF, desglose trimestral T1-T4). Permiso `invoices:manage` (contabilidad, owner+manager). Frontend: página `/fiscal` (3 pestañas con selectores de periodo + tablas + **export CSV** cliente con BOM+`;` para Excel español), item sidebar en COBROS (`FileSpreadsheet`, gated `invoices:manage`). e2e `fiscal` 1/1 (factura base 3000 + 21% → libro/303 base 3000 cuota 630, 347 incluye al cliente >umbral; año vacío + año inválido 400). **Gotcha**: el BOM del CSV debe escribirse como `\uFEFF` (no carácter literal) o ESLint `no-irregular-whitespace` falla.

**🚀 Desplegado en producción (2026-06-16):** vivo sobre VPS con Portainer + Nginx Proxy Manager, como **stack de tipo Repository en Portainer** desde `docker-compose.portainer.yml` (clona el repo y construye las imágenes; variables por la sección Environment de Portainer, no `env_file`). Para que funcionara se sanearon los Dockerfiles de producción (que nunca se habían construido end-to-end): copiar `apps/worker/package.json` en api/web, `PUPPETEER_SKIP_DOWNLOAD=true` en el stage `deps`, copiar el Prisma client generado al `/out` del `pnpm deploy` (no lo incluye de forma fiable), declarar `multer`/`express`/`prisma` (CLI) como dependencies (imports directos no declarados), quitar `image:` de los servicios build-only (Portainer hacía `pull` de un registry inexistente), `z.coerce.boolean` → patrón `union('true'/'false')` en `OPENAPI_ENABLED`/`ENABLE_WORKERS_IN_API` (coerce de `"false"` daba `true`), Redis `maxmemory-policy noeviction` (BullMQ no debe perder jobs) y `MqttLockProvider` que solo conecta si `LOCK_PROVIDER=mqtt`. **Bootstrap de producción**: `apps/api/src/scripts/bootstrap.ts` (→ `dist/scripts/bootstrap.js`, idempotente) siembra planes `free/starter/pro` (el registro exige `starter`) + crea super admin si `BOOTSTRAP_SUPERADMIN_EMAIL/PASSWORD`, como servicio one-shot `bootstrap` en el compose o `node dist/scripts/bootstrap.js` (sustituye a `pnpm db:seed`, que no corre en prod). Gotchas: passwords de servicio alfanuméricas (la validación `z.string().url()` rechaza `/`,`+`,`=`); Postgres solo aplica `POSTGRES_PASSWORD` al crear el volumen (cambiarla luego requiere recrear volumen o `ALTER ROLE`); NPM en la red `storageos-net` con Proxy Hosts Scheme **http** → `api:3001`/`web:3000`. Detalle en `docs/DEPLOYMENT.md` (§6B Portainer, §7 bootstrap).

**🚀 Desplegado en producción (2026-06-16):** vivo sobre VPS con Portainer + Nginx Proxy Manager, como **stack de tipo Repository en Portainer** desde `docker-compose.portainer.yml` (clona el repo y construye las imágenes; variables por la sección Environment de Portainer, no `env_file`). Para que funcionara se sanearon los Dockerfiles de producción (que nunca se habían construido end-to-end): copiar `apps/worker/package.json` en api/web, `PUPPETEER_SKIP_DOWNLOAD=true` en el stage `deps`, copiar el Prisma client generado al `/out` del `pnpm deploy` (no lo incluye de forma fiable), declarar `multer`/`express`/`prisma` (CLI) como dependencies (imports directos no declarados), quitar `image:` de los servicios build-only (Portainer hacía `pull` de un registry inexistente), `z.coerce.boolean` → patrón `union('true'/'false')` en `OPENAPI_ENABLED`/`ENABLE_WORKERS_IN_API` (coerce de `"false"` daba `true`), Redis `maxmemory-policy noeviction` (BullMQ no debe perder jobs) y `MqttLockProvider` que solo conecta si `LOCK_PROVIDER=mqtt`. **Bootstrap de producción**: `apps/api/src/scripts/bootstrap.ts` (→ `dist/scripts/bootstrap.js`, idempotente) siembra planes `free/starter/pro` (el registro exige `starter`) + crea super admin si `BOOTSTRAP_SUPERADMIN_EMAIL/PASSWORD`, como servicio one-shot `bootstrap` en el compose o `node dist/scripts/bootstrap.js` (sustituye a `pnpm db:seed`, que no corre en prod). Gotchas: passwords de servicio alfanuméricas (la validación `z.string().url()` rechaza `/`,`+`,`=`); Postgres solo aplica `POSTGRES_PASSWORD` al crear el volumen (cambiarla luego requiere recrear volumen o `ALTER ROLE`); NPM en la red `storageos-net` con Proxy Hosts Scheme **http** → `api:3001`/`web:3000`. Detalle en `docs/DEPLOYMENT.md` (§6B Portainer, §7 bootstrap).

Lo construido hasta hoy: **Fase 1** (auth + multi-tenant), **Fase 2** (facilities + units + editor visual), **Fase 3** (customers + contracts + reservations EXCLUDE), **Fase 4** (Verifactu ready con hash encadenado + Stripe gateway + BullMQ recurrente + dunning + RGPD + customer portal), **Fase 5** (EmailProvider abstracto Resend/SMTP + WhatsApp stub + plantillas Handlebars con whitelist + outbox communications + motor de automations EventEmitter2 + leads kanban + widget público embebible), **Fase 6** (tasks + incidents con state machines y comentarios + productos accesorios con stock por facility y venta inline a invoice + analytics 4 KPIs + reports async con Puppeteer/exceljs en MinIO), **Fase 7** (credenciales PIN/QR/RFID con state machine y secret revealed-once + devices con API key + LockProvider abstracto MQTT/stub + verify endpoint con audit trail completo), **Fase 8** (super admin con login propio + impersonation auditada + métricas + support tickets bidireccionales + Stripe Billing SaaS con Checkout y Customer Portal + dockerfiles multi-stage + docker-compose.prod + DEPLOYMENT.md paso a paso + backup automatizado + listener contract.signed crea credencial + dunning gate access*block), **Fase 9 — Hardening pre-MVP** (seed CLI super admin idempotente + 2FA TOTP super admin con recovery codes + refresh cookie httpOnly `path=/admin` con rotación paranoid + AeatClient abstracto con factory por `AEAT_MODE` + Resend producción documentado en DEPLOYMENT.md), **Fase 10 — Veri\*Factu real** (`tenant_aeat_credentials` cifrado AES-GCM con upload de PKCS#12 + `VerifactuXmlBuilder` conforme al XSD oficial + `RealAeatClient` con mTLS via `https.Agent` + cola BullMQ `verifactu` con retry exponencial + endpoint `POST /billing/invoices/:id/resend-aeat` + UI `/settings/billing/verifactu` y `<VerifactuBadge>` en facturas), **Fase 11 — Compliance + observabilidad post-MVP** (tabla global `security_events` para login_failed sin tenant context + endpoint admin con filtros y cron de limpieza a 90d + histórico de `tenant_aeat_credentials` quitando UNIQUE y rotando vía `$transaction` + CSP `Report-Only` en panel autenticado con endpoint `/api/csp-report` + rectificativas Veri\*Factu R1-R5 por diferencias con `<TipoRectificativa>` y `<FacturasRectificadas>` en el XML AEAT), **Fase 12 — Hardening operacional adicional** (flag tenant `requireTwoFactorForManagers` con flow de enrolment forzado en login y página pública `/security/enrolment/[token]` + `SecurityAlertsService` con `groupBy` sobre `security_events` y dedup in-memory y cron `*/5 \* \* \* _`+ tabla global`super_admin_audit_logs`con endpoint admin y página`/admin/audit-logs`+ 5 smoke tests Playwright`apps/web/e2e/`) , **Fase 13 — Robustez técnica pre-venta** (worker separado `apps/worker`con processors BullMQ + crons fuera del proceso API + Dockerfile + servicio en`docker-compose.prod.yml`+ OpenAPI 3.x en`/api/docs`con`@nestjs/swagger`+ API versioning URI`/v1/`con legacy redirect 308 + F2 facturas simplificadas con`customer_id NULLABLE`y límites 400€/3000€ + rectificativas por sustitución con`<TipoRectificativa>S</...>`y bloque`<ImporteRectificacion>`+ CSP enforce promovida desde Report-Only + workflow`.github/workflows/e2e.yml`con Playwright en CI no bloqueante) **Fase 14 — Hardening final pre-deploy** (flag`ENABLE_WORKERS_IN_API`con default`true`que apaga Processors + Crons en el API cuando arranca el worker aparte + refactor service/processor para que el wrapper se pueda condicionar + tests del worker con`ioredis-mock`via`moduleNameMapper`+ API keys`sk_live_<tenantId>.<secret>`revealed-once con`ApiKeyGuard`Bearer + webhooks salientes con HMAC SHA-256 al estilo Stripe + cola BullMQ`webhooks`con retry 3× exponencial + listeners`invoice.paid/issued/overdue`, `contract.signed`, `lead.created`+ UI tab`/settings/integrations`) y **Fase 15 — Cierre de TODOs y operabilidad** (AEAT `getStatus`polling con XML SOAP`ConsultaFactuSistemaFacturacion`+`VerifactuStatusPollerCron`cada 15 min sobre invoices`pending`huérfanas con`take: 50`batch + endpoint manual`POST /v1/billing/invoices/:id/refresh-aeat-status`+ botón "Consultar AEAT" en`<VerifactuBadge>`+ dashboard`/settings/webhooks/[id]`con deliveries paginadas cursor + filtros + retry manual`POST /v1/settings/webhooks/:webhookId/deliveries/:deliveryId/retry`con reset`attempts=0`antes de encolar + scopes API keys enforced con decorador`@RequireScope(scope)` + whitelist 5 scopes (`invoices:read/write`, `contracts:read`, `customers:read`, `webhooks:trigger`) + `ApiKeyGuard`con`Reflector`+ endpoint`GET /v1/integrations/whoami`como ejemplo + UI multiselect scopes en dialog crear API key) y **Fase 16 — Cierre de TODOs residuales + RGPD tenant** (tests del worker`worker-bootstrap`2/2 y del flag`workers-flag`3/3 activados desde`describe.skip`+`email_reminder`del dunning conectado al outbox vía`CommunicationsService.enqueue`(plantilla`invoice_overdue_email`) + fix de seguridad `AdminGuard`en el catálogo de planes SaaS +`SecurityThrottlerGuard`que persiste`login_failed_throttled`/`register_throttled`/`password_reset_throttled`en`security_events`+ anonimización RGPD del tenant`POST /admin/tenants/:id/anonymize`con UI de confirmación por slug en`/admin/tenants/[id]`preservando facturas) y **Fase 17 — Pagos SEPA + portal de cobro + hardening pre-deploy** (webhooks Stripe idempotentes con tabla`processed_stripe_events`+ fin del doble-conteo de`amountPaid`+ SEPA Direct Debit vía Stripe`sepa_debit`con disputes/R-transactions + self-service de IBAN y pago de facturas en el portal del inquilino + auto-charge opt-in al emitir factura con emisión por fin de`domain.invoice_issued`+ Sentry en API/worker +`GET /health/ready`y`GET /health/worker`+ reparación del historial de migraciones + dinero en céntimos enteros + heartbeat de workers + visibilidad de colas`/admin/queues`). El MVP está **listo para vender, emitir facturas F1/F2 conformes, rectificarlas por diferencias o sustitución, reconciliar pendings AEAT con polling automático, forzar 2FA al owner/manager, exponer una spec OpenAPI versionada, correr smoke tests en CI, separar el worker BullMQ del API en producción, exponer eventos vía API keys + webhooks HMAC con scopes enforced y dashboard de retry manual para integradores externos (Zapier, n8n, sistemas del cliente), anonimizar tenants completos por RGPD y cobrar por SEPA + tarjeta con auto-charge y pago self-service desde el portal del inquilino**. Detalle en [`docs/ROADMAP.md`](docs/ROADMAP.md). Resumen de lo que existe ya funcionando:

### Backend (`apps/api`)

- **Auth completo**: register, login (con detección 2FA), refresh con rotación + detección de reuso paranoid, logout (single + global), `/auth/me`.
- **Verificación de email** con tokens single-use; bloquea login con 403 `email_not_verified` hasta verificar.
- **Password recovery** (`/auth/password/forgot`, `/auth/password/reset`); reset revoca todas las sesiones.
- **Gestión de usuarios** (`/users`, `/me`): invitaciones (única vía para crear users), edición, desactivación (soft delete), transferencia de propiedad atómica, `PATCH /me`, `POST /me/change-password` (revoca otras sesiones manteniendo la actual).
- **Invitaciones** (`/invitations`): crear, listar, revocar, reenviar (revoca la anterior con `revokedReason: 'replaced_by_resend'`), aceptar público (verifica email automáticamente). Token opaco `<invitationId>.<secret>`, hash argon2id, single-use atómico, TTL 7 días.
- **2FA TOTP**: setup/verify/disable/regenerate; login con `pendingToken` corto y `/auth/2fa/challenge`. Secret cifrado AES-256-GCM con `MASTER_ENCRYPTION_KEY`. 10 recovery codes `XXXX-XXXX` hashed argon2id, single-use.
- **Autorización por permisos finos**: cadena de guards global `Throttler → JwtAuth → Permissions` (el `RolesGuard`/`@Roles` se retiró tras completar la migración RBAC v2). Toda la autorización del panel se expresa con `@RequirePermission(...)` (ver "Permisos finos" abajo).
- **Permisos finos (RBAC de grano fino)**: catálogo `Permission` (`recurso:acción`) + `ROLE_PERMISSIONS` (mapa rol→permisos en código) + helpers (`permissionsForRole`, `roleHasPermission`, `roleHasAllPermissions`) en `packages/shared/src/auth/permissions.ts`. Decorador `@RequirePermission(...)` + `PermissionsGuard` (APP_GUARD tras `JwtAuthGuard`; única capa de autorización fina tras retirar `@Roles`); 403 `insufficient_permission` con `details.requiredPermissions`. `/auth/me` devuelve `permissions` (derivados del rol). Mapa: `owner` todo, `manager` todo menos refunds/settings/billing/integraciones/users:manage/rgpd/borrados, `staff` operativa diaria (write sin manage/delete/refund), `readonly` solo `:read`. Regla más fina real: `invoices:refund` solo `owner`. **Migración RBAC v2 (en curso, fiel al catálogo):** invoices + **operativa diaria** (PR1: customers, customer-documents, contracts, reservations, contract-signatures, contract-pdf, leads, tasks, incidents) + **inventario/catálogo/comms** (PR2: facilities, facility-floors, unit-types, units, products, product-stock, product-sales, communications, message-templates, automations) migrados a `@RequirePermission`. La autorización sigue ahora `ROLE_PERMISSIONS`: `staff` gana las escrituras de operativa que el catálogo le concede (firmar/convertir contratos, KYC…; en PR2 además crear/editar units `units:write` y reintentar comunicaciones `communications:send`) y `manager` pierde `customers:delete` (owner-only). Nuevos permisos `leads:manage`/`tasks:manage`/`incidents:manage` (owner+manager) para los `DELETE`. Mapeo PR2: facilities/floors → `facilities:read|manage`; unit-types → `units:read|manage` (config estructural, owner+manager); units → `units:read`, create/update/change-status `units:write`, delete `units:manage`; products (catálogo) → `products:read|manage`; product-stock + product-sales → `products:read|write` (operativa staff); communications → `communications:read|send`; message-templates → `templates:read|manage`; automations → `automations:read|manage`. Las lecturas pasan a `:read` (readonly gana el GET de stock, antes owner+manager+staff). **PR3: billing/pagos/accesos** (payments, payment-methods, redsys, invoice-series, tenant-aeat-credentials, verifactu-aeat, holded, access-credentials, access-devices) migrados con el principio **configuración (owner) vs operación (manager/staff)**: cobros y métodos de pago → `payments:read`/`payments:charge` (**staff gana** cobrar facturas, registrar/borrar PMs y el redirect Redsys); config de integraciones (series, PUT settings de Holded/Redsys, upload/revoke de credenciales AEAT) → `billing:configure` (owner-only, **manager pierde** crear/editar series); operaciones y lecturas de billing (resend/refresh AEAT + GET de credenciales AEAT history/me, Holded test/backfill/sync) → `invoices:manage` (owner+manager, sin cambio); lecturas de settings de pasarela (GET de Holded/Redsys, antes abiertas a todos) → `settings:read`; accesos → `access:read` (GET + device ping, readonly gana ping) y `access:manage` (mutaciones de credenciales y devices, owner+manager; staff NO gestiona accesos). **PR4: admin/settings** (users, tenant-settings, invitations, tenant-roles, billing-saas, integrations, rgpd, imports, reports) cierra la migración — **ya no queda ningún `@Roles` en controllers de tenant** (el `RolesGuard` global y el decorador `@Roles`/`roles.decorator.ts` se **retiraron** como cleanup; la cadena de guards queda `Throttler → JwtAuth → Permissions`): gestión de usuarios e invitaciones → `users:read` (GET de users/invitations, antes invitations era owner+manager → ahora también staff/readonly leen) + `users:manage` (editar/desactivar/transferir/invitar/revocar/reenviar, owner-only → **manager pierde** toda la gestión de usuarios e invitaciones); settings del tenant → `settings:read` (GET) + `settings:manage` (PATCH security) + `billing:configure` (PATCH billing auto-charge); roles custom (`/settings/roles`) → `settings:manage` (owner); suscripción SaaS (`/settings/saas-billing`) → `billing:configure` (owner); API keys + webhooks → `integrations:manage` (owner-only → **manager pierde** ver/gestionar integraciones); RGPD (export/anonymize/requests) → `rgpd:manage` (owner-only → **manager pierde**); importación CSV → nuevo permiso `imports:manage` (owner+manager; templates GET siguen abiertos); reports → `reports:read` (GET) + `reports:run` (**staff gana** ejecutar informes). Endpoints `/me` (perfil/cambio de contraseña) siguen sin permiso (self-service autenticado). Frontend: `useHasPermission(permission)` + componente `<Can permission="…">` (`apps/web/src/components/auth/can.tsx`) que gatea los botones de acción de las páginas principales (customers, contracts list+detalle, invoices list+detalle, reservations, units, products, facilities, tasks, incidents, reports, access credenciales/devices) contra el permiso exacto del backend; las pestañas de `/settings` ya se ocultaban por permiso en su layout. El gating de UI es **cosmético**: el backend (`PermissionsGuard`) sigue siendo la frontera de seguridad. Tests: unit `permissions.guard` 11/11 + e2e `permissions` 18/18.
- **Roles personalizados por tenant (RBAC v1)**: tabla `tenant_roles` `(tenant_id, name, description, permissions[], base_role)` con RLS (migración `20260618100000`) + `users.tenant_role_id` (FK ON DELETE SET NULL). **Permisos efectivos** = del rol custom si el user lo tiene asignado, si no del rol enum; se resuelven en `AuthService.resolvePermissions` al emitir el access JWT (nuevo claim `permissions` en el token) y en `/auth/me`. El **`PermissionsGuard` lee `request.user.permissions` del JWT** (fallback a `roleHasAllPermissions(role)` para tokens antiguos). El `base_role` enum se mantiene para los endpoints `@Roles` (custom roles solo afectan a `@RequirePermission` + UI; cambios de rol aplican al siguiente refresh, ≤15 min). `TenantRolesModule`: CRUD `/settings/roles` (owner) + `PATCH /settings/users/:id/tenant-role`. UI: `/settings/roles` (matriz de permisos por recurso) + selector en el dialog de edición de usuario (solo owner). `UserDetailDto.tenantRoleId` nuevo. Tests: e2e `tenant-roles` 3/3.
- **Throttle**: 60/min default, 5/min login y 2FA challenge/disable, 3/h register y forgot-password, 30/min refresh.
- **Audit logs**: cada acción crítica queda en `audit_logs` (auth.register/login/refresh/logout, user.invited/updated/deactivated/ownership_transferred/password_changed, auth.2fa.enabled/disabled/challenge.success/failed/recovery_codes_regenerated/recovery_code_used).
- **Multi-tenancy**: 2 conexiones Postgres — `storageos_app` (RLS, usada por todo el día a día con `PrismaService.withTenant`) y `storageos` (admin, bypass RLS, solo para flujos sin tenant context: register, login lookup por slug, audit logs, invitaciones).
- **Email** vía nodemailer + plantillas React Email (verificación, password reset, invitación). En dev apunta a Mailpit.
- **Facilities + units + plano** (Fase 2): `facilities` con soft delete, `unit_types` por tenant, `units` con columnas generadas (`area_m2`, `volume_m3`), `unit_status_history` para trazabilidad. Endpoints `/facilities`, `/unit-types`, `/units` (con cursor pagination + filtros), `/units/:id/change-status` con transiciones validadas, `/units/:id/history`, `/floors/:id/plan-upload-url` (signed URL PUT a MinIO), `/floors/:id/units-layout` (coords del editor en `$transaction`), `/dashboard/occupancy` (agregación por facility + tipo).
- **MinIO** activo: `@aws-sdk/client-s3` + `getSignedUrl` PUT directo desde navegador (path `<tenantId>/<facilityId>/floors/<floorId>-<uuid>.<ext>`).
- **Customers + contratos + reservas** (Fase 3): `customers` con soft delete + búsqueda + KYC, `customer_documents` con upload directo MinIO (DNI/CIF/comprobante domicilio), `contracts` con state machine (draft→active→ending→ended/cancelled), `contract_events` para timeline inmutable, `reservations` con **EXCLUDE USING gist** sobre `tstzrange` (extensión `btree_gist`) que impide overbooking a nivel BD. Endpoints `/customers`, `/contracts`, `/reservations`, `/contracts/:id/{sign,request-end,end,cancel,change-price,generate-pdf,notes}`, `/reservations/:id/{confirm,cancel,convert-to-contract}`.
- **Pricing snapshot**: `contracts.priceMonthly` se congela al firmar; cambios via endpoint dedicado con motivo (registrado como `contract_events.price_changed`). Sincronización automática `units.status` desde el flujo de contratos (occupied/available).
- **PDF de contratos**: Puppeteer headless **síncrono** dentro del request. Dynamic import (`await import('puppeteer')`) por ser ESM-only, evitando que Jest CJS rompa. PDF a MinIO bucket `uploads` con `<tenantId>/contracts/<contractId>-<uuid>.pdf`. Mover a BullMQ en Fase 4.
- **Facturación + pagos** (Fase 4): `invoice_series` (numeración secuencial atómica por serie), `invoices` con Verifactu (hash SHA-256 encadenado entre facturas de la misma serie, QR AEAT como data URL, campos `aeat_*`) en state machine `draft → issued → paid/overdue/cancelled/refunded/partially_refunded`. `invoice_items` con snapshot del precio resuelto. PDFs con Puppeteer + QR embebido a MinIO bucket `invoices`. **Verifactu opera en `AEAT_MODE=stub`** en Fase 4: hashes + QR reales pero el envío AEAT es simulado; en Fase 8 cambia a `sandbox`/`production` sin tocar código.
- **Stripe gateway**: `PaymentGateway` interface + `StripeGateway` (Customer + SetupIntent + PaymentIntent + Refund + verifyWebhook con HMAC). Endpoints `POST /payments/invoices/:id/charge`, `POST /payment-methods/setup-intent`, webhook público `POST /webhooks/stripe` con raw body. Tokens de Stripe cifrados con `CryptoService` (AES-GCM, ADR-007).
- **Webhooks Stripe robustos** (2026-06-11): dedup global de eventos entrantes en tabla `processed_stripe_events` (sin tenant_id, sin RLS, PK = `event.id`) — `StripeEventsService.markProcessed` inserta ANTES de procesar (duplicado → P2002 → descarte) y `release` borra si el handler falla para no comerse el retry de Stripe; cleanup cron diario 04:00 a 30 días (gated `WORKERS_ENABLED_IN_API`). `PaymentsService.syncFromWebhook` idempotente: descarta eventos con status repetido o sobre payments en estado terminal (`succeeded/refunded/partially_refunded`) — antes CADA cobro con éxito doble-contaba `amountPaid` (suma síncrona en `chargeInvoice` + suma del webhook). `charge.refunded` ya no es log-only: `syncRefundFromWebhook` sincroniza por delta contra el `amount_refunded` acumulado de Stripe (idempotente por construcción), actualiza payment (`refunded/partially_refunded`) y propaga a la invoice capando en `total`; resuelve tenant por metadata o fallback lookup admin por `gatewayPaymentId`. Unit specs `payments.service.spec.ts` 9/9 + `stripe-events.service.spec.ts` 4/4.
- **SEPA Direct Debit vía Stripe** (2026-06-11, v1 panel staff): cobro por domiciliación con el `StripeGateway` existente (payment method `sepa_debit`), sin GoCardless. `ChargeParams.paymentMethodType` nuevo — el PaymentIntent declara `payment_method_types: [tipo]` (el default de Stripe es `['card']` y un cobro SEPA sin esto falla). `chargeInvoice` rechaza PMs no cobrables (`cash/bank_transfer/other` → 400 `payment_method_not_chargeable`). El registro de PMs deriva el `type` real del gateway (`PaymentMethodDetails.type`), no confía en el input. Los cobros SEPA quedan `processing` (liquidación 2-5 días hábiles); el webhook resuelve a `succeeded/failed`. Devoluciones post-liquidación (R-transactions): handler `charge.dispute.created` → `syncDisputeFromWebhook` revierte el payment `succeeded` a `failed` (`failureReason: disputed: <reason>`), resta `amountPaid` y devuelve la invoice `paid` a `overdue/issued` (idempotente: solo actúa sobre `succeeded`). UI: pestaña **Pagos** en `/customers/[id]` (`payment-methods-tab.tsx`) con lista de PMs + dialog "Añadir método de pago" con Stripe `<PaymentElement>` (`@stripe/react-stripe-js`, locale es, renderiza tarjeta + IBAN con texto de mandato); flujo SetupIntent → `confirmSetup` → `POST /payment-methods`. Botón "Cobrar (auto)" en `/invoices/[id]` distingue `processing` (toast SEPA) de `succeeded/failed`. Verificación manual con IBANs de test en `DEPLOYMENT.md` §12B. Unit specs `payments.service.spec.ts` (chargeInvoice SEPA + disputes).
- **Self-service de IBAN + pago en el portal** (2026-06-11): el inquilino registra su propio método de pago y paga facturas desde el portal (mandato SEPA aceptado online por el propio pagador). Endpoints `@Public` con auth manual por JWT de portal (helper `requirePortalSession` en `PortalController`): `GET /portal/me/payment-methods`, `POST /portal/me/payment-methods/setup-intent`, `POST /portal/me/payment-methods` (schema `PortalRegisterPaymentMethodSchema` sin `customerId` ni `type` — salen del token y del gateway; el PM pasa SIEMPRE a default), `POST /portal/me/invoices/:id/charge` (verifica propiedad → 404; cobra el pendiente con el PM default vía `chargeInvoice` con `userId: null` — firma relajada a `string | null`, `audit_logs.user_id` es nullable). Todos los POST con throttle 5/min. `PortalModule` importa `PaymentsModule`. UI: `consume/page.tsx` con card "Método de pago" + dialog con `<StripeSetupForm>` (componente compartido extraído a `apps/web/src/components/billing/stripe-setup-form.tsx`, reutilizado por la pestaña staff) + botón "Pagar" real (processing → toast domiciliación; 400 `no_payment_method` → abre el dialog de alta). E2e `portal-payments.e2e-spec.ts` 4/4 (401 sin token, [] sin PMs, 404 cross-customer, 400 `no_payment_method`).
- **Auto-charge al emitir factura** (2026-06-11, opt-in por tenant): flag `tenants.auto_charge_on_issue` (default false, migración `20260611100000`) con `GET/PATCH /settings/tenant/billing` (PATCH role `owner`, audit `tenant.billing.auto_charge_changed`) y card "Cobro automático" en `/settings/billing`. `InvoicesService.issue()` ahora **emite `domain.invoice_issued`** (estaba declarado con listeners — automations + webhooks salientes `invoice.issued` — pero NUNCA se emitía; este fix activa ambos). `AutoChargeService` (`@OnEvent`, registrado SIEMPRE en el API) encola `{tenantId, invoiceId}` en la cola BullMQ `payments` (antes huérfana) si el flag está activo; `AutoChargeProcessor` (gated `WORKERS_ENABLED_IN_API`, corre en el worker en prod) re-chequea flag + invoice `issued/overdue` + customer + pendiente > 0 + PM default cobrable y llama `chargeInvoice` con `userId: null` — cualquier condición no cobrable es skip con log (`[auto-charge] skip <reason>`), y un cobro RECHAZADO por el gateway no lanza (sin retry BullMQ; entra el dunning). Unit spec `auto-charge.service.spec.ts` 9/9 + e2e `auto-charge.e2e-spec.ts` 2/2 (settings + skip limpio sin PM).
- **Sentry + readiness** (2026-06-11): `@sentry/nestjs` en `apps/api` y `apps/worker` via `instrument.ts` (primera línea de cada `main.ts`; sin `SENTRY_DSN` es no-op total). El API captura 5xx desde `HttpExceptionFilter`; el worker captura unhandled rejections. Env nuevas: `SENTRY_DSN`, `SENTRY_TRACES_SAMPLE_RATE` (default 0). Nuevo `GET /health/ready` (VERSION_NEUTRAL, exento del legacy redirect via `/health/...`): `SELECT 1` a Postgres + `PING` a Redis (reusa la conexión BullMQ de la cola `billing`); 503 con `details: { database, redis }` si algo cae. Uptime Kuma debe apuntar a `/health/ready`, no a `/health` (solo liveness).
- **BullMQ + Redis** activo en el mismo proceso NestJS (separar a `apps/worker` en Fase 8). Colas: `billing`, `dunning`, `payments`, `verifactu`, `email`. Cron diario `0 2 * * *` para `billing.generate-recurring` (crea drafts mensuales por contrato activo) + `0 6 * * *` para `dunning.daily` (marca overdue + programa acciones).
- **Dunning** calendario: +1 email_reminder, +7 email_reminder con recargo, +14 access_block (flag para Fase 5), +30 legal_notice manual. Cada acción se persiste en `dunning_actions` con `scheduled_for` / `executed_at`. `DunningService.executeAction('email_reminder')` encola el recordatorio en el outbox de comunicaciones vía `CommunicationsService.enqueue` (plantilla `invoice_overdue_email`, trigger `invoice_overdue` con su whitelist de variables, `source='dunning.email_reminder'`); si la factura no tiene customer/email (p. ej. F2) se loguea y se marca `executed` con `emailEnqueued:false` sin romper la acción. `dailyTick` emite **`domain.invoice_overdue`** por cada factura recién marcada (2026-06-11 — estaba declarado con listeners pero sin emisor, como `invoice_issued`: los webhooks salientes `invoice.overdue` no disparaban hasta ahora). Unit spec `dunning.service.spec.ts` 5/5.
- **RGPD**: `data_subject_requests` con SLA 30 días + `consents`. Endpoint `POST /rgpd/customers/:id/anonymize` que sustituye datos personales por `*** ANONIMIZADO ***` y borra docs/payment_methods, **preservando invoices** por obligación fiscal (Verifactu + Ley 58/2003). Anonimización a nivel **tenant** (derecho al olvido al darse de baja): `AdminTenantsService.anonymize` + endpoint super admin `POST /admin/tenants/:id/anonymize` (`AdminGuard`, requiere `reason`) — en una `$transaction` anonimiza todos los customers, borra sus docs/payment_methods, anonimiza el staff (`users`: email único irreversible por el `@@unique([tenantId,email])`, 2FA off, `passwordHash` = hash de secreto aleatorio, `isActive=false`), revoca sesiones y marca el tenant `cancelled`+`deletedAt`+borra `billingEmail`/`taxId`. Preserva invoices + audit_logs. Deja rastro en `audit_logs` + `super_admin_audit_logs`. Unit spec `admin-tenants.service.spec.ts` 2/2. UI: botón "Anonimizar (RGPD)" en `/admin/tenants/[id]` con dialog de confirmación fuerte (hay que teclear el `slug` del tenant para habilitar el botón) + motivo obligatorio; al completarse redirige a `/admin/tenants`.
- **Customer portal mínimo**: magic link en **Redis** (clave `portal:magiclink:<tokenId>` con TTL 30 min y single-use vía `GETDEL` atómico; reutiliza la conexión ioredis de BullMQ vía `queue.client` — sobrevive a deploys y funciona con réplicas; antes era un Map in-memory), endpoints públicos `POST /portal/login/{request,consume}` + `GET /portal/me/invoices` con JWT corto firmado con `JWT_2FA_PENDING_SECRET` y `purpose: 'portal'`.
- **Email provider abstracto** (Fase 5): `EmailProvider` clase abstracta + DI por `Symbol('EmailProvider')`. Selección por env `EMAIL_PROVIDER`: `smtp` (nodemailer/Mailpit en dev) o `resend` (HTTP API). `EmailService` mantiene la API previa (`send({ template })`) y añade `sendRendered({ html, text })` para plantillas custom.
- **Comunicaciones + outbox** (Fase 5): `communications` persiste cada envío ANTES de encolarse en la cola BullMQ `communications`. Estados `pending → processing → sent/failed/bounced/skipped`. Retry manual via `POST /communications/:id/retry`. WhatsApp via `WhatsAppProvider` abstracto + stub Fase 5 (envío real en Fase 8).
- **Plantillas + templating** (Fase 5): `message_templates` con `kind=system|transactional|marketing`. Sistema readonly. Handlebars con whitelist de variables por trigger (`TEMPLATE_VARIABLES_BY_TRIGGER`) para evitar exfiltrar datos. Built-ins seedeables (welcome, contract_signed, invoice_overdue, ...).
- **Automations event-driven** (Fase 5): `EventEmitterModule` global. Services emiten `domain.contract_signed`, `domain.customer_created`, etc. `AutomationsService` (con `@OnEvent` listeners) matchea reglas activas y encola jobs en la cola `automations` que crean la communication.
- **Leads + state machine** (Fase 5): pipeline kanban `new → contacted → qualified → won|lost`. Conversion atomic crea customer + (opcional) reservation en `$transaction`. Soft delete (`deletedAt`).
- **Widget público** (Fase 5): `/public/widget/:slug/{facilities,leads}` sin auth, throttle 5/min en POST, honeypot anti-bot. Resolución de tenant por slug vía `PrismaAdminService`.
- **Landing pública SEO** (`/s/[slug]`): página SSR indexable (server component `(public)/s/[slug]/page.tsx` con `generateMetadata` — title/description/OG/canonical — + JSON-LD `SelfStorage` por local) que lista la empresa + cada local (dirección/contacto) con tipos + precio "desde" + plazas disponibles y CTA "Reservar ahora" → `/book/[slug]`. Datos vía `GET /public/landing/:slug` (`LandingModule`: `LandingService` resuelve por slug con `PrismaAdminService` + `groupBy` de units `available`; controller VERSION_NEUTRAL + @Public + throttle 60/min). `/public/landing/` añadido a las exenciones del legacy-redirect (prod) y del rewrite del test (como `/public/widget/`). **Páginas SEO por local**: `Facility.publicSlug` único por tenant (autogenerado del nombre con `slugify` + dedupe `-N`, migración `20260618170000` + backfill SQL; editable enviando `publicSlug` en create/update); página `/s/[slug]/[facility]` (SSR + metadata + JSON-LD + breadcrumb) vía `GET /public/landing/:slug/:facilitySlug`; la landing del tenant enlaza a cada local. **Sitemap/robots**: `GET /public/landing/sitemap` (tenants activos + slugs de locales) → `apps/web/src/app/sitemap.ts` + `robots.ts` (allow `/`,`/s/`; disallow panel/portal/api/widget/book; usan `NEXT_PUBLIC_SITE_URL`, nuevo build-arg en el Dockerfile web + composes). Nota: el sitemap expone los slugs públicos de todos los tenants del dominio compartido (las landings ya son públicas). e2e `landing` 4/4.
- **Tasks + Incidents** (Fase 6): `OperationsModule` con `TasksService` (state machine `open → in_progress → done|cancelled`, comments en tabla separada) y `IncidentsService` (state machine `reported → investigating → resolved|dismissed`, severity, vínculos a unit/customer/contract). `IncidentsService` emite `domain.incident_created` cuando severity≥high para automations.
- **Products + ventas** (Fase 6): catálogo por tenant (`ProductsService`), stock por facility (`ProductStockService` con decrement atómico via `updateMany WHERE quantity ≥ n`), `ProductSalesService` que crea invoice Verifactu inline cuando hay customer.
- **Analytics** (Fase 6): `AnalyticsService` con 4 KPIs (occupancy física + económica, churn cohort mensual, aging buckets, leads funnel con conversion ratios + bySource).
- **Ingresos por mes / histórico (2026-06-23):** el Resumen (dashboard) solo mostraba el **mes actual** (cobrado/facturado del mes en curso + estado actual). `AnalyticsService.getMonthlyRevenue(tenantId, months=12)` (clamp 1-24) + `GET /analytics/monthly-revenue?months=` devuelve una serie por mes con **`invoiced`** (facturas emitidas en el mes por `issueDate`, estados ≠ draft/cancelled) y **`collected`** (pagos `succeeded` por `paidAt`, del **`payment` table** → captura parciales; nota: los mark-paid de F2 sin customer no crean payment → no se reflejan en cobrado). Bucketing en JS (2 queries: invoices + payments con `gte` fromDate). Shared `MonthlyRevenuePointDto`/`MonthlyRevenueKpiDto` en `packages/shared/src/reports/types.ts`. Frontend: **nueva pestaña "Ingresos"** (primera) en `/analytics` con BarChart facturado (gris) vs cobrado (azul) de los últimos 12 meses + 2 KPIs (totales) + tabla mensual. Hook `useMonthlyRevenue`. Igual que el resto de `/analytics`, el endpoint **no lleva `@RequirePermission`** (el sidebar gatea por `analytics:read`; gatear el controller entero queda como follow-up). e2e `analytics-monthly-revenue` 1/1.
- **Tracking de campañas (UTM) en leads (2026-06-23):** captura el origen de captación. Migración `20260623120000`: `leads.utm_source/utm_medium/utm_campaign` (TEXT nullable). Shared: `utmFields` reutilizable añadido a `WidgetLeadSchema` + `CreateLeadSchema`; `LeadDto` gana los 3 campos + nuevos `LeadsUtmRowDto`/`LeadsUtmKpiDto`. **Captura**: el **widget público** (`/widget/[slug]`) lee `utm_source/medium/campaign` de `window.location.search` al cargar y los manda en el POST → `LeadsService.createFromWidget`/`create` (staff) los persisten. **Reporte**: `AnalyticsService.getLeadsUtm(from?,to?)` agrupa los leads con UTM por (origen, campaña) con total + `won` (2 `groupBy`) → tasa de conversión; `GET /analytics/leads-utm?from=&to=`. UI: card **"Campañas (UTM)"** en la pestaña Leads de `/analytics` (tabla origen/campaña → leads/ganados/conversión); leads sin UTM no aparecen. e2e `leads-utm` 2/2 (captura por widget + agregación; lead sin UTM excluido).
- **Reserva online con pago obligatorio + fianza (2026-06-23):** completa el booking self-service (`/book/[slug]` → firma `/sign/[token]`), que ya cobraba la 1ª factura **solo por Redsys**. (1) **Fianza por tipo**: `unit_types.default_deposit_amount` (migración `20260623140000`, default 0); el booking la copia a `contract.depositAmount` y `SignaturesService.maybeIssueFirstInvoice` añade una **línea "Fianza" con IVA 0** (indemnizatoria, no servicio) a la 1ª factura, junto al alquiler (IVA 21). Config en el form de tipo de trastero (`/facilities/[id]` pestaña Tipos). (2) **Pago con tarjeta (Stripe)** en la página de firma además de Redsys: componente `BookingPayment` reusa el flujo del portal (setup-intent → registrar PM → `charge`) con el **portalToken** que devuelve la firma; maneja `succeeded`/`processing` (SEPA)/`failed`. (3) **Obligatorio**: `ContractsService.sign` acepta `deferAccessUntilPaid` → lo añade al `scope.deferAccess` del evento `contract_signed`; `signViaToken` (todo sign remoto self-service) lo pasa `true`, y `AccessIntegrationsService.onContractSigned` **salta la emisión del PIN** si `deferAccess` → el acceso se emite al **pagar la 1ª factura** (reusa la auto-emisión del listener `invoice_paid`). UX de la firma: "completa el pago para activar tu acceso". e2e `booking-deposit` 1/1 (línea de fianza IVA 0 + alquiler IVA 21). **Gotcha**: `maybeIssueFirstInvoice` necesita una serie por defecto (`ensureDefaultSeries` en el test) o no emite factura.
- **Permisos por local / facility scope (2026-06-23):** un usuario puede restringirse a ciertos locales y solo ve/gestiona las entidades ancladas a esos locales. Migración `20260623160000`: tabla `user_facilities` (RLS, único `(user_id, facility_id)`). **Sin filas para un usuario = sin restricción** (ve todos los locales — típico de owner/manager); con filas = restringido. **Resolución**: `AuthService.resolveFacilityScope(tenantId, userId)` → array o `null`; va en el **JWT** (`facilityScope` en el access token, junto a `permissions`) + `AuthenticatedUser.facilityScope` + `MeResponse.facilityScope`; aplica al siguiente login/refresh (≤15 min, como los permisos). **Asignación**: `PATCH /settings/users/:userId/facilities {facilityIds}` (`settings:manage`, owner) → `TenantRolesService.setUserFacilities` (reescribe la lista); `UserDetailDto.facilityIds` (el list `GET /users` pasó de `UserSummaryDto[]` a `UserDetailDto[]` con `include facilities`, y se retiró `toSummary`/`UserSummaryDto`). **Enforcement** (helper `apps/api/src/common/facility-scope.ts`: `assertFacilityAllowed`, `resolveFacilityFilter`): **lists filtradas por scope** en facilities, units, contracts, reservations, occupancy (`analytics/occupancy`) y access-devices; **guards de escritura** (crear unit/contract/device en local fuera de scope → 403 `facility_not_in_scope`); **guards `:id`** en las mutaciones de facilities (update/delete/images). El booking público llama `contracts.create` sin scope (no restringido). **Decisión v1**: inquilinos/facturas/leads quedan a nivel tenant (no anclados a un local); los guards `:id` de get/mutación en units/contracts/reservations/devices son follow-up (las lists ya ocultan los datos de otros locales). Frontend: multiselect "Locales asignados" en el diálogo de editar usuario (`/settings/users`, solo owner; vacío = ve todos); el backend filtra `/facilities` → el resto de selectores/switchers de local solo muestran los permitidos sin tocar nada más. e2e `facility-scope` 1/1 (staff asignado a A solo ve A + sus units + 403/404 en B; owner ve los dos). **Gotcha**: `exactOptionalPropertyTypes` → pasar `user.facilityScope ?? null` en las call-sites (no `undefined`).
- **Toque de queda de acceso por local + acceso 24h (curfew) (2026-06-23):** franja horaria en la que el local bloquea el acceso. Migración `20260623180000`: `facilities.access_curfew_enabled/start/end` (`HH:MM`) + `access_credentials.bypass_curfew`. El `allowedHours` por credencial seguía siendo un stub (solo `always !== false`); esto añade el **toque de queda real**. **Enforcement** en `AccessVerifyService.verify`: carga el `facility` del device (`device.facilityId`) y llama `checkCurfew(facility, credential)` (función pura) junto al `evaluateCredential`; calcula la hora **en la zona horaria del local** (`Intl.DateTimeFormat` con `timeZone`) → minutos desde medianoche, ventana `[start, end)` que **soporta cruzar medianoche** (`start>end`); si está dentro y la credencial **no** tiene `bypassCurfew` → `denied_outside_hours`. Las credenciales con `bypassCurfew` (staff, "acceso 24h") lo saltan. Config en la pestaña **Ajustes** del local (`/facilities/[id]`: toggle + horas) + checkbox "Acceso 24h" en el alta de credencial (`/access/credentials`). `FacilityDto`/`Create/UpdateFacilitySchema` ganan los 3 campos curfew; `AccessCredentialDto`/`Create/UpdateCredentialSchema` ganan `bypassCurfew`. e2e `access-curfew` 1/1 (ventana cubriendo "ahora" en Europe/Madrid → normal denegada `denied_outside_hours`, bypass permitida, curfew off → permitida). **Pendiente** (follow-up): ventanas horarias reales por credencial.
- **Pase nocturno (single-use de pago) (2026-06-23):** el inquilino compra desde su portal un código de **un solo uso** que **salta el toque de queda** y caduca a la mañana siguiente (08:00 Europe/Madrid). Migración `20260623200000`: `access_credentials.max_uses`/`uses_count` (single-use genérico; `max_uses` NULL = ilimitado) + `tenants.night_pass_enabled`/`night_pass_price`. **Single-use en el verify**: `evaluateCredential` deniega si `usesCount >= maxUses` (`denied_inactive_credential`, "pase ya utilizado"); al abrir con éxito incrementa `usesCount` y marca `expired` al alcanzar el límite. **Emisión**: `AccessCredentialsService.createNightPassForCustomer` (PIN, `maxUses=1`, `bypassCurfew=true`, `expiresAt=nextMorningIso()` 08:00 Madrid, `metadata.source='night_pass'`). **Orquestación**: `NightPassService` (en `PortalModule`, que ahora importa `BillingModule`) — `buy()` valida `nightPassEnabled` (409 `night_pass_disabled`), emite el código + **factura best-effort** una F1 "Pase nocturno" (precio + IVA 21, serie por defecto). Endpoints portal `GET/POST /portal/me/access/night-pass` (`@Public` + sesión de portal; POST con throttle). Config en `GET/PATCH /settings/tenant/access` (extendido con `nightPassEnabled`/`nightPassPrice`). `AccessCredentialDto`/`CreateCredentialSchema` ganan `maxUses`/`usesCount`. Frontend: card "Pase nocturno (de pago)" en `/access/credentials` (toggle + precio) + botón "Comprar pase nocturno" en el portal (`/portal/consume`). e2e `night-pass` 1/1 (compra → PIN single-use + factura "Pase nocturno" → 1er uso entra, 2º denegado).
- **Hardening de autorización en backend (2026-06-23, follow-ups):** el backend ahora **hace cumplir** lo que el frontend ya gateaba. **(1) Gating por plan (`@RequireFeature`)** — decorador `@RequireFeature(feature)` (`apps/api/src/common/decorators/require-feature.decorator.ts`) + `FeatureGuard` (`common/guards/feature.guard.ts`, APP_GUARD tras `PermissionsGuard`): consulta el plan del tenant **en vivo** (`tenantSubscription` → `featuresForPlan(plan.slug)`) y lanza **403 `feature_not_in_plan`** con `details.requiredFeature` si no lo incluye; sin `@RequireFeature` o sin `request.user` (rutas públicas/device key) deja pasar. Aplicado a los **7 módulos premium**: ai (`ai_assistant`), sepa (`sepa`), bank-reconciliation (`bank_reconciliation`), rent-increases (`rent_increases`), insurance (`insurance`), automations (`automations`) y los 3 controllers de access credentials/devices/logs (`access_control`). Antes el gating por plan era **solo frontend** (`<FeatureGate>`), así que un usuario podía consumir features premium por API; ahora es infranqueable. **(2) Permiso en analytics** — `@RequirePermission('analytics:read')` a nivel de `AnalyticsController` (antes no exigía permiso; lo tienen los 4 roles base, así que no quita acceso a nadie, solo lo hace exigible a roles custom). **Tests**: helper `setTenantPlan(slug, plan)` en `tenant-fixtures` para subir a `pro` las suites de features fuera de `starter` (ai/sepa/bank — `starter` solo trae rent_increases/insurance/access_control/automations); e2e `feature-gate` 1/1 (starter → 403 en `/ai`, `/automations` 200, pro → 200). **Gotcha**: las suites e2e de IA/SEPA/conciliación ahora **deben** llamar `setTenantPlan(owner.slug, 'pro')` tras registrar, o el `FeatureGuard` las corta (el registro crea tenants en `starter`).
- **Guards `:id` del facility scope (2026-06-23, follow-up de [[#56]]):** cierra el residual del facility scope — las listas ya se filtraban por local, pero un usuario con scope podía **leer/mutar un recurso de un local fuera de su scope si conocía el id**. Ahora `assertFacilityAllowed` está en el **`findOrThrow`** de units, reservations, access-devices y contracts (verifica el `facilityId` del recurso contra el scope); el `facilityScope` se enhebra desde los controllers a **detail + todas las mutaciones** de los 4: units (detail/history/update/changeStatus/delete), reservations (detail/confirm/cancel/convert), access-devices (detail/update/regenerate/remove/ping/remoteOpen), contracts (detail/events + 9 mutaciones). Los flujos **sin scope** (portal `requestEndByCustomer`, internos) pasan `undefined` → sin assert (compatibles). 403 `facility_not_in_scope`. e2e `facility-scope` extendido (staff con scope A → 403 leyendo/mutando un trastero de B; A sí). Inquilinos/facturas/leads siguen a nivel tenant (decisión v1 de [[#56]]); el resto de entidades ancladas a local quedan **totalmente scopeadas**.
- **Fotos de inspección (check-in + check-out) del trastero (2026-06-23):** el staff captura el estado del trastero **a la entrada (check-in)** y **a la salida (check-out)** como **evidencia para fianzas y disputas**, vinculada al contrato. Nació como solo check-out (`contract_checkout_photos`, migración `20260623210000`) y se generalizó con un campo `kind` (migración `20260623220000`: renombra la tabla a **`contract_inspection_photos`** + columna `kind` default `checkout`; renombra el índice; la policy RLS y los constraints viajan con la tabla). `FilesService` gana **`getPresignedGetUrl(bucket, key, ttl)`** (GET firmado para servir objetos de un bucket **privado** — antes solo había PUT firmado + `buildPublicUrl`, que no sirve para `uploads`) + `buildInspectionPhotoKey(tenant, contract, kind, mime)` (`<tenant>/contracts/<id>/<kind>/<uuid>.<ext>`). `InspectionPhotosService` + `InspectionPhotosController` en `ContractsModule`, ruta **`/contracts/:id/inspection-photos`** con `kind` en query (GET, filtra) y body (upload-url/register): `POST .../upload-url` (presigned PUT, sube directo a MinIO sin pasar por la API), `POST .../inspection-photos` (registra la key; **valida que empieza por `<tenant>/contracts/<id>/<kind>/`** → 400 `invalid_photo_key`), `GET .../inspection-photos?kind=` (lista con **URL firmada GET** de 5 min; sin `kind` devuelve todas), `DELETE .../inspection-photos/:photoId`. Permisos `contracts:read` (lista) / `contracts:write` (subir/borrar) + **scope por local** (`assertContract` carga el contrato con `unit.facilityId` y aplica `assertFacilityAllowed`). Las fotos van al bucket **privado `uploads`** (evidencia, no público). Frontend: dos cards `InspectionPhotosCard` (kind `checkin` + `checkout`) en la ficha del contrato (`/contracts/[id]`, entre seguro y timeline) con subida múltiple + grid de miniaturas + borrar (gated `contracts:write`). Shared: `InspectionKindEnum` + `RequestInspectionPhotoUploadSchema`/`RegisterInspectionPhotoSchema` + `InspectionPhotoDto`/`InspectionPhotoUploadDto` en `customers` (donde viven los tipos de contrato). e2e `inspection-photos` 1/1 (sube por kind → lista filtrando por kind → sin filtro ambas → key ajena 400 → borra). **Gotcha**: el presigned GET no comprueba que el objeto exista, así que el e2e valida el flujo completo sin subir bytes reales a MinIO.
- **Dashboard rediseñado + ajuste de sidebar (2026-06-24):** lavado de cara de la página de **Resumen** (`/dashboard`) para ver el negocio de un vistazo, **frontend-only** reutilizando los hooks de `/analytics` ya existentes (sin tocar backend). `dashboard-content.tsx` pasa a orquestar: header compacto con chips de plan/trial (antes 3 stat cards grandes) + **6 KPIs hero** (`kpi-tiles.tsx`: MRR, ocupación física, inquilinos activos, pendiente de cobro, nuevos este mes, RevPAU; de `useRevenueKpis`/`useOccupancy`/`useCustomerStats`/`useAging`) + gráficos Recharts en cards nuevas: `revenue-trend-card` (ingresos 12m facturado vs cobrado, `useMonthlyRevenue`), `occupancy-card` (donut, reutilizada), `forecast-card` (previsión MRR 6m área, `useRevenueForecast`), `aging-card` (morosidad por tramo, `useAging`), `churn-risk-card` (resumen alto/medio/bajo + top 5 contratos, `useChurnRisk`), `leads-card` (embudo + conversión, `useLeadsFunnel`), `facility-occupancy-card` (barras de progreso por local, `useOccupancy.perFacility`). **Gating**: todo el bloque de métricas se muestra con `useHasPermission('analytics:read')` (los 4 roles base lo tienen; rol custom sin él ve solo la ocupación + aviso). Se **retiran** las 3 cards antiguas (`billing-metrics-card`, `customers-kpi-card`, `revenue-kpi-card`), ahora integradas en los KPIs hero. **Gotcha Recharts**: el `formatter` del `<Tooltip>` debe devolver un solo string (no `[value, label]` → error de tipos); la etiqueta de cada serie va en su `name`. **Sidebar**: se retira el grupo `Principal` (solo tenía Resumen, ya que la IA está gateada por plan) — **Resumen** pasa a ser un **item suelto sin cabecera, primero del todo** (`PRIMARY_ITEM` renderizado en su propio `SidebarGroup` sin label), y **Asistente IA se mueve al grupo Análisis** (`app-sidebar.tsx`; el render de item se extrajo a un helper `renderItem`; clave i18n `sidebar.groups.principal` eliminada). También se **retira el grupo `Accesos`** (tenía un solo item) y **Accesos se mueve al grupo Operativa** (clave i18n `sidebar.groups.access` eliminada; el label `sidebar.access` del item se mantiene). Verificado: typecheck 8/8 + lint 8/8 + build web 73/73 + db 15/15. Sin migración ni cambios de API.
- **Filtro de fechas en Ingresos (rango por meses + atajos) (2026-06-24):** la pestaña "Ingresos" de `/analytics` (y `monthly-revenue`) solo mostraba los **últimos 12 meses fijos**; ahora permite acotar el periodo. **Backend** (`AnalyticsService.getMonthlyRevenue`): la firma pasa de `(tenantId, months=12)` a `(tenantId, { months?, from?, to? })`; nuevo helper `resolveMonthList` genera la serie mensual desde un rango `from`/`to` (`YYYY-MM`) o, si no hay rango válido, los últimos `months` (clamp a `MONTH_RANGE_MAX=36`; normaliza from/to invertidos y no proyecta al futuro). Las queries añaden `lt: toExclusive` (mes siguiente al último) además del `gte`. `GET /analytics/monthly-revenue?from=&to=&months=` (`from`/`to` priman sobre `months`). **Frontend**: `MonthlyRevenuePanel` gana una fila de **atajos** (Últimos 12 meses [default] · Últimos 6 · Este año · Año pasado · Este trimestre) + dos `<input type="month">` (Desde/Hasta) para rango libre; los KPIs y títulos muestran el periodo activo. `useMonthlyRevenue` acepta `number | { months?, from?, to? }` (compat con las llamadas `useMonthlyRevenue(12)` del dashboard). **Gotcha**: ya había un `parseYearMonth` en el service (devuelve `{year,month}`) → el nuevo helper se llama `parseYearMonthIdx` (índice absoluto de mes). e2e `analytics-monthly-revenue` extendido (rango de 3 meses inclusive + from/to invertidos normalizados). Sin migración. **Fix (2026-06-25):** los dos `<input type="month">` (Desde/Hasta) **no abren el selector en varios navegadores** (Safari y otros) → no se podía elegir fecha. Sustituidos por un componente `MonthYearSelect` (dos `Select` del proyecto: mes 1-12 con nombre en español + últimos 6 años; emite `YYYY-MM`, placeholder cuando no hay valor) → fiable en todos los navegadores; el backend no cambia. **Regla**: evitar `<input type="month">`/`type="date"` para inputs críticos por su soporte irregular; preferir `Select` o un date-picker propio.
- **Mantenimiento recurrente (2026-06-24):** plantillas que **generan tareas automáticamente** según una frecuencia (revisiones, limpiezas, rondas). Migración `20260624120000`: tabla `maintenance_plans` (RLS) + `tasks.maintenance_plan_id` (FK SET NULL, trazabilidad del origen). El enum de tipo de tarea en Postgres es **`task_type`/`task_priority`** (snake_case, no `"TaskType"`) — gotcha que tumbó la migración (P3009 `type "TaskType" does not exist`); se resolvió con `migrate resolve --rolled-back` + corregir el SQL. **Frecuencia** (`packages/shared/src/maintenance`): `freq` = daily (cada N días, `interval`) · weekly (días concretos, `weekdays` 0=dom..6=sáb) · monthly (cada N meses, `dayOfMonth` 1-28). Lógica pura `firstOccurrence`/`nextOccurrence`/`scheduleLabel` (unit `recurrence.spec` 5/5; **gotcha**: `firstOccurrence` monthly cuando el día ya pasó va al **mes siguiente**, no salta el intervalo completo). **Service** `MaintenanceService` (CRUD con `withTenant` + `generateDue` cron cross-tenant vía `PrismaAdminService` → `generateFromPlan(tenantId, planId)` con `withTenant`): crea la tarea (reusa la tabla `tasks`, `dueDate`=fecha que tocaba, `maintenancePlanId`=origen) y **avanza `nextRunDate` hasta el futuro** (catch-up sin acumular tareas atrasadas; idempotente). **Cron** `MaintenanceCron` (`@Cron('0 6 * * *')`, gated `WORKERS_ENABLED_IN_API`, el worker importa `MaintenanceModule`). Endpoints `/maintenance-plans` (GET `tasks:read`; create/update/`:id/run`/delete `tasks:manage`; `run` genera ya sin esperar al cron). `TaskDto` gana `maintenancePlanId`. Frontend: página `/maintenance` (lista de planes con frecuencia/próxima/estado + Generar ahora/Pausar/Borrar + diálogo de creación con frecuencia condicional: días de la semana para semanal, día del mes para mensual), item sidebar `Wrench` en **Operativa** (gated `tasks:read`). e2e `maintenance` (crea plan trimestral → run genera tarea ligada + avanza nextRunDate → re-run no duplica → pausar).
- **Rondas con checklist (2026-06-25):** segundo PR del bloque de mantenimiento — las tareas (sobre todo rondas) pueden tener **puntos a marcar** (puerta cerrada, luces, cámaras…) como evidencia de inspección. Migración `20260625120000`: `maintenance_plans.checklist_template` (jsonb, los puntos de la plantilla) + `tasks.checklist` (jsonb, instancia con estado por punto). Shared: `ChecklistTemplateItemSchema` (`{label}`) en `maintenance` (Create/Update plan ganan `checklistTemplate`) + `ChecklistItemDto` (`{id, label, status: pending|ok|issue, note}`) + `UpdateChecklistItemSchema` en `operations`; `TaskDto` gana `checklist`. **Generación**: `MaintenanceService.generateFromPlan` copia `checklistTemplate` → `task.checklist` (cada punto con `id` UUID + `status:'pending'`). **Marcado**: `TasksService.updateChecklistItem` + `PATCH /tasks/:id/checklist/:itemId` (`tasks:write`) → marca un punto ok/issue + nota (404 `checklist_item_not_found`). Frontend: editor de checklist (añadir/quitar puntos) en el diálogo de crear plan (`/maintenance`); sección **Checklist** en el detalle de tarea (`/tasks` → `ChecklistSection`) con botones OK (✓) / Incidencia (⚠) + nota por punto y, si un punto es incidencia, botón **"Abrir incidencia"** que crea una `incident` (reusa `useCreateIncident`, severity medium, `metadata.source='checklist'`, hereda el local de la tarea). e2e `maintenance` 2/2 (el segundo: plan con template → tarea generada con 2 puntos pending → marcar uno como issue+nota → itemId inexistente 404). **Gotcha**: el cast del jsonb del checklist a `Prisma.InputJsonValue` necesita `as unknown as`. Cierra el bloque "mantenimiento recurrente + rondas".
- **Insights heurísticos (churn risk + precio dinámico)**: `InsightsService` en `AnalyticsModule` (sin ML, read-only). `GET /analytics/churn-risk` puntúa 0-100 cada contrato `active|ending` por señales (facturas vencidas, deuda > 1 mensualidad, cobros fallidos, dunning ejecutado, vencimiento ≤60d sin auto-renovación, sin método de pago) → niveles high/medium/low + factores; el detalle omite `low` y ordena por score desc. `GET /analytics/pricing-suggestions` recomienda precio por ocupación de cada unit type (≥90% +10%, ≥80% +5%, ≤60% -5%, ≤40% -10%, resto mantener). **Aplicar (2026-06-25)**: `POST /analytics/pricing-suggestions/apply {unitTypeId, price}` (`@RequirePermission('units:manage')` a nivel de método, override del `analytics:read` de clase) → `InsightsService.applyPricing` fija `unit_types.default_price_monthly` (precio de catálogo para **nuevos** contratos; los activos NO cambian — para subir cartera está ECRI/rent-increases) + audit `pricing.suggestion_applied`. El `InsightsService` inyecta ahora `AuditService` (gotcha: el unit spec lo construía con 1 arg). UI: botón **Aplicar** por fila en la pestaña "Precio dinámico" (gated `units:manage`, deshabilitado si `action==='hold'`). **Forecasting** (`getRevenueForecast` + `GET /analytics/forecast?months=`, 1-24, default 6): proyecta MRR + contratos activos + ocupación mes a mes por tendencia (tasa media de baja + altas medias de los últimos N meses cerrados + valor medio por contrato); devuelve `current` + `assumptions` (transparenta el modelo) + `points`. UI: pestañas "Riesgo de baja" + "Precio dinámico" + "Previsión" (AreaChart doble eje) en `/analytics`. Tests: unit `insights.service` 6/6 + e2e `insights` 4/4.
- **Pricing por trastero INDIVIDUAL — dinámico + competencia (2026-07-01, PRs #182/#183):** extiende el pricing dinámico (que era **por tipo**) a **trastero individual**, con desglose transparente de factores. `InsightsService.getUnitPricingSuggestions(tenantId, facilityId?, includeCompetition?)` sugiere precio por cada trastero **disponible** combinando: **(1) ocupación de su dimensión** `(facilityId, unitTypeId)` (≥90% +8% · ≥80% +4% · ≤60% −4% · <40% −8%) + **(2) días vacío** (del último paso a `available` en `unit_status_history`, si no desde `createdAt`: ≥15 −2% · ≥30 −5% · ≥60 −8% · >90 −12%) + **(3) competencia** (v2, opcional). Suma **acotada `[−20%,+15%]`** (asimétrico, cauto al subir), redondeo a **euro entero**, «mantener» si `<±2%`. `GET /analytics/unit-pricing-suggestions?facilityId=&includeCompetition=` + `POST .../apply {unitId, price}` (`@RequirePermission('units:manage')`, override del `analytics:read` de clase) → fija `unit.basePriceMonthly` (solo **nuevos** contratos; la cartera se sube con ECRI/rent-increases) + audit `pricing.unit_suggestion_applied`. Shared `UnitPricingSuggestion(s)Dto`/`UnitPricingFactorDto`/`ApplyUnitPricingSchema` en `reports`. UI: pestaña **«Precio por trastero»** en `/analytics` (filtro por local + tabla actual→sugerido + motivo desglosado + «Aplicar» por fila + checkbox «Incluir competencia»). **Factor competencia (v2, #183 — entrada MANUAL, no scraping):** migración `20260701140000` `competitor_facilities` (local de competencia + opcional vínculo a mi `facilityId` SET NULL) + `competitor_units` (**solo m²** `areaM2`, `priceMonthly`, `status` available/occupied, `lastCheckedAt`), ambas RLS. `CompetitorsModule` CRUD (`GET/POST/PATCH/DELETE /competitors` + `/competitors/:id/units` + `/competitors/units/:unitId`; lecturas `analytics:read`, gestión `units:manage`; `lastCheckedAt` se **re-fija al tocar el precio** — «fecha de última comprobación»). En el cálculo: si `includeCompetition`, casa por **banda de m² (±20%, `COMP_BAND_PCT`)** los trasteros **disponibles** de la competencia → **mediana** → si tu precio > mediana×1.08 (`COMP_MARGIN_PCT`) **−6%** (`COMP_ADJ`, caro) / < mediana×0.92 **+6%** (barato); factor en el desglose. Página **`/competitors`** (cards por competidor + CRUD de sus trasteros con **toggle disponible/ocupado** de un clic; item «Competencia» en grupo Análisis, icono `Store`) + shared module `competitors`. **Follow-up (punto 7)**: inferir la **ocupación de la competencia** fichando también sus trasteros ocupados (los datos ya lo permiten). e2e `unit-pricing-suggestions` 1/1 (0% ocupación → −8% → aplicar cambia basePriceMonthly) + `competitors` 1/1 (sin competencia −8% → caro −14% → marcar ocupado vuelve a −8%).
- **Pases nocturnos para el staff — subpágina + badge (2026-07-01, PR #181):** el staff veía un pase nocturno comprado por el inquilino como **una credencial PIN más**, sin distinguirlo. Ahora **sin migración**: (1) **badge** en la tabla de `/access/credentials` («🌙 Pase nocturno · usos/1» cuando `label==='Pase nocturno'` o `maxUses===1 && bypassCurfew`; «24 h» para credenciales con `bypassCurfew` que no son single-use); (2) nueva pestaña **Accesos → «Pases nocturnos»** (`/access/night-passes`) con listado (inquilino, comprado, caduca, estado sin usar/utilizado/caducado) + resumen (total/sin usar/utilizados/caducados/**INGRESOS**). Backend `AccessCredentialsService.listNightPasses` (credenciales `metadata.source='night_pass'` → estado derivado de `usesCount`/`expiresAt`/`status`; **ingresos** = suma de `invoiceItem` con `description contains 'Pase nocturno'` no draft/cancelled) + `GET /access/credentials/night-passes` (`access:read`, **declarado antes de `:id`**). Shared `NightPassStaffDto`/`NightPassListDto` en `access`. **Dónde ve el staff un pase nocturno (3 sitios)**: credenciales (con badge) + factura F1 «Pase nocturno» en la ficha del inquilino + registro de accesos (`/access/logs`) al usarlo. e2e `staff-night-passes` 1/1 (compra → 1 pase `active` + revenue 6.05 = 5×1.21 + label en credenciales).
- **Pestaña «Hoy» operativa — entradas/salidas + seguimientos + acciones inline (2026-07-01, PR #184):** rediseño de `/today` de bandeja informativa a **centro operativo del día**, **sin migración**. `TodayService.getToday` amplía el `Promise.all` y `TodayDto` gana `date`/`urgentCount`/`moveInsToday`/`moveOutsToday`/`followupsDue`/`newLeads`/`signaturesPending`/`invoicesDueToday`; `TodayItemDto` gana `overdue`/`linkId`. Secciones nuevas: **entradas/salidas de hoy** (contratos `active|ending` con `startDate`/`endDate` = hoy → move-in a emitir acceso / move-out a hacer check-out+fianza), **seguimientos CRM** (`customer_followups` `pending` con `dueDate<=hoy`, enlace al cliente vía `linkId`), **leads nuevos** (`status:'new'` sin contactar), **firmas pendientes** (contratos `signedAt=null` + `signingTokenHash!=null` + `signingTokenExpiresAt>=now`), **facturas que vencen HOY** (issued `dueDate`=hoy, distinto de las ya vencidas). **Acciones inline** desde la bandeja: completar tarea (`useTransitionTask` → `{status:'done'}`, gated `tasks:write`) y marcar seguimiento hecho (`useUpdateFollowup`, `customers:write`). **UX**: cabecera con fecha del día + «N cosas urgentes» (`urgentCount` = tareas+seguimientos+move-ins+move-outs+vencen-hoy+vencidas), ítems atrasados (`overdue`) en rojo, enlaces al recurso exacto (`SectionCard` con `itemHref(it)` + botón ✓ opcional). **Follow-up**: filtro por local (la mayoría de secciones —leads/seguimientos/facturas— no están ancladas a local). e2e `dashboard-today` 2/2 (estructura nueva a 0 en tenant limpio + un lead nuevo/seguimiento de hoy cuentan con `urgentCount`).
- **Reorganización del menú lateral — submenús desplegables (2026-07-01, PRs #186–#190):** limpieza de navegación **frontend puro** (i18n `sidebar.*` + `app-sidebar.tsx`; rutas y backend sin tocar). **(#186) Analítica**: las dos vistas de pricing («Precio dinámico» por tipo + «Precio por trastero» individual) se agrupan en **una sola tab «Precios»** con sub-pestañas «Por tipo de trastero» / «Por trastero individual» (sub-`Tabs` anidado). **(#187) Soporte de submenús en el sidebar**: `NavItem` gana `children?`; `renderItem` renderiza un item desplegable con `SidebarMenuSub`/`SidebarMenuSubItem`/`SidebarMenuSubButton`; helper `badgeCount(href)` (nº de pendientes por href) → el badge del padre plegado = **suma de los de sus hijos** (cada hijo conserva su badge); estado `openSubs` (abre al clic, y automáticamente si una ruta hija está activa); el **filtro de nivel grupo** cuenta un submenú solo si le queda **algún hijo visible** (así los submenús con features off —SEPA/conciliación/ECRI/automations según plan— se **ocultan solos** sin dejar cabecera vacía). Primer uso: **«Peticiones de trastero»** (Cambios de trastero + Solicitudes/Trastero adicional) en Operativa. **(#188) «Facturación y cobros»**: Facturas (suelto) + **Cobros por banco** (Remesas SEPA + Conciliación) + **Fiscalidad** (Libro IVA/303/347 + Veri\*Factu) + **Cartera** (Subidas de precio + Renovaciones). **(#189) «Crecimiento (CRM)»**: **Captación** (Leads + Seguimientos) + **Mensajería** (Campañas + Comunicaciones + Plantillas + Automatizaciones) + **Fidelización** (Promociones + Referidos + Reseñas). **(#190) «Operativa»**: Inquilinos (suelto) + **Contratación** (Contratos + Reservas) + Peticiones de trastero (submenú) + **Operaciones diarias** (Calendario + Tareas + Mantenimiento + Incidencias) + Accesos (suelto). **Restricción**: submenús de **UN solo nivel** (no anidables → «Peticiones de trastero» no puede meterse dentro de otro submenú). Los grupos «Inventario» y «Análisis» se dejaron planos (pocos items). **Gotcha stacked**: #188→#189→#190 comparten el soporte de #187 y todos tocan `app-sidebar.tsx` → cadena stacked; se rebasa cada rama a `main` (`git rebase origin/main` skippea los commits ya mergeados) + `push --force-with-lease` conforme mergea el anterior. Verificado por PR: typecheck 8/8 + lint + build web. **(#193 fix i18n)**: `opsContracting`/`opsDaily` (labels de los submenús de «Operativa») se insertaron por error en el **namespace de descripciones de la landing** de `es.json` (había otro `"reservations"` allí), no en `sidebar` → el menú pintaba la clave cruda `sidebar.opsContracting`. Movidas a `sidebar`. **Gotcha permanente**: `messages/es.json` tiene labels repetidos (p. ej. `reservations`) en varios namespaces (landing vs sidebar) → al añadir claves i18n con `sed`/script por anchor, verificar que caen en el objeto correcto (`python3 -c "import json;print('opsDaily' in json.load(open('apps/web/messages/es.json'))['sidebar'])"`).
- **Responsive móvil — panel tenant + sidebar admin como drawer (2026-07-01, PR #192):** el panel no se veía bien en móvil. **Causa raíz (tenant)**: el `SidebarInset` (`<main>`) no tenía **`min-w-0`** → en un contenedor flex, el contenido ancho (tablas) **rompía el layout** provocando **scroll horizontal de toda la página** en móvil en vez de scrollear localmente; añadido `min-w-0` (fix de mayor impacto: arregla todas las páginas de listado a la vez; el `Table` ya trae `overflow-auto` y el `TabsList` `overflow-x-auto`, pero sin el `min-w-0` del padre empujaban la página). + header responsive (`gap-2 px-3` → `sm:gap-3 sm:px-4`, acciones e icono `shrink-0`) + `FacilitySwitcher` con nombre corto en móvil (`max-w-[88px] sm:max-w-[180px]`). **Admin en móvil**: el `<aside>` del super admin (`apps/web/src/app/admin/layout.tsx`) era `hidden md:flex` → en móvil **desaparecía sin forma de abrirlo** (no había trigger ni drawer). Ahora, como el panel del tenant: el nav se extrae a `AdminNavLinks` (reutilizado), en desktop sigue el aside fijo, y en móvil hay un **botón hamburguesa (`md:hidden`) en el header que abre el nav en un `Sheet`** (drawer lateral, `side="left"`), que se cierra al elegir opción o al cambiar de ruta (`useEffect` sobre `pathname`). Frontend puro; rutas y backend sin tocar. **Gotcha permanente**: cualquier `<main>`/contenedor flex que aloje tablas necesita `min-w-0` o el contenido ancho rompe el layout en móvil.
- **Locales/Trasteros más visuales (2026-06-25):** frontend-only, sin tocar API. (1) **Locales** (`/facilities`) pasa de `DataTable` a **grid de tarjetas** (`FacilityCard`): muestra la **imagen del local** (`FacilityDto.images[0].url`, que se podía subir pero no se veía en la lista) o un placeholder con icono, ciudad, **barra de ocupación coloreada** (`barColor` por %: verde≥85/azul≥60/amarillo≥35/rojo) y nº de trasteros; + fila de 4 KPIs de resumen (locales/trasteros/ocupados/ocupación media) + búsqueda client-side por nombre/ciudad. **Gotcha**: `const all = facilities.data ?? []` crea array nuevo cada render → envolver en `useMemo` (si no, `react-hooks/exhaustive-deps` rompe el lint). (2) **Trasteros** (`/units`) conserva la tabla con filtros + añade una fila de **5 KPIs por estado** (de `useOccupancyDashboard().byStatus`) **clicables** (filtran la tabla por ese estado). (3) **Vista de plano** (`/units` toggle Tabla/Plano): `plan-viewer.tsx` (react-konva, `dynamic` con `ssr:false` — Konva no va en SSR) reusa la lógica del editor de planos (`plan-editor.tsx`: zoom/pan/pinch/`fitToViewport`/imagen de fondo del floor/autoflow de los units sin posición) en **modo solo lectura**: dibuja cada trastero en su posición física con **relleno por estado** (`STATUS_FILL`) + código + leyenda; clic en un trastero → `/units/[id]`. En modo plano, selectores de **local + planta** (`useFloors`, que tiene `enabled:!!facilityId`); guardas: pide elegir local / avisa si el local no tiene plantas. **Nota**: los colores de estado divergen entre `plan-editor`/`plan-viewer` (occupied=rojo) y el dashboard (`occupancy-card`, occupied=verde) — convenciones distintas heredadas, no unificadas. Verificado: typecheck 8/8 + lint 8/8 + build web 74/74.
- **Plano de trasteros: UX + más info (2026-06-25, follow-up):** mejoras sobre la vista de plano. (1) **Auto-selección de local**: en `/units`, un `useEffect` selecciona el local automáticamente si solo hay uno (`facilities.data.length===1 && !facilityId`); con varios, **recuerda el último** vía `useFacilityStore` (zustand `persist` en localStorage `storageos.facility`, **compartido con el selector de la cabecera**) — wrapper `selectFacility(id)` = `setFacilityId` + `setCurrentFacility(id ?? null)`, usado en los `onValueChange` del selector de local (tabla y plano) en vez del `setFacilityId` pelado. (2) En `plan-viewer.tsx`, cada trastero muestra **código + m²** (Konva `Text` multilínea con `\n` + `lineHeight`) y al **hover** un **tooltip HTML** (estado `hover{unit,x,y}` desde `onMouseMove`+`stage.getPointerPosition()`; div `absolute pointer-events-none` posicionado, clamp al ancho del viewport) con tipo · m² · **precio/mes** · estado. `UnitRect` gana `areaM2`/`price`/`typeName` (de `UnitDto.areaM2`/`basePriceMonthly`/`unitTypeName`). Verificado: typecheck 8/8 + lint 8/8 + build web 74/74.
- **Historial de pagos del inquilino + meses por adelantado (2026-06-25):** nueva pestaña **«Historial de pagos»** en la ficha del inquilino (`/customers/[id]`, `payment-history-tab.tsx`); la antigua «Pagos» (métodos de pago tarjeta/IBAN) se renombra a **«Métodos de pago»**. **Sin migración** (el modelo ya tenía `invoices.period_start/period_end`). **Backend**: `InvoiceDto` gana `unitCode` + `facilityName` (cambio **aditivo**, retrocompatible) vía ampliar el `include` de `contract` en `InvoicesService.includeRelations()`/`InvoiceWithRelations`/`toDto` con `unit: { code, facility: { name } }`. Reutiliza los endpoints existentes `GET /invoices?customerId=` + `GET /payments?customerId=` (no se tocó el controller). **Frontend** (todo el cálculo en cliente con `useInvoices`/`usePayments`): 4 KPIs (total pagado = Σ`amountPaid`; pendiente = Σ`amountPending` de issued/overdue; **pagado por adelantado**; pagos con retraso) + tabla de **cargos mensuales** (facturas, excluye `draft`: periodo de `periodStart`, trastero·local, vencimiento, importe, `InvoiceStatusBadge`, **puntualidad** = `paidAt`≤`dueDate`→«En plazo» / si no «+N d» rojo) + tabla de **transacciones de cobro** (payments: fecha, método, importe, reembolsos, estado). **«Pagado por adelantado»** = mes más lejano cubierto por una factura **pagada** (`max(periodEnd)`) menos el mes actual (en meses); funciona tanto con N facturas mensuales pagadas como con una promo anual. Verificado: typecheck 8/8 + lint 8/8 + build web 74/74 + e2e invoices/payments 11/11. **Follow-up — concepto + desglose (2026-06-25):** la tabla de cargos solo mostraba el `total`, así que una **venta de producto** (factura con `customerId` pero sin `contractId`/`periodStart` — `ProductSalesService` crea una F1 con customer) salía con «—» sin saber qué era, y el **seguro** (línea «Protección de contenido…» DENTRO de la factura mensual del alquiler, no factura aparte) no se veía. Ahora la columna «Periodo» pasa a **«Concepto»** (`invoiceConcept()`: `lateFeeForInvoiceId`→«Recargo»; `contractId`/`periodStart`→«Alquiler» + mes + «incluye protección» si algún `item.description` casa `/protecci|seguro/i`; si no →«Producto» con el `description` del primer item) + cada fila es **expandible** (`InvoiceRow` con `useState`) y muestra `invoice.items` (description + `total`) que el `InvoiceDto` ya traía. Frontend puro.
- **Enlaces cruzados entre entidades (2026-06-25):** las referencias (inquilino/trastero/local/contrato/factura) que aparecían como **texto plano** en las tablas ahora **enlazan a su ficha** (patrón `<Link href={`/customers|units|facilities|contracts/${id}`} className="hover:underline">`). Sitios: **Contratos** lista (inquilino→`/customers`, local→`/facilities`, trastero→`/units`) + **Reservas** lista y `reservations-tab` del inquilino (local + trastero, inquilino si `customerId`) + **Pagos** lista (cliente) + **Facturas** lista (cliente si identificado) + **Factura** detalle (contrato→`/contracts`) + **Trasteros** lista (local) + **Incidencias** lista (inquilino + local, nullable) + **Registro de accesos** (inquilino) + **pestaña Contratos** del inquilino (trastero). Los campos nullable solo enlazan si el id existe. **Follow-up — historial de pagos (2026-06-25):** para enlazar trastero/local en `payment-history-tab` hizo falta que `InvoiceDto` ganara **`unitId` + `facilityId`** (cambio **aditivo**): el `include` de `contract.unit.facility` ya existía, solo se añadió `id` al `select` (unit + facility) + `InvoiceWithRelations` + `toDto`. e2e invoices 7/7. Todo frontend salvo ese DTO aditivo. **Pendiente conocido**: el `UnitDto`/`ContractDto` ya traen los ids; el `CommunicationDto` no referencia unit/contract (solo recipient), por eso `/communications` no enlaza.
- **Historial de comunicaciones + interacciones manuales del inquilino (2026-06-25):** nueva pestaña **«Comunicaciones»** en la ficha del inquilino (`/customers/[id]`, `communications-tab.tsx`) con un **timeline unificado** de dos fuentes ordenadas por fecha desc: (1) **outbox saliente** —reutiliza `useCommunications({customerId})` (`GET /communications?customerId=`, ya existía con `customerId`): emails/WhatsApp de dunning, campañas, reviews, automations, manuales; muestra canal, asunto, `bodyText`, estado y **`source` traducido a etiqueta legible** (`sourceLabel`: dunning→«Recordatorio de pago», campaign→«Campaña», etc.)— y (2) **interacciones manuales (NUEVO, full-stack)**: llamadas/visitas/reuniones/notas de lo hablado. **Migración `20260625130000_customer_interactions`**: tabla `customer_interactions` (RLS; tenant + customer + **user_id** del staff que la creó + `type` TEXT (enum Zod, no PG) + `content` + `occurred_at`). Modelo Prisma `CustomerInteraction` + 3 relaciones inversas (Customer/Tenant/User; **el User usa `fullName`, no firstName/lastName** — gotcha). Shared: `InteractionTypeEnum` (note/call/visit/meeting/whatsapp/email/other) + `CreateCustomerInteractionSchema` + `CustomerInteractionDto` (con `userName`). `CustomerInteractionsService`/`Controller` en `CustomersModule`: `GET`/`POST /customers/:customerId/interactions` (`customers:read`/`customers:write`) + `DELETE` (`customers:write`). **Gotcha**: el include de Prisma con `as const` da error de tipo (readonly) → usar `satisfies Prisma.CustomerInteractionInclude` + `Prisma.CustomerInteractionGetPayload`. El UUID del staff es `user.sub` (no `userId`). **Alcance**: outbox saliente + notas manuales; **no hay inbound** (respuestas del inquilino por WhatsApp/email) — follow-up. e2e `customer-interactions` 1/1 (crea/lista/borra + aislamiento por tenant). Verificado: typecheck 8/8 + lint 8/8 + build web 74/74.
- **Detalle de trastero: inquilino actual + edición (2026-06-25):** frontend-only en `/units/[id]`. (1) Card **«Estado actual»**: el estado + si hay contrato vigente, el **inquilino que lo ocupa** (link a `/customers/[id]`) + botón «Ver contrato» (link a `/contracts/[id]`); si `available`, lo indica. El contrato vigente sale de `useContracts({ unitId })` (el backend ya filtraba por `unitId`: `@Query('unitId')` en `contracts.controller` → `where.unitId`) buscando status `active`/`ending`. (2) Botón **«Editar trastero»** (gated `units:write`, **oculto si está alquilado** = `status==='occupied'` o hay contrato vigente) → diálogo que edita dimensiones (ancho/fondo/alto → `areaM2`/`volumeM3` son columnas generadas, se recalculan solas), `basePriceMonthly` y `notes`. **Reutiliza `UpdateUnitSchema` + `useUpdateUnit` (`PATCH /units/:id`) que YA existían — solo faltaba la UI** (por eso parecía que no se podían editar trasteros ya creados). `react-hook-form` con `values` (no `defaultValues`) para precargar del `UnitDto`; inputs numéricos con `onChange={(e)=>field.onChange(e.target.valueAsNumber)}`. Verificado: typecheck 8/8 + lint 8/8 + build web 74/74.
- **Reports** (Fase 6): `ReportsModule` con registry `ReportGenerator`, renderer PDF (Puppeteer dynamic import) + Excel (exceljs), cola BullMQ `reports`, `report_runs` con polling, subida a MinIO bucket `storageos-reports`.
- **Control de accesos** (Fase 7): `AccessCredential` (PIN/QR/RFID con state machine `pending→active→suspended⇄active→revoked` y `secretHash` argon2id, `revealedSecret` solo en create/rotate), `AccessDevice` (cerraduras con `apiKeyHash` argon2id), `AccessLog` (audit trail completo de cada intento). `LockProvider` abstracto + `StubLockProvider` (dev) + `MqttLockProvider` (publica comandos a broker MQTT con env `LOCK_PROVIDER=stub|mqtt`). Endpoint `POST /access/verify` con header `X-Device-Key` para autenticación HTTP de devices, sanitiza `attemptedValue` (PIN last4, QR first8, RFID UID completo).
- **Acceso del inquilino por QR/PIN (portal)**: además del hash argon2 (no reversible), las credenciales pin/qr guardan ahora una **copia cifrada AES-256-GCM** del secreto en `access_credentials.secret_encrypted` (migración `20260618120000`, poblada en create/rotate vía `CryptoService`) para poder volver a mostrárselo a su dueño. `AccessCredentialsService.listForCustomer/regenerateForCustomer` + endpoints portal `GET /portal/me/access` (lista pin/qr activas con el valor descifrado) y `POST /portal/me/access/:id/regenerate` (throttle login; verifica propiedad por `customerId`). UI en `/portal/consume`: card "Tu acceso" con QR (`qrcode.react`) + PIN + botón regenerar. El inquilino presenta el código en el lector → flujo `/access/verify` existente. `PortalModule` importa `AccessModule`. Credenciales antiguas (sin `secret_encrypted`) devuelven `value:null` → regenerar para obtener uno visible. e2e `portal-access` 3/3. **(2026-06-23) Accesos adicionales autoservicio:** el inquilino se crea accesos extra (familiar, empleado…) desde el portal hasta un máximo por tenant `tenants.extra_access_limit` (migración `20260623100000`, default 2). `POST /portal/me/access/extra {label}` → `AccessCredentialsService.createExtraForCustomer` (cuenta credenciales activas/suspendidas con `metadata.source='portal_self_service'`; 409 `extra_access_limit_reached` al exceder; emite PIN con etiqueta). El límite se ajusta en `GET/PATCH /settings/tenant/access` (`settings:read`/`settings:manage`, schema `UpdateTenantAccessSettingsSchema` 0-10) → card "Accesos adicionales del inquilino" en `/access/credentials`. UI portal: botón "Añadir acceso" en la card "Tu acceso". e2e `portal-access` 4/4 (extra → 201 PIN, segundo → 409 con límite 1).
- **Provider HTTP + apertura remota**: `HttpLockProvider` (`LOCK_PROVIDER=stub|mqtt|http`) hace POST a la `controlUrl` del device firmado con HMAC-SHA256 (cabecera `X-StorageOS-Signature: t=<ts>,v1=<hmac>` sobre `<ts>.<body>`; mismo estilo que los webhooks salientes); no lanza (timeout 8s con `AbortController`, devuelve `dispatched:false` + motivo). `access_devices.control_url` + `control_secret_encrypted` (AES-GCM, migración `20260618140000`); el secreto se descifra en `access-devices`/`access-verify` y se pasa por `OpenLockArgs.{controlUrl,controlSecret}`. Funciona con cualquier controlador HTTP (Shelly/ESP32/Home Assistant/gateway). **Apertura remota** staff `POST /access/devices/:id/open` (`@RequirePermission('access:manage')`) → `AccessDevicesService.remoteOpen` llama al provider + registra `access_logs` (`method='pin'` placeholder, `metadata.remote=true`, reason `remote_open_by_staff`). UI: botón "Abrir (remoto)" en `/access/devices` + campos URL/secreto HMAC en el alta de device (`AccessDeviceDto.controlUrl` + `hasControlSecret`, sin exponer el secreto). Tests: unit `http-lock.provider` 4/4 + e2e `access` 10/10.
- **PWA del inquilino** (v1): el portal es instalable. `apps/web/public/manifest.webmanifest` (start_url `/portal/login`, scope `/portal`, standalone, iconos SVG `icon.svg`+`icon-maskable.svg`) enganchado SOLO en `(public)/portal/layout.tsx` (`metadata.manifest` + `appleWebApp` + `viewport.themeColor`) para no ofrecer instalar en staff/admin. Service worker `public/sw.js` (cache `storageos-pwa-v1`): navegaciones network-first con fallback `public/offline.html`, cache-first de `/_next/static`; **descarta `/api/`, cross-origin y no-GET** (pago/facturas requieren red). Registro vía `<PwaRegister>` (client, solo `NODE_ENV=production`, en `window.load`). Botón "Instalar app" (`<InstallPwaButton>`, captura `beforeinstallprompt`) en `/portal/consume`. Iconos en SVG (no se generaron PNG): instala en Android; iOS usa "Añadir a inicio". Sin tests automáticos (PWA difícil de e2e headless); verificación manual en móvil.
- **Super Admin** (Fase 8): tabla `super_admins` global, login propio con JWT `purpose='superadmin'` firmado con `SUPER_ADMIN_JWT_SECRET`, `AdminGuard` separado del JwtAuthGuard. Endpoints `/admin/{auth,tenants,metrics,support/tickets}`. Impersonation con `ImpersonationLog` (TTL 1h) que firma un access JWT normal con `purpose='impersonation'` y `superAdminId` claim para audit. Soporte de tickets con state machine + mensajes `isInternal` (notas privadas admin).
- **Stripe Billing SaaS** (Fase 8): distinto del gateway Fase 4. Stripe Checkout `mode='subscription'` para que el tenant pague la suscripción. Customer Portal para gestionar tarjeta/cancelar. Webhooks `customer.subscription.{created,updated,deleted}` + `invoice.payment_*` mapean a `tenant_subscription.status`. Stripe SDK 22: `current_period_*` en `items.data[0]`. El catálogo de planes (`SubscriptionPlansController`): `GET /subscription-plans` público (pricing landing); `GET /subscription-plans/admin` + `POST/PATCH/DELETE` protegidos con `@UseGuards(AdminGuard)` (super admin) — antes era un apaño `@Roles('owner')`. `BillingSaasModule` registra `AdminGuard` + `JwtModule.register({})` para resolver su `JwtService` sin acoplar todo `AdminModule`.
- **Access integrations** (Fase 8): `AccessIntegrationsService` escucha `domain.contract_signed` → emite PIN + email `access_credential_issued_email`. Escucha `domain.invoice_paid` → reactiva credenciales suspendidas por dunning. `DunningService.executeAction('access_block')` llama `AccessIntegrationsService.suspendForDunning` con `reason: 'dunning:invoice-<id>'`. **(2026-06-23) Auto-emisión al primer pago:** el listener `invoice_paid`, tras reactivar, comprueba `listForCustomer`; si el inquilino **no tiene ninguna credencial activa** (modelo "pago primero", o contratos sin evento de firma) le emite un PIN + email (mirando el email del customer vía Prisma). La lógica de emisión se extrajo a un helper privado `issueCredential` compartido por `contract_signed` e `invoice_paid` (idempotente: no duplica si ya tiene acceso). `AccessIntegrationsService` inyecta ahora `PrismaService`. **(2026-06-23) Página de registro de accesos:** faltaba `apps/web/src/app/(app)/access/logs/page.tsx` — la pestaña "Registro" del layout enlazaba a `/access/logs` (con `useAccessLogs` + el endpoint `GET /access/logs` ya existentes) pero la página nunca se creó → 404. Creada (tabla con filtros por resultado/dispositivo, badges de resultado, gated por el sidebar `access:read`). Además el `AccessLogsController` **no tenía `@RequirePermission`** (se quedó fuera de la migración RBAC; cualquier autenticado leía el log) → añadido `@RequirePermission('access:read')` a nivel de clase.
- **Super admin 2FA + refresh cookie httpOnly** (Fase 9): nuevas tablas `super_admin_sessions` y `super_admin_recovery_codes`; `super_admins` con `two_factor_*`. `SuperAdminTwoFactorService` reusa el `TotpService` de tenant (un solo motor TOTP), secret AES-256-GCM en BD y 10 recovery codes `XXXX-XXXX` argon2id, single-use. Login en dos pasos cuando 2FA está activo: primer `/admin/auth/login` devuelve `{requires2fa, pendingToken}` (JWT `purpose='superadmin-2fa-pending'`, secret `JWT_2FA_PENDING_SECRET`, TTL 5 min) y NO emite cookie; `/admin/auth/2fa/challenge` canjea. La cookie de refresh `super_admin_refresh` es `httpOnly + secure + sameSite=strict + path=/admin`, con rotación atómica y revocación total de sesiones del admin ante reuso paranoid. TTL via `SUPER_ADMIN_REFRESH_TTL_SECONDS` (default 7d). Acciones críticas del super admin se loguean al stdout via `Logger` (no a `audit_logs`, cuya FK `tenant_id` es NOT NULL).
- **AeatClient abstracto** (Fase 9): `apps/api/src/modules/billing/aeat-client/` con `AeatClient` abstract + `StubAeatClient` (default dev/test) + `RealAeatClient` skeleton (lanza `not_implemented` hasta certificación AEAT). Factory en `BillingModule` resuelve la implementación según `AEAT_MODE=stub|sandbox|production`. Las invoices siguen calculando hash + QR como antes; sólo cambia el envío.
- **`security_events` global** (Fase 11A.1): tabla **sin `tenant_id`** (FK de `audit_logs` es NOT NULL) que persiste `login_failed_tenant_not_found`, `login_failed_email_not_found`, `login_failed_wrong_password` y `refresh_token_reuse`. `SecurityEventsService` invocado desde `AuthService`/`SessionsService`. Endpoint `GET /admin/security-events` con filtros (`eventType`, `email`, `fromDate`, `toDate`) + cursor pagination. Cron diario `0 3 * * *` que borra eventos > 90 días. Los eventos de rate-limit (`login_failed_throttled`, `register_throttled`, `password_reset_throttled`) los persiste `SecurityThrottlerGuard` (extiende `ThrottlerGuard`, registrado como primer `APP_GUARD` en vez del base): cuando el throttler corta un endpoint sensible de auth, mapea la ruta al `eventType` y llama `SecurityEventsService.record` antes de lanzar `ThrottlerException`.
- **`tenant_aeat_credentials` histórico** (Fase 11A.2): drop del UNIQUE en `tenant_id`; ahora la activa se identifica por `revoked_at IS NULL`. `TenantAeatCredentialsService.upload` reescrito como `$transaction` (UPDATE actual con `revoked_reason='rotated'` + INSERT nueva). Nuevo `listHistory(tenantId)` + endpoint `GET /billing/aeat-credentials/history` (role `owner|manager`).
- **Rectificativas Veri\*Factu R1-R5** (Fase 11A.4): enums `InvoiceType` (F1, F2, R1-R5) + `CorrectionMethod` (I/S). Columnas `invoice_type`, `rectifies_invoice_id` (FK self), `rectification_reason`, `correction_method` en `invoices`. `InvoicesService.rectify(originalId, args)` crea draft (items pueden ser negativos). `VerifactuXmlBuilder` añade `<TipoRectificativa>I</TipoRectificativa>` + `<FacturasRectificadas>` cuando `invoiceType` empieza por R. `RealAeatClient` carga la original via Prisma. Endpoint `POST /invoices/:id/rectify` (role `owner|manager`).
- **Forzar 2FA owner/manager** (Fase 12A.1): nueva columna `require_two_factor_for_managers BOOLEAN NOT NULL DEFAULT false` en `tenants`. Cuando está activa, el login de un user `owner`/`manager` sin 2FA devuelve `{requires2faEnrolment: true, enrolmentToken}` sin emitir access. `enrolmentToken` es JWT corto firmado con `JWT_2FA_PENDING_SECRET` y `purpose='2fa-enrolment'` (TTL 10 min). Endpoints públicos `POST /auth/2fa/enrol-required/{setup,verify}` + endpoint admin `PATCH /settings/tenant/security` (role `owner`).
- **Alertas brute-force** (Fase 12A.2): `SecurityAlertsService.scanAndAlert()` con `groupBy` sobre `security_events` filtrado por ventana + `HAVING count ≥ threshold`. Dedup in-memory por `${kind}:${identifier}`. Email vía `EmailProvider` al `SECURITY_ALERT_EMAIL`. Cron `*/5 * * * *` + endpoint manual `POST /admin/security-alerts/scan` (`AdminGuard`). Env nuevas: `SECURITY_BRUTE_FORCE_THRESHOLD=5`, `SECURITY_BRUTE_FORCE_WINDOW_MINUTES=15`, `SECURITY_ALERT_EMAIL`.
- **`super_admin_audit_logs` en BD** (Fase 12A.3): tabla global nueva **sin `tenant_id`** y **sin RLS**. `SuperAdminAuditService.record()` defensivo (try/catch silencioso). Integrado en `SuperAdminAuthService.login` (login.success/failed), `SuperAdminTwoFactorService` (2fa.enabled/disabled/regenerated/challenge), `SuperAdminTenantsService.{suspend,reactivate,extendTrial,impersonate}` (tenant.\*). Endpoint `GET /admin/audit-logs` con filtros (`action`, `superAdminId`, `targetTenantId`, `fromDate`, `toDate`) + cursor.
- **Worker separado `apps/worker`** (Fase 13A.1): nuevo paquete en el monorepo (`name: worker`) con Dockerfile multi-stage (Chromium del sistema), `tsconfig.json` con `rootDir: ".."` + `jsx: react-jsx` que permite importar por path relativo los módulos del API que tienen processors/crons (Billing, Communications, Automations, Reports, Dunning, Access, SecurityEvents + infra). `main.ts` usa `NestFactory.createApplicationContext` (no HTTP) + graceful shutdown SIGTERM/SIGINT. `docker-compose.prod.yml` añade servicio `worker`. Tests del worker `worker-bootstrap.spec.ts` 2/2 verdes (cierre del grafo DI + registro de las 6 colas BullMQ; antes `describe.skip`, activados al añadir `FilesModule` — única dep DI faltante, provider global que `InvoicePdfService` inyecta en `BillingModule`).
- **OpenAPI + API versioning `/v1/`** (Fase 13A.2): `@nestjs/swagger ^11` + `swagger-ui-express ^5`. Swagger UI en `/api/docs`, gated en producción con env `OPENAPI_ENABLED`. `app.enableVersioning({ type: VersioningType.URI, prefix: 'v', defaultVersion: '1' })`. Integración Zod↔OpenAPI vía **nestjs-zod 5** (`cleanupOpenApiDoc(SwaggerModule.createDocument(...))` en `main.ts`): procesa el documento ya generado e inyecta los schemas de los DTOs `createZodDto`, sin el antiguo monkey-patch `patchNestJsSwagger()` (que en v4 hacía un deep-import a `@nestjs/swagger/dist/services/schema-object-factory`, ruta que `@nestjs/swagger@11.4.3` dejó de exportar → `ERR_PACKAGE_PATH_NOT_EXPORTED` al arrancar en dev; ese try/catch ya no existe). El OpenAPI generado lista los ~121 schemas Zod en `components.schemas`. Legacy redirect `308 Permanent Redirect` aplicado via `app.use(legacyRedirectHandler)` ANTES de `enableVersioning` (la versión clase via `consumer.apply().forRoutes('*')` no se ejecuta antes del router con versioning activo). Excepciones `VERSION_NEUTRAL`: `/health`, `/webhooks/stripe`, `/public/widget/...`. Tests existentes funcionan via flag `rewriteLegacyToV1: true` en `test-app.factory.ts`.
- **F2 (factura simplificada)** (Fase 13A.3): `invoices.customer_id` ahora nullable. `CreateInvoiceSchema` añade `invoiceType: 'F1'|'F2'`, `customerId.optional()`, `simplifiedJustification`. Validación: F1 sin customer → 400 `customer_required`; F2 > 400€ sin justification → 400 `f2_amount_limit_exceeded`; F2 > 3000€ → 400 `f2_amount_hard_limit_exceeded`. XML AEAT: F2 sin recipient emite `<FacturaSinIdentifDestinatarioArt61d>S</...>` en lugar del bloque `<Destinatarios>` (RD 1619/2012 art. 4).
- **Rectificativas por sustitución** (Fase 13A.3): `RectifyInvoiceSchema` añade `correctionMethod: 'by_differences'|'by_substitution'`. Cuando `by_substitution`, el XML emite `<TipoRectificativa>S</TipoRectificativa>` + bloque `<ImporteRectificacion>` con `BaseRectificada`/`CuotaRectificada`/`CuotaRecargoRectificado` (cargados de la factura original).
- **Flag `ENABLE_WORKERS_IN_API`** (Fase 14A.1): env var nueva (default `true`) leída como constante en `apps/api/src/config/workers-enabled.ts` (no via `ConfigService` porque se consume en `@Module()` decorator). Cada module con processors/crons condiciona los providers via `...(WORKERS_ENABLED_IN_API ? [BillingRecurringProcessor] : [])`. Refactor: services que combinaban `@Cron` + `@Processor` partidos en service base (lógica + cron) + wrapper `@Processor` (`BillingJobsService` + `BillingRecurringProcessor`, `DunningService` + `DunningProcessor`). En `apps/worker/src/main.ts` primera línea fuerza `process.env.ENABLE_WORKERS_IN_API='true'` (defense-in-depth). `docker-compose.prod.yml` con `ENABLE_WORKERS_IN_API: 'false'` en servicio API.
- **API keys `sk_live_<tenantId>.<secret>`** (Fase 14A.3): tabla `api_keys` `(id, tenant_id, name, prefix, secret_hash argon2id, scopes JSONB, last_used_at, revoked_at)` con RLS. Token revealed-once en `POST /settings/api-keys`. `ApiKeysService.create/verify/list/revoke` + `ApiKeyGuard` con `Authorization: Bearer sk_live_...` (parsea tenantId, busca por prefix en `PrismaAdminService`, verifica argon2id contra `secret_hash`, actualiza `last_used_at`, expone `request.apiKey = { tenantId, scopes }`). **Scopes persistidos pero NO enforced en MVP** (informativos hasta Fase 15+).
- **Webhooks salientes con HMAC** (Fase 14A.3): tablas `webhooks` `(tenant_id, url, secret_encrypted AES-GCM, events TEXT[], is_active, last_delivery_at)` + `webhook_deliveries` `(webhook_id, event, payload JSONB, status, http_status, error_message, attempt, delivered_at)` con RLS. `WebhooksService.dispatch(tenantId, event, payload)` busca webhooks activos donde `event = ANY(events)` y encola en cola BullMQ `webhooks`. `WebhooksProcessor` re-serializa el payload en cada attempt (JSONB de Postgres no preserva orden de claves), calcula HMAC SHA-256 con `secret`, hace POST con headers `X-Storageos-Signature: t=<ts>,v1=<hmac>`, `X-Storageos-Event`, `X-Storageos-Delivery`. Retry 3× exponencial `backoff: { type: 'exponential', delay: 60_000 }`. Listeners `domain.invoice_paid/issued/overdue`, `contract_signed`, `lead_created` mapeados a eventos públicos `invoice.paid/issued/overdue`, `contract.signed`, `lead.created` y enviados a `WebhooksService.dispatch`.
- **AEAT `getStatus` polling** (Fase 15A.1): `RealAeatClient.getStatus(args)` con XML SOAP `ConsultaFactuSistemaFacturacion` (AEAT espera filtro por NIF emisor + número + fecha, no por CSV ni hash). `VerifactuXmlBuilder.buildConsultaFactu(args)` genera el envelope con `Cabecera/ObligadoEmision` + `FiltroConsulta/PeriodoImpositivo` + `IDFactura/NumSerieFactura/FechaExpedicionFactura`. Parseo SOAP con regex tolerante a namespaces idéntico al de `sendInvoice`. `VerifactuService.refreshStatus(invoiceId, tenantId)` carga la invoice `pending`, llama `getStatus`, actualiza `aeat_status` + `aeat_csv` + `aeat_response`. `VerifactuStatusPollerCron` con `@Cron('*/15 * * * *')` busca invoices con `aeat_status='pending'` y `aeat_sent_at < now() - 5min`, hasta 50 en batch ordenadas por `aeat_sent_at ASC`. Condicionado al spread `WORKERS_ENABLED_IN_API`. Endpoint `POST /v1/billing/invoices/:id/refresh-aeat-status` (role `owner|manager`) permite consulta manual desde la UI.
- **Webhooks retry manual + dashboard** (Fase 15A.2): `WebhooksService.retryDelivery(args)` resetea `attempts=0`, `status='pending'`, `error_message=null` **antes** de encolar (orden importa por race con el worker — si encolas primero, el worker puede leer `attempts=3` y dropear antes del UPDATE). Validaciones: `delivery_not_found` (404), `delivery_not_retryable` (400) si `status !== 'failed'`. Endpoint `POST /v1/settings/webhooks/:webhookId/deliveries/:deliveryId/retry` (role `owner|manager`). Reemplaza el "últimos 50 expand" de Fase 14A.3 por una página dedicada `/settings/webhooks/[id]` con cursor pagination + filtros (`status`, `fromDate`, `toDate`) — `webhook_deliveries` puede crecer a 100k+ filas en un tenant activo.
- **API keys scopes enforced** (Fase 15A.3): lista whitelist de 5 scopes en `packages/shared/src/integrations/api-keys.schema.ts` (`invoices:read`, `invoices:write`, `contracts:read`, `customers:read`, `webhooks:trigger`). Decorador `@RequireScope(scope: string)` + metadata `REQUIRE_SCOPE_KEY`. `ApiKeyGuard` lee con `Reflector.getAllAndOverride(REQUIRE_SCOPE_KEY, [handler, class])`; si la API key no tiene el scope ni el wildcard `'*'`, lanza `ForbiddenException` con `code: 'insufficient_scope'` + `details: { requiredScope }` (`HttpExceptionFilter` solo propaga `code` + `details` al body — usar `details`, no top-level). `ApiKeysService.create` normaliza: `[]` → `['*']` (backwards-compat con Fase 14A.3); los 5 scopes públicos completos → `['*']` (atajo); scope desconocido → 400 `invalid_scope` con `details: { invalidScope }`. Endpoint `GET /v1/integrations/whoami` con `@RequireScope('invoices:read')` como ejemplo (primer endpoint público real).

### Frontend (`apps/web`)

- **Públicas**: landing, `/login` (con paso 2FA challenge integrado), `/register`, `/verify-email-sent`, `/verify-email/[token]`, `/forgot-password`, `/forgot-password-sent`, `/reset-password/[token]`, `/invite/[token]` (aceptar invitación → login automático), `/portal/login` + `/portal/consume` (cliente final con magic link → ver y pagar facturas).
- **Autenticadas**: `/dashboard` (con `<BillingMetricsCard>` MRR + pendiente + cobrado mes y `<OccupancyCard>` Recharts), `/facilities` + `/facilities/[id]` (tabs: trasteros, plantas + plano, tipos), `/units` + `/units/[id]`, `/customers` + `/customers/[id]` (tabs: contratos, reservas, documentos, datos + KYC), `/contracts` + `/contracts/new` (wizard 4 pasos) + `/contracts/[id]` (timeline + acciones + PDF), `/reservations`, `/invoices` + `/invoices/[id]` (state machine + PDF + QR Verifactu + acciones: issue/cancel/refund/mark-paid/charge), `/payments`, `/leads` (kanban con drag-and-drop nativo), `/communications` (historial + retry), `/message-templates` (catálogo), `/automations` (reglas trigger→template), `/tasks` (lista + transiciones + comentarios), `/incidents` (lista + severity + transiciones), `/products` (tabs catálogo + ventas), `/analytics` (4 tabs Recharts: ocupación, churn, morosidad, leads), `/reports` (catálogo + tracking de runs), `/settings/users`, `/settings/profile`, `/settings/security`, `/settings/billing` (series de facturación), `/settings/widget` (snippet de embed).
- **Widget público** (sin auth): `/widget/[slug]` — iframe-friendly con middleware Next que aplica `X-Frame-Options: ALLOWALL` + CSP `frame-ancestors *`. Honeypot + throttle 5/min/IP.
- **Admin (Fase 9)**: `/admin/login` con paso 2FA challenge (cuando la respuesta trae `requires2fa: true` se muestra input de código TOTP o recovery `XXXX-XXXX`). `/admin/security` con setup (QR + secret), verify (muestra 10 recovery codes una sola vez), disable (pide password) y regenerate. Cliente admin (`lib/admin/{auth-store,api,hooks}.ts`) con refresh transparente cookie-based: el access JWT vive en memoria (Zustand), el refresh viaja en cookie httpOnly `super_admin_refresh` con path acotado a `/admin`.
- **CSP enforce + observabilidad** (Fase 11 → Fase 13A.4): `next.config.mjs` aplica `Content-Security-Policy` (enforce desde Fase 13A.4; antes Report-Only durante el mes Fase 11A.3). Directivas idénticas: `default-src 'self'; script-src 'self' 'unsafe-inline' https://js.stripe.com; img-src 'self' data: blob: https:; connect-src 'self' https:; frame-src 'self' https://js.stripe.com https://hooks.stripe.com; frame-ancestors 'none'; report-uri /api/csp-report`. Endpoint `POST /api/csp-report` que loggea violaciones a Pino. **Excepción `/widget/:path*`** en `middleware.ts`: mantiene `frame-ancestors *` + `X-Frame-Options: ALLOWALL` (iframe-friendly). Nueva página `/admin/security-events` (panel super admin) con tabla + filtros (`eventType`, `email`, rango de fechas) + cursor pagination. En `/invoices/[id]` botón "Rectificar" cuando la factura está `issued/paid/overdue` + badge "Rectificativa" cuando la factura tiene `rectifiesInvoiceId` + link cruzado a la original.
- **F2 + sustitución UI** (Fase 13A.3): dialog "Nueva factura" añade selector F1/F2 + campo `simplifiedJustification` (visible si F2). Modal "Rectificar" añade radio "Por diferencias" / "Por sustitución".
- **Enrolment 2FA forzoso** (Fase 12A.1): página pública `/security/enrolment/[token]` con 3 pasos (setup → verify → recovery codes), banner persistente y handler `beforeunload` para evitar cerrar la pestaña sin guardar los recovery codes. En `/settings/security` se añade el switch (sólo visible a `owner`) para activar/desactivar `requireTwoFactorForManagers`.
- **`/admin/audit-logs`** (Fase 12A.3): nueva página en el panel super admin con tabla + filtros (`action`, `superAdminId`, `targetTenantId`, rango de fechas) + cursor pagination. Item en sidebar admin con icono `ScrollText`.
- **Smoke tests Playwright** (Fase 12A.4): 5 specs en `apps/web/e2e/` (`01-tenant-onboarding`, `02-billing-flow`, `03-rectify-invoice`, `04-admin-2fa-impersonate`, `05-public-widget`). Helpers en `apps/web/e2e/helpers.ts`. Config `apps/web/playwright.config.ts` con `fullyParallel: false, workers: 1` (BD compartida con la API), `retries: 2` + `forbidOnly` en CI. **Gate bloqueante en CI, verde 5/5** (workflow `.github/workflows/e2e.yml`, job "Smoke E2E (Playwright)"). Estaba rojo permanente; se arregló: (1) compilar `@storageos/database` antes de `nest build` (sin su `dist/index.d.ts` daba ~709 errores TS); (2) sembrar el super admin del test 04; (3) la API arranca con `NODE_ENV=test` (hereda del job) para desactivar el throttler — antes con `production` los registros daban 429; (4) en `NODE_ENV=test` Swagger ya no se monta (`patchNestJsSwagger` de nestjs-zod petaba el boot contra la versión instalada de `@nestjs/swagger`; ahora Swagger solo en `development` o con `OPENAPI_ENABLED`); (5) selectores: `CardTitle` es `<div>` (usar `getByText`, no `getByRole('heading')`), la navegación admin debe ser SPA (un `page.goto` duro pierde el token en memoria → `/admin/login`), y la aserción del popup de impersonación tolera el redirect a `/login?next=`. Arranque con logs + **esperas fail-fast**, `retries: 2` + `forbidOnly` en CI. **`main` protegida**: solo avanza vía PR con los **4 checks required** verdes (`Smoke E2E (Playwright)`, `Lint, typecheck & build`, `Tests database (Vitest)`, `Tests API e2e (Jest + Supertest)`), 0 approvals, `enforce_admins`. También se reparó `ci.yml` (estaba rojo de antes): build web necesita `NEXT_PUBLIC_API_URL`; `test-database` necesita seed; `test-api-e2e` tenía un arg `-- --runInBand` sobrante (→ "No tests found"), no compilaba `@storageos/database` (jest lo resuelve por `dist/`) y no tenía MinIO (el test de reports sube un xlsx real; se arranca `minio/minio` con `docker run` porque `services:` no admite `command` y `bitnami/minio` ya no está en Docker Hub). Flujo de contribución en `README.md`.
- **`/settings/integrations`** (Fase 14A.3 + 15A.2/15A.3): nueva página con tabs "API keys" + "Webhooks". API keys: CRUD + dialog post-create que muestra `sk_live_<tenantId>.<secret>` raw una sola vez (revealed-once) + lista con `prefix` + `last_used_at` + revoke + **multiselect de scopes** (5 checkboxes con `invoices:read/write`, `contracts:read`, `customers:read`, `webhooks:trigger`) en el dialog "Nueva API key" con hint "(sin selección = acceso total)" (Fase 15A.3). Webhooks: CRUD con URL + selector multi-evento (`invoice.paid/issued/overdue`, `contract.signed`, `lead.created`) + dialog post-create que muestra el `secret` raw una sola vez + botón "Ver deliveries" por webhook que lleva a `/settings/webhooks/[id]` (Fase 15A.2). Item en sidebar con icono `Plug`.
- **`/settings/webhooks/[id]`** (Fase 15A.2): nueva página dashboard de deliveries de un webhook. Tabla paginada **cursor** (no offset — `webhook_deliveries` puede crecer a 100k+ filas) + filtros (`status: pending|delivered|failed`, `fromDate`, `toDate`) + dialog detalle por delivery con `payload` JSON formateado, `signature` header, `httpStatus`, `responseBody`, `errorMessage`. Botón "Reintentar" sólo visible cuando `status='failed'`; reset `attempts=0` + reencola con un solo click.
- **Botón "Consultar AEAT" en `<VerifactuBadge>`** (Fase 15A.1): visible cuando `aeatStatus` es `pending` o `error`. Lanza `POST /v1/billing/invoices/:id/refresh-aeat-status`, refresca el query y muestra el nuevo status sin recargar la página. Útil para casos críticos donde el operador no quiere esperar al tick del cron (15 min).
- **Layout autenticado**: shadcn sidebar + header con FacilitySwitcher placeholder, UserMenu, ThemeToggle (light/dark/system), TrialBanner.
- **Auth client**: store Zustand (access token en memoria, refresh en cookie httpOnly), `apiFetch` con refresh transparente + deduplicación, hooks TanStack Query por módulo (`auth`, `users`, `invitations`, `two-factor`).
- **i18n** vía next-intl, locale `es-ES` único de momento (todas las claves en `messages/es.json`).

### Estado de tests

- **Database** (`packages/database`, Vitest): **15/15** en 4 suites — uuid_v7, RLS, seed, seed-superadmin.
- **API unit** (`apps/api`, Jest, `pnpm --filter api test`): **123/123** en 17 suites (servicios, guards, providers, builders…; sin BD).
- **API e2e** (`apps/api`, Jest + Supertest, `pnpm --filter api test:e2e --runInBand`): **314/314** en 53 suites (requiere Postgres + Redis + MinIO de docker-compose; `NODE_ENV=test` desactiva el throttler).
- **Smoke E2E** (`apps/web/e2e/`, Playwright): 5/5 (gate bloqueante en CI).
- Helpers comunes en `apps/api/test/helpers/`: `registerVerifiedUser`, `waitForEmail`, `extractToken`, `cleanupTestTenants`, `generateTotpCode`. En tests el throttler aplica `skipIf: () => true`.
- Verificado en la última sesión (2026-06-21): typecheck monorepo 8/8 + lint + las 4 suites verdes tras subir `nestjs-zod` a v5.

### Stack y dependencias instaladas

- **Backend**: NestJS 11, Prisma 6, PostgreSQL 16, nestjs-pino, nestjs-zod, passport-jwt, @nestjs/throttler, @node-rs/argon2, otpauth, nodemailer, React Email, **handlebars** (templating Fase 5), **@nestjs/event-emitter** (Fase 5), Resend (HTTP API).
- **Frontend**: Next.js 15 (App Router) + React 19, Tailwind v3, shadcn/ui (button, input, label, form, card, dropdown-menu, sheet, sidebar, separator, skeleton, avatar, badge, alert, sonner, checkbox, tooltip, table, dialog, select, tabs), next-intl, next-themes, TanStack Query, Zustand, react-hook-form + @hookform/resolvers, qrcode.react.

### Notas operativas

- `main` protegida con gate de CI; cambios vía PR (sin `develop` hasta que tengamos staging). Ver flujo en `README.md`.
- Runtime Node 20.18.1 vía fnm (`.nvmrc`). Siempre `eval "$(fnm env --use-on-cd --version-file-strategy=recursive)"` antes de pnpm si se ejecuta desde scripts no-interactivos.
- `pnpm db:seed` crea un tenant demo `demo-storage` con owner `jota@storageos.local` / `Jota69` (configurable en `packages/database/.env`).
- Editor: el usuario usa **Google Antigravity IDE**, no VSCode. No generar config `.vscode/`.
- **Git lo hace el usuario a mano**. No hacer commit / push / branch automático ni configurar Husky / pre-commit hooks sin pedir antes.

### Próximo paso

**El MVP está cerrado, hardenizado, emite facturas F1/F2 conformes a AEAT, soporta rectificativas por diferencias o sustitución, fuerza 2FA al owner/manager, registra todas las acciones del super admin, separa el worker BullMQ del API en producción via `ENABLE_WORKERS_IN_API=false`, expone una spec OpenAPI versionada en `/v1/`, corre smoke tests Playwright en CI, permite a integradores externos consumir eventos vía API keys + webhooks HMAC y anonimiza tenants completos (RGPD).** Tras Fase 16 el código queda sin TODOs de peso ni tests en `describe.skip`. **Deuda técnica del bloque 2 cerrada (2026-06-21, PRs #14 + #15, ya en `main`):** `RolesGuard`/`@Roles` retirados (cadena de guards `Throttler → JwtAuth → Permissions`), gating de UI por permiso (`<Can>`) en las páginas principales, y `nestjs-zod` subido a v5 con `cleanupOpenApiDoc` (arreglo de raíz del Swagger en dev, sin paliativos). No hay fase técnica obligatoria pendiente para vender en España. **El despliegue VPS ya está hecho (2026-06-16, ver bloque de despliegue arriba): la app está viva en producción sobre un dominio de prueba.** La siguiente prioridad: **onboarding del primer cliente real con el dominio definitivo** — todo por variables de Portainer + redeploy, sin tocar código: configurar Resend (DNS SPF/DKIM verificado), Stripe live + webhook, subir cert FNMT del cliente, cambiar `AEAT_MODE=sandbox`→`production` para el dry-run, y apuntar Uptime Kuma a `/health/ready` y `/health/worker`. **Historial de migraciones reparado (2026-06-11):** las migraciones de Fases 11-14 tenían timestamps `202605200xxxxx` que ordenaban ANTES que `phase4_billing`/`phase10a`, cuyas tablas alteran, rompiendo cualquier BD nueva (shadow DB, CI, primer `migrate deploy` del VPS). Fix aplicado: los 7 directorios renombrados a `202605290xxxxx` (orden relativo preservado), `migration_name` actualizado en `_prisma_migrations` de la BD dev, y borradas 3 filas rolled-back históricas con checksums de versiones antiguas de los ficheros. Verificado: **`migrate deploy` aplica las 40 migraciones limpias en una BD desde cero** (el path del VPS está sano). ⚠️ `migrate dev` funciona mecánicamente pero SIEMPRE detectará un drift esperado y NUNCA hay que aceptarlo: propone `DROP COLUMN reservations.time_range` (columna generada `tstzrange` del EXCLUDE anti-overbooking) y `DROP DEFAULT` en `units.area_m2/volume_m3` (columnas generadas) porque Prisma no puede representarlas en `schema.prisma` — viven solo en SQL manual. Convención para migraciones nuevas: seguir escribiéndolas a mano (`prisma/migrations/<timestamp>_<nombre>/migration.sql` con timestamp posterior a la última) y aplicarlas con `pnpm --filter @storageos/database migrate:deploy`; si se usa `migrate dev --create-only`, revisar el SQL generado y BORRAR las sentencias del drift antes de aplicar. **Opcionales menores cerrados (2026-06-12):** (1) `invitation_token_invalid` instrumentado en `InvitationsService` (`findByToken` + `accept` registran en `security_events` con ip/ua — última traza de seguridad pendiente); (2) anonimización RGPD del tenant ampliada a `leads` (nombre/email/teléfono/mensaje/metadata + soft delete) y `communications` (recipient/subject/cuerpos/variables purgados) dentro de la misma `$transaction`; (3) **dinero sin floats**: helpers `apps/api/src/common/money.ts` (`toCents/addAmounts/subtractAmounts/isAtLeast/isGreaterThan`, aritmética en céntimos enteros) reemplazan los 11 epsilons `0.001` en `payments.service.ts` e `invoices.service.ts` — spec `money.spec.ts`; (4) **heartbeat de workers**: `WorkersHeartbeatCron` (en `QueuesModule` con spread `WORKERS_ENABLED_IN_API` — vive ahí y NO en HealthModule porque el worker no carga HealthModule; las constantes de cola se extrajeron a `queue-names.ts` sin imports para evitar el ciclo queues.module→cron→queues.module que dejaba `@InjectQueue(undefined)`) escribe `workers:heartbeat` en Redis cada minuto (TTL 3 min, primer latido en `onApplicationBootstrap`); `GET /health/worker` devuelve 503 `worker_stale` si falta — Uptime Kuma debe monitorizarlo; (5) **visibilidad de colas**: `GET /admin/queues` (AdminGuard) con counts por cola + últimos 10 failed (motivo/intentos/fecha) + página `/admin/queues` en el panel super admin (refresco 15s, item sidebar `Layers`). **Tras el despliegue se cerraron los Planes 1-4 del backlog de valor añadido (ver bloque arriba + `docs/ROADMAP.md`): importador CSV, WhatsApp real, move-in self-service + firma electrónica, y pagos Holded + Redsys.** Backlog restante (menor prioridad) en `docs/ROADMAP.md`: control de accesos físico real (MQTT/PTI/Noke), GoCardless, conciliación bancaria + informes fiscales (303/347), multi-idioma EN/CA, app móvil PWA, asistente IA para staff, A3/Sage.

**Observabilidad — Loki + Grafana + Promtail (2026-06-17):** stack autohospedado opcional (perfil compose `observability`, apagado por defecto) en `observability/` + servicios en `docker-compose.prod.yml` y `docker-compose.portainer.yml`. Promtail descubre contenedores por el socket de Docker y etiqueta logs por servicio (`api`/`worker`/`web`), parseando pino JSON (extrae `level`) y las violaciones CSP. Grafana provisiona datasources (Loki + Postgres), el dashboard **StorageOS — Overview** (volumen de logs, errores API/worker, violaciones CSP, login fallidos sobre `security_events`, top emails/IP) y **alertas por email** (pico CSP > 50/5m, pico errores API/worker > 20/5m; el brute-force ya lo cubre `SecurityAlertsService`). El endpoint `/api/csp-report` emite ahora JSON estructurado (`evt=csp_violation`, con dedup TTL en memoria) en vez de texto libre, para que Loki lo grafique/alerte. Variables nuevas (solo si se activa el perfil): `GF_ADMIN_PASSWORD`, `GF_SERVER_ROOT_URL`, `GF_ALERT_EMAIL` (cae a `SECURITY_ALERT_EMAIL`), `GF_SMTP_*`; el datasource Postgres reutiliza `POSTGRES_*`. Activar: `COMPOSE_PROFILES=observability` (Portainer) o `--profile observability`. Detalle en `docs/DEPLOYMENT.md §13.1` + `observability/README.md`.

- **Modo oscuro en el panel admin (2026-06-25):** el panel super admin no tenía control de tema. Ahora tiene toggle claro/oscuro (como el panel normal) y **arranca en oscuro por defecto**. El `ThemeProvider` (next-themes) pasa de **global** (`providers.tsx`) a **por sección**: los grupos de rutas `(app)` y `admin` son hermanos que nunca coexisten → vivir el provider por sección evita el conflicto de doble provider y residuos al navegar. `providers.tsx` deja de envolver en `ThemeProvider` (mantiene QueryClient + Toaster, que usa tema `system`); `(app)/layout.tsx` con `defaultTheme=system` (clave por defecto → conserva la preferencia ya guardada del panel normal); `admin/layout.tsx` con `defaultTheme=dark` + `storageKey` separada (`storageos-admin-theme`) + `<ThemeToggle>` en el header (envuelve también el login del admin). Frontend-only. Verificado: typecheck 8/8 + lint 8/8 + build web 74/74.

- **Historial de pagos de la suscripción del tenant (panel admin, 2026-06-26):** en el detalle del tenant (`/admin/tenants/[id]`) hay una card **«Historial de pagos»** con los pagos de su suscripción SaaS (lo que la empresa nos paga). **Persistido en BD, desacoplado del gateway** (decisión: no consultar Stripe en vivo, para soportar futuros métodos de pago / pagos manuales). Migración `20260625140000_tenant_subscription_payments` (RLS): `provider` (stripe/manual/...), `external_id` (idempotencia, índice único parcial), `status`, `amount`/`currency`, `plan_slug`/`plan_name` (snapshot), `period_start`/`period_end` (periodo cubierto → se ve si pagó anual por adelantado), `paid_at`, `invoice_url`/`pdf_url`. `BillingSaasService`: `recordStripeInvoiceFromWebhook` (upsert por external_id, resuelve tenant por `customer` — en Stripe SDK v22 `invoice.subscription` ya no está tipado), `syncSaasPaymentsFromStripe` (backfill `stripe.invoices.list`, no-op si no hay `stripeCustomerId`), `listSaasPayments`. El webhook `invoice.payment_succeeded` ahora **persiste** el pago (antes solo logueaba). Endpoints admin `GET/POST /admin/tenants/:id/saas-payments` (AdminModule importa BillingSaasModule). Frontend: `SaasPaymentsCard` + hooks `useAdminTenantSaasPayments`/`useSyncTenantSaasPayments`. **Vacío hasta configurar Stripe live** (correcto en onboarding). typecheck 8/8 + lint 8/8.

- **Histórico de conversaciones con el tenant (panel admin, 2026-06-26):** réplica de las interacciones manuales del inquilino (#84) pero a nivel plataforma — el super admin registra las **conversaciones con el tenant** (llamada/email/reunión/WhatsApp/nota) en el detalle del tenant. **El autor es un super admin**, no un `user`. Migración `20260626120000_tenant_interactions` (RLS por coherencia/defensa, aunque solo la consume el admin vía `PrismaAdminService` que bypassa RLS, como `tenant_subscription_payments`): `tenant_id` (FK→tenants CASCADE), `super_admin_id` (FK→super_admins **SET NULL**), `type`, `content`, `occurred_at`. Modelo Prisma `TenantInteraction` + relaciones inversas en `Tenant` y `SuperAdmin` (`@relation("TenantInteractionAuthor")`). Shared: `TenantInteractionTypeEnum` (`note/call/email/meeting/whatsapp/other`) + `CreateTenantInteractionSchema` + `TenantInteractionDto` (`authorName` desde `superAdmin.fullName`) en `packages/shared/src/admin`. Backend: `AdminTenantInteractionsService` (en el módulo admin) + endpoints en `AdminTenantsController` (bajo `AdminGuard`): `GET/POST /admin/tenants/:id/interactions` (autor = `@CurrentSuperAdmin('sub')`) + `DELETE /admin/tenants/:id/interactions/:interactionId`; create/delete dejan rastro en `super_admin_audit_logs` (`admin.tenant.interaction_created`/`_deleted`). Frontend: **el detalle del tenant pasa de cards apiladas a 3 pestañas** (`Tabs` shadcn) — **General** (las 3 cards: datos/suscripción/uso), **Pagos** (`SaasPaymentsCard`) y **Conversaciones** (`TenantInteractionsCard` nueva: form tipo+contenido + timeline con icono/autor/fecha + borrar). Hooks `useAdminTenantInteractions`/`useCreateTenantInteraction`/`useDeleteTenantInteraction`. e2e `admin-tenant-interactions` 1/1 (crear→listar→borrar + aislamiento por tenant + 404 cross + 401 sin token). typecheck 8/8 + lint 8/8 + build web + e2e admin 8/8 + db 15/15.

- **Pago manual de la suscripción del tenant + extensión de periodo (panel admin, 2026-06-26):** el super admin puede **registrar a mano** un pago de la suscripción SaaS con su **origen** (Stripe / PayPal / Efectivo / Transferencia / Otros) y, a todos los efectos, **es un pago más**: extiende el periodo de la suscripción igual que un cobro de Stripe. Clave de diseño: hoy el `current_period_end` lo mueve el webhook `customer.subscription.updated` de Stripe (no el registro del pago); para un pago manual no hay webhook, así que **el propio registro extiende el periodo**. Migración `20260626130000_saas_payment_discount` (aditiva): `tenant_subscription_payments.discount` (Decimal nullable, descuento informativo sobre el precio de lista); el campo `provider` ya era texto libre (acepta paypal/cash/bank_transfer/other). Shared: `SaasPaymentProviderEnum` (`stripe/paypal/cash/bank_transfer/other`) + `CreateManualSaasPaymentSchema` (`provider`, `amount>0`, `discount?`, `currency` default EUR, `durationMonths` 1-36, `paidAt?`, `description?`) + `TenantSubscriptionPaymentDto.discount`. Backend: `BillingSaasService.recordManualPayment` (en una `$transaction`: crea el pago `status='paid'` con `periodStart=max(currentPeriodEnd, ahora)` y `periodEnd=base + durationMonths` vía helper `addMonths` con ajuste de fin de mes, **y** actualiza `tenant_subscription.currentPeriodEnd=newEnd` + `status='active'`); se factorizó el mapeo fila→DTO en `toPaymentDto` (incluye `discount`), reusado por `listSaasPayments`. Endpoint admin `POST /admin/tenants/:id/saas-payments/manual` (`AdminGuard`, 201, auditado `admin.tenant.saas_payment_manual_created`). Frontend: botón **«Añadir pago manual»** en `SaasPaymentsCard` → diálogo (origen, importe, descuento, **duración en meses propuesta = importe ÷ precio mensual del plan, editable**, fecha, nota) + columnas **Origen** y descuento en la tabla; hooks `useAddManualSaasPayment` (invalida los pagos **y** el detalle del tenant, porque cambia el periodo) + `useAddManualPaymentDeps` (cruza `useAdminTenant` × `useAdminSubscriptionPlans` para sugerir la duración). e2e `admin-saas-manual-payment` (transferencia 2 meses → periodo extendido + `status active` + en historial; 2º en efectivo con descuento extiende desde el nuevo fin; importe negativo 400; sin token 401). **Convivencia Stripe + manual (acumulador permanente):** para que un pago manual **no se pise** con el siguiente cobro de Stripe (cuyo webhook fija un `current_period_end` absoluto en su propia línea temporal), el crédito manual se **suma** en vez de reemplazarse. Migración `20260626140000_subscription_manual_extension`: `tenant_subscriptions.manual_extension_days` (Int default 0, acumulador permanente). `recordManualPayment` incrementa el acumulador con los días que aporta (`diffInDays(base, newEnd)`); `syncSubscriptionFromStripe` calcula el periodo efectivo como **`fecha_de_Stripe + manual_extension_days`** (helper `addDays`). Así un pago manual de 1 mes + un cobro de Stripe de 3 = 4 meses (el +1 manual persiste en cada renovación). El diálogo muestra un **aviso** si el tenant ya se cobra por Stripe (`hasStripe`, de `stripeSubscriptionId`): "se sumará por encima, no la pisa". e2e cubre el acumulador (pago manual → webhook simulado vía `app.get(BillingSaasService)` → periodo = Stripe + días manuales; el acumulador no se toca en el webhook). typecheck 8/8 + lint + build web + e2e admin 9/9.

- **Desglose de usuarios del tenant (panel admin, 2026-06-26):** en el detalle del tenant (`/admin/tenants/[id]`, pestaña General), el número de **Usuarios** de la card «Uso» es ahora **clicable** → abre un diálogo con el **listado de usuarios** del tenant y sus datos relevantes para soporte. Solo lectura, **sin migración**. Shared: `AdminTenantUserDto` (`fullName`, `email`, `phone`, `role`, `tenantRoleName` —rol custom—, `isActive`, `emailVerified`, `twoFactorEnabled`, `facilitiesCount` —locales asignados; 0 = ve todos—, `lastLoginAt`, `createdAt`). Backend: `AdminTenantsService.listUsers(tenantId)` (vía `PrismaAdminService`, include `tenantRole` + `_count.facilities`, orden activos primero) + endpoint `GET /admin/tenants/:id/users` (`AdminGuard`, 404 si el tenant no existe). Frontend: `<TenantUsersDialog>` (lista con nombre/email/teléfono + badge de rol + chips email verificado/2FA/locales + último acceso/alta) cargado on-demand (`useAdminTenantUsers(id, enabled=open)`); el componente `Stat` gana un `onClick` opcional que lo renderiza como botón (valor en azul, hover underline). e2e `admin-tenant-users` 1/1 (lista incluye al owner con role=owner/emailVerified/facilitiesCount=0; 401 sin token; 404 tenant inexistente). **(2026-06-26)** Cerrado el follow-up: **Inquilinos** también es clicable → `<TenantCustomersDialog>` con el listado (nombre completo o razón social según `customerType` individual/business, email/teléfono, documento, KYC, nº de contratos total + vigentes, alta). `AdminTenantsService.listCustomers` (vía `PrismaAdminService`, `_count.contracts` + `groupBy` de contratos `active/ending` por `customerId`) + endpoint `GET /admin/tenants/:id/customers` + hook `useAdminTenantCustomers`. e2e `admin-tenant-users` 4/4. Con esto los **4** datos de la card «Uso» (Usuarios, Locales, Inquilinos, Contratos) son clicables. typecheck 8/8 + lint + build web.

- **Acciones de soporte sobre el tenant y sus usuarios (panel admin, 2026-06-26):** el super admin puede **editar** datos del tenant y **operar** sobre sus usuarios, cross-tenant. **Sin migración.** `AdminSupportService` (módulo admin; inyecta `PrismaAdminService` + `AuditService` + `SuperAdminAuditService` + `AuthService` + `SessionsService` —`AuthModule` ya exporta los 3 últimos—); cada acción audita en `audit_logs` del tenant **y** `super_admin_audit_logs` (helper `trace`). **Editar tenant**: `AdminUpdateTenantSchema` (name, billingEmail nullable, country, currency, timezone, taxId nullable; al menos un campo) → `PATCH /admin/tenants/:id` (devuelve el detalle actualizado); el `AdminTenantDto` gana `timezone` + `taxId` (aditivo, para precargar el form). **Acciones sobre un usuario** (`POST /admin/tenants/:id/users/:userId/<accion>`): `resend-verification` (reusa `AuthService.resendVerification`; 400 `already_verified`), `password-reset` (reusa `AuthService.forgotPassword`), `revoke-sessions` (`SessionsService.revokeAllForUser` → `{revoked}`), `disable-2fa` (Prisma directo: limpia `twoFactor*` + borra `recovery_codes`; 400 `twofactor_not_enabled` — no se reusa `TwoFactorService.disable` porque exige contraseña + TOTP del propio usuario), `deactivate`/`reactivate` (`isActive` + al desactivar cierra sesiones; guard **400 `last_owner`** si es el único propietario activo). Frontend: botón **«Editar»** en el header del detalle → `<TenantEditDialog>` (react-hook-form + zod, precargado); en `<TenantUsersDialog>` cada usuario tiene un **menú de acciones** (`DropdownMenu`) contextual (reenviar verificación solo si no verificado, quitar 2FA solo si activo, desactivar/reactivar según estado) con confirmación en las destructivas. Hooks `useUpdateTenant` + `useTenantUserAction(tenantId)` (un solo hook parametrizado por acción; invalida users + detalle). e2e `admin-support` 1/1 (editar tenant; resend ya-verificado 400; reset 200; revoke-sessions 200; disable-2fa no-activo 400; desactivar único owner 400 `last_owner`; 401). typecheck 8/8 + lint + build web + e2e admin 8/8.

- **Status page / salud del sistema + acciones de colas (panel admin, 2026-06-26):** la página `/admin/queues` (renombrada a **«Sistema y colas»**) gana una sección de **salud de la infraestructura** y **acciones** sobre las colas. **Sin migración.** Backend: `AdminSystemController` (`GET /admin/system-health`, `AdminGuard`) comprueba **Postgres** (`PrismaAdminService.$queryRaw SELECT 1`), **Redis** (`PING` sobre la conexión ioredis de la cola BullMQ `billing`), **MinIO** (`FilesService.ping()` nuevo → `HeadBucket` del bucket uploads) y **worker** (lee `workers:heartbeat` de Redis, TTL 3 min) → `AdminSystemHealthDto { checkedAt, services: [{key, label, status: up|down, detail, latencyMs}] }` (helper `timed` mide latencia y captura el error como `detail`). Acciones en `AdminQueuesController`: `POST /admin/queues/:name/retry-failed` (`getFailed` + `job.retry()` → `{retried}`) y `POST /admin/queues/:name/clean-failed` (`queue.clean(0,1000,'failed')` → `{cleaned}`); 404 `queue_not_found`. Las colas BullMQ y `FilesService` (global) ya eran inyectables desde `AdminModule` (`AdminQueuesController` ya las usa). Shared: `AdminSystemServiceDto` + `AdminSystemHealthDto`. Frontend: en `/admin/queues`, cards de salud (icono ✓/✗ + badge Operativo/Caído + latencia + detalle, refresco 15s) + en la tabla de colas una columna **Acciones** con **Reintentar** / **Limpiar** (solo si hay fallidos, con confirmación). Hooks `useAdminSystemHealth` + `useQueueFailedAction('retry-failed'|'clean-failed')`. e2e `admin-system` 2/2 (health con `database` up + las 4 claves; retry/clean 200; cola inexistente 404; 401). typecheck 8/8 + lint + build web. **2ª de las 4 mejoras de admin** (siguientes: tenants en riesgo, comunicación).

- **Tenants en riesgo / retención (panel admin, 2026-06-26):** nueva página `/admin/at-risk` (item de nav **«En riesgo»**, icono `AlertTriangle`) que identifica los tenants que conviene atender, agrupados por motivo. **Sin migración.** Backend: `AdminTenantsService.getAtRisk()` (cross-tenant) → `AdminAtRiskDto { trialExpiring, pastDue, inactive }` con `AdminAtRiskTenantDto { id, name, slug, status, planName, reason, since, detail }`. Cálculo: **trialExpiring** = tenants `trial` con `trialEndsAt` en ≤7 días (orden asc); **pastDue** = tenants con `subscription.status='past_due'`; **inactive** = tenants `active` cuyo **`_max(users.lastLoginAt)`** (groupBy users por tenantId) es < hoy−14 días o `null` (nunca han accedido). Endpoint `GET /admin/tenants/at-risk` **declarado antes de `@Get(':id')`** para no colisionar con el ParseUUIDPipe. Frontend: 3 cards (Trials por expirar / Pago fallido / Inactivos) con contador, motivo + fecha relevante y botón **«Ver tenant»** → `/admin/tenants/[id]` (donde están las acciones: extender trial, cambiar plan, conversaciones, soporte). Hook `useAdminAtRisk` (refresco 60s). El item de nav «Colas» se renombra a **«Sistema»** (coherencia con la página «Sistema y colas»). e2e `admin-at-risk` 1/1 (un trial a +3 días aparece en `trialExpiring` con `reason='trial_expiring'`; 401). typecheck 8/8 + lint + build web. **3ª de las 4 mejoras** (queda: comunicación con tenants).

- **Comunicación con tenants: email directo + anuncios/broadcast (panel admin, 2026-06-26):** el super admin puede enviar **un email a un tenant** y **anuncios masivos** a varios tenants. **Sin migración.** `AdminCommsService` (módulo admin; inyecta `PrismaAdminService` + `EmailService` + `SuperAdminAuditService`; `AdminModule` ahora importa `EmailModule`). Destinatarios de un tenant (`recipientsFor`) = **owners verificados activos** (emails únicos en minúsculas); fallback al `billingEmail`. Render: `escapeHtml(body)` + `\n→<br>` en un `<div>` inline + `text` plano. **Email directo**: `emailTenant` (síncrono, ≤ pocos owners) → 400 `no_recipients` si no hay destino, 404 si el tenant no existe. **Broadcast**: `broadcast({audience, subject, body})` con `audience` = `active`|`trial`|`all` (=active+trial; excluye suspended/cancelled/deleted); resuelve los destinatarios de TODOS los tenants en **2 queries** (tenants del público + sus owners en bloque, agrupados en JS — no 2·N) y **encola un job por email en la cola BullMQ `email`** (`addBulk`): no bloquea el request y BullMQ aporta reintentos; devuelve `{tenants, recipients, failed:0}`. Ambas acciones auditan en `super_admin_audit_logs` (`admin.tenant.email_sent` / `admin.broadcast_sent`). **Cola `email` con consumidor**: `QUEUE_EMAIL` existía sin `@Processor`; se añade **`EmailSendProcessor`** (`@Processor(QUEUE_EMAIL)`, job `email.send` → `EmailService.sendRendered`) en `EmailModule`, gateado por `WORKERS_ENABLED_IN_API` (lo procesa el **worker** en prod, el API en dev/test; el worker ya importa `EmailModule`). `AdminCommsService` inyecta `@InjectQueue(QUEUE_EMAIL)`. Shared: `AdminEmailTenantSchema` + `AdminBroadcastSchema` (+ `AdminBroadcastAudienceEnum`) + result DTOs. `AdminCommsController` (`@Controller('admin')`): `POST /admin/tenants/:id/email` + `POST /admin/announcements`. Frontend: botón **«Email»** en el header del detalle del tenant → `<TenantEmailDialog>` (asunto + mensaje); nueva página **`/admin/announcements`** (item de nav «Anuncios», icono `Megaphone`) con selector de público + asunto + mensaje + confirmación. Hooks `useEmailTenant(id)` + `useAdminBroadcast`. e2e `admin-comms` 1/1 (email directo al owner verificado `recipients≥1`; asunto corto 400; tenant inexistente 404; broadcast a trials encola `tenants≥1`/`recipients≥1`; 401). **Gotcha del test**: el broadcast encola muchos jobs en una BD local con tenants residuales → el spec **pausa la cola `email`** (`queue.pause()`) y la **obliterate** en el teardown para que el worker no la drene (cierre lento). typecheck 8/8 + lint + build web + e2e admin 10/10. **4ª y última de las mejoras de admin pedidas** (soporte, salud del sistema, tenants en riesgo, comunicación).

- **📦 Portal del inquilino — contratar/solicitar trastero adicional + accesos nocturnos (2026-07-01, PRs #173/#175/#177):** el inquilino existente no tenía por dónde conseguir OTRO trastero (el «cambio de trastero» es para _cambiar_ el actual; el booking público `/book/[slug]` es para _clientes nuevos_). **(1) Solicitud (#173):** migración `20260701120000` `unit_requests` (RLS; tenant + customer + facility/unit_type/unit opcionales + note + status pending/handled/rejected). `UnitRequestsModule`: `availableForCustomer` (trasteros `available` de los locales donde el inquilino tiene contrato **active/ending**), `createFromPortal` (notifica al staff vía `NotificationsService`), `list`/`resolve`. Portal `GET /portal/me/available-units` + `GET/POST /portal/me/unit-requests`; staff `GET /unit-requests` (+`pending-count` para el badge) + `PATCH :id`. Pestaña «Contratar trastero» en el portal + página staff `/unit-requests` (Operativa, badge) + shared `unit-requests`. **(2) Self-service con pago (#175):** `SignaturesService.bookForExistingCustomer` (valida trastero disponible + del mismo local → `ContractsService.create` userId null → `sign` con `deferAccessUntilPaid` + firma **typed** → `maybeIssueFirstInvoice` → `{contractId, invoiceId, portalToken}`); `POST /portal/me/contracts` (`@Public`+sesión+throttle). `MoveInModule` **exporta** `SignaturesService`; `PortalModule` lo importa. El botón «Solicitar» de un trastero disponible pasa a **«Contratar ahora»** → diálogo (nombre/firma + aceptar condiciones) → recarga facturas + va a la pestaña «Facturas» a pagar (flujo de pago existente); el **acceso se emite al pagar** la 1ª factura. **Modelo de cobro**: cada trastero = 1 contrato = **1 factura recurrente mensual** independiente (la facturación recurrente itera por contrato); el inquilino paga todas en «Facturas». **(3) Accesos nocturnos (#177):** en la pestaña Accesos, el inquilino ve el **horario de acceso de su local** (toque de queda del `PortalFacilityDto.accessCurfew*`: «puedes entrar de X a Y; de Y a X necesitas un pase nocturno») para decidir si compra el pase, y un **historial de pases comprados** (`NightPassService.history` = credenciales `metadata.source='night_pass'` del customer → estado `active`/`used`/`expired` derivado de `usesCount`/`expiresAt`/`status`) vía `GET /portal/me/access/night-passes`; componente `NightAccessCard`. **Recordatorio del sistema de accesos**: el **pase nocturno** (código single-use de pago que salta el toque de queda, caduca 08:00) se **activa** en Accesos → credenciales (toggle `nightPassEnabled` + precio); el **toque de queda** se configura por local en la ficha del local (Ajustes); **acceso 24h** permanente = casilla `bypassCurfew` al crear credencial. e2e `unit-requests`, `portal-self-booking`, `portal-night-passes`. **Gotcha**: `SignaturesService.bookForExistingCustomer` necesita pasar `cancellationNoticeDays` en `CreateContractInput` (el default del schema lo hace requerido en el tipo de salida). **Fix (#172):** la página `/admin/impersonation` (super admin) solo mostraba el log; se añadió un banner «para entrar como un tenant, abre su ficha → Impersonar» + CTA a `/admin/tenants` (el flujo de impersonación ya existía en `/admin/tenants/[id]`).

- **🐛 CAUSA RAÍZ del 404 en `/access/logs` en prod — `.dockerignore **/logs` (2026-07-01, PR #180):** la página «Registro de accesos» (`/access/logs`) daba **404 solo en producción** (en local cargaba). **NO era cache, ni despliegue, ni versiones web/API distintas** (varios diagnósticos equivocados por el camino). La causa: el `.dockerignore` tenía el patrón **`**/logs`**, que excluye del contexto de build **cualquier carpeta llamada `logs`** — incluida la ruta de UI `apps/web/src/app/(app)/access/logs`. Al construir la imagen del `web`, el `COPY` **nunca copiaba esa carpeta** → Next construía **sin** la ruta → `/access/logs` 404 (el `web` literalmente no la tenía), mientras `/renewals`, `/today`, etc. (no se llaman `logs`) sí. **En local no hay `.dockerignore`**, por eso ahí siempre funcionó. El log del navegador lo confirmó: `GET https://<dominio-web>/access/logs?_rsc=… 404` (el `_rsc` = React Server Component de Next → el 404 lo da el **web**, no el API). **Fix**: quitar `**/logs` (los archivos `.log` los cubre `**/*.log`; no hay directorio de logs de runtime en el repo) + comentario de aviso. **Gotcha permanente para el `.dockerignore`**: patrones amplios como `**/logs`, `**/build`, `**/out`, `**/dist` pueden comerse **rutas de código** con esos nombres → 404 SOLO en prod, imposible de reproducir en local; usar rutas concretas para directorios de runtime. **Descartado**: se llegó a preparar un pipeline GHCR (imágenes en CI, compose con `image:`) creyendo que era el cache del `COPY` de Portainer; se **cerró (#178)** al encontrar la causa real — el fix es este one-liner, no hace falta GHCR.

- **🛠️ Mejoras de valor del panel super admin (2026-06-30, PRs #159–#170):** 7 mejoras tras analizar el panel super admin (ya muy completo), cada una en su PR con e2e, implementadas como **pila stacked**. (1) **Feature flags / overrides por tenant** — migración `20260630220000` `tenant_feature_overrides` (RLS; tenant + feature + enabled, único por tenant+feature); helper `effectiveFeatures(planSlug, overrides)` = plan ∪ enabled \ disabled; **el `FeatureGuard` Y `AuthService.me` aplican los overrides** (no solo el plan); `AdminTenantsService.getFeatures/setFeatures` + `GET/PUT /admin/tenants/:id/features` (solo guarda los overrides que difieren del plan; audita en `audit_logs` + `super_admin_audit_logs`); card «Features premium» en la pestaña General del detalle del tenant. **Palanca comercial**: activar el asistente IA de cortesía un mes, betas, o conservar una feature legacy tras un downgrade, sin cambiar el plan. (2) **Checklist de puesta a punto** — `AdminTenantsService.getOnboarding` + `GET /admin/tenants/:id/onboarding` (deriva de counts/flags: email del propietario verificado, primer local/trastero/inquilino/contrato, marca del portal, Veri\*Factu); card «Puesta a punto» con barra de progreso. Sin migración. (3) **Gestión de super admins (CRUD)** — `AdminSuperAdminsController` `GET/POST/PATCH :id/active` (`SuperAdminService.list/create` ya existían; nuevo `setActive` con guards: no a sí mismo, no desactivar al último `superadmin` → revoca sus sesiones); **solo el rol `superadmin` gestiona** (support solo lee); `SuperAdminDto` gana `twoFactorEnabled`; página `/admin/super-admins`. (4) **Export CSV** — helper común `apps/web/src/lib/csv.ts` (`;` + BOM `String.fromCharCode(0xfeff)`) + botón «Exportar CSV» en `/admin/metrics` (serie mensual altas/bajas/cobrado), `/admin/audit-logs` y `/admin/security-events` (lo cargado). Frontend-only. (5) **Alertas proactivas de plataforma** — migración `20260630240000` `platform_alert_settings` (singleton: enabled, alertEmail, notifyPastDue, notifyTrialExpiring, trialExpiringDays); `PlatformAlertsService.evaluateAndNotify` envía un **digest por email** (vía `EmailProvider`) con tenants en `past_due` + trials por expirar; **cron diario 07:00 SIN gatear** (corre en el API, ligero) + `GET/PUT /admin/platform-alerts` + `POST /run` («Evaluar ahora»); página `/admin/platform-alerts`. (6) **Auditoría de impersonación** — `AdminImpersonationAuditService.listSessions/getActivity` + `GET /admin/impersonation-logs` (+`?tenantId`) y `:id/activity` (cruza `impersonation_logs` con los `audit_logs` del tenant en la ventana `[createdAt, revokedAt ?? expiresAt]`); página `/admin/impersonation` (sesiones desplegables con su actividad). Sin migración. **Limitación**: la ventana captura toda la actividad del tenant, no solo la del admin (marcar cada acción con el impersonador = follow-up). (7) **Gestión de planes desde la UI** — el CRUD (`POST/PATCH/DELETE /subscription-plans` con `AdminGuard`) ya existía sin UI; `SubscriptionPlanDto` ampliado (aditivo) con `priceYearly` + `maxUnits/maxFacilities/maxUsers`; página `/admin/plans` (tabla + crear/editar/desactivar precios y límites). **Las features por plan siguen en código** (`PLAN_FEATURES` por slug); esta UI gestiona precio/límites/visibilidad (data-driven = follow-up). e2e por feature: `admin-feature-overrides`, `admin-onboarding`, `admin-super-admins`, `admin-platform-alerts`, `admin-impersonation-audit`, `admin-plans-crud`. **Gotchas**: (a) **pila stacked** — al mergear cada PR su rama base se borra → GitHub **cierra** el PR stacked siguiente; hay que rebasar la rama a `main` (`git rebase origin/main` quita los commits ya mergeados) y **crear un PR nuevo** (no se puede reabrir uno cerrado con base borrada). (b) Varios `toDto` construyen `SuperAdminDto`/`SubscriptionPlanDto` a mano (login, 2FA challenge, billing-saas) → al añadir un campo al DTO hay que actualizarlos todos o el typecheck falla. (c) BOM del CSV con `String.fromCharCode(0xfeff)` (no literal, por ESLint `no-irregular-whitespace`). (d) `ActionMeta` del `AdminTenantsService` exige `reason` → `setFeatures` pasa `reason: 'feature_override'`. (e) e2e API con OOM intermitente → `gh run rerun --failed`.

- **🧰 Mejoras de valor del portal del inquilino y del panel del tenant (2026-06-30, PRs #144–#156):** dos tandas de análisis→propuestas→implementación, cada feature en su PR con e2e. **(I) Portal del inquilino (3, PRs #144/#147/#146):** (1) **pantalla de inicio** `/portal/consume` pestaña «Inicio» (`overview-card.tsx`, frontend-only): saldo pendiente total + próximo vencimiento (o «Estás al día»), avisos accionables (facturas vencidas, mensajes sin leer, baja en curso) y accesos rápidos; el `Tabs` pasa a **controlado** (`tab` state) para navegar desde el resumen. (2) **subir documentos / KYC**: el inquilino sube/ve sus documentos (DNI, justificante…) — `GET /portal/me/documents`, `POST /portal/me/documents/upload-url` (presigned PUT a MinIO), `POST /portal/me/documents`; reusa `CustomerDocumentsService` (relajado a `userId: null`) **provisto en `PortalModule`** (NO importar `CustomersModule`: reintroduce el `JwtModule` de `AuthModule` y rompe la verificación del token del portal). Card «Mis documentos» en la pestaña «Mis datos». (3) **historial de accesos**: `AccessCredentialsService.listLogsForCustomer` + `GET /portal/me/access-logs` (DTO slim `PortalAccessLogDto`, sin attemptedValue/IP) → card «Historial de accesos» en la pestaña Accesos. **(II) Panel del tenant (8, PRs #149–#156):** (1) **«Hoy»** — bandeja operativa (`DashboardModule` + `GET /dashboard/today`: tareas vencidas, contratos que vencen en 30 d, reservas que expiran en 7 d, facturas vencidas, incidencias, cambios, mensajes; página `/today` + item del menú). (2) **búsqueda global** — `SearchModule` + `GET /search?q=` (inquilinos/contratos/trasteros/facturas, ≥2 chars, RLS) + componente `GlobalSearch` en el header con **⌘K/Ctrl+K**. (3) **calendario operativo** — `CalendarModule` + `GET /calendar?from=&to=` (tareas/mantenimientos/fin de contrato/fin de reserva, máx 92 d) + página `/calendar` (grid mensual con date math, eventos por color). (4) **seguimientos/recordatorios CRM** — migración `20260630200000` `customer_followups` (RLS; inquilino + autor staff + due_date + status); `FollowupsModule` (`GET /followups` bandeja, `GET/POST /customers/:id/followups`, `PATCH/DELETE /followups/:id`); página `/followups` + pestaña «Seguimientos» en la ficha del cliente + item sidebar. (5) **página de notificaciones** `/notifications` (histórico completo; reusa el `NotificationsModule`; enlace «Ver todas» en el `NotificationBell`). (6) **acciones rápidas** en el dashboard (`QuickActions`: Nuevo contrato/inquilino/factura/tarea, gated por permiso). (7) **registro de actividad** — `AuditLogModule` + `GET /audit-logs?cursor=` (`settings:manage`, cursor) + página `/settings/audit` (los `audit_logs` ya se escribían pero el tenant no los veía). (8) **renovaciones** — `ContractsService.listRenewals` + `GET /contracts/renewals` (active/ending con `endDate` en 60 d, antes de `@Get(':id')`) + página `/renewals` (palanca de retención). e2e por feature: `dashboard-today`, `search`, `calendar`, `followups`, `audit-log`, `contract-renewals` (+ portal `portal-documents`, `portal-access-logs`). **Gotchas de la tanda**: (a) los PRs paralelos que tocan `packages/shared/src/index.ts`, `app.module.ts` y `app-sidebar.tsx` dan **conflictos triviales** al mergear en cascada (combinar imports/exports/items; las zonas de los distintos grupos del sidebar no se solapan → git suele auto-resolver); (b) al insertar endpoints en `PortalController` **cuidar el `@Public()`** del endpoint siguiente — un `@Public()` mal colocado dejó `/portal/me/incidents` bajo el `JwtAuthGuard` → 401 (lo cazó el e2e `portal-incidents`, el flujo del navegador no tiene cobertura); (c) el e2e API (100+ suites `--runInBand`) da **OOM** (`heap out of memory`) intermitente con varios suites cayendo a la vez → re-ejecutar el job (`gh run rerun --failed`), no es regresión.

- **🔔 Badges de pendientes en los menús + Soporte tenant↔admin enlazado a conversaciones (2026-06-30):** tanda de "novedades de un vistazo" en ambos paneles + cierre del canal de soporte. **Patrón común**: endpoint de count ligero + hook con `refetchInterval` 60 s gated por permiso + badge en el item/pestaña; los badges de "sin leer" se limpian al abrir, los de "estado" (pendientes/abiertas) bajan al resolver. **(A) Fixes y pestañas del portal**: el portal del inquilino reorganizado en **8 pestañas** (`Tabs`: Contratos · Cambio de trastero · Facturas · Accesos · Mensajes · Incidencias · Tu local · Mis datos; «Cambiar de trastero» pasó a pestaña propia); fix de "se queda cargando" en el primer acceso (`useEffect` dep `[]` porque el `replaceState` del token cambiaba `useSearchParams` y cancelaba la carga a media); fix del 500 al añadir IBAN/tarjeta sin Stripe configurado → `payment-methods.service` lanza **503 `payments_not_configured`** (mensaje claro) si `STRIPE_SECRET_KEY` es el placeholder `sk_test_dummy`. **(B) Badges en el menú del staff**: `GET /unit-change-requests/pending-count` (cambios de trastero `pending`, badge azul en «Cambios de trastero»); `GET /incidents/pending-counts` → `{reported, investigating}` (dos contadores en «Incidencias»: 🔴 reportadas, 🟠 en investigación); `GET /customer-messages/unread-summary` → `{total, byCustomer}` (un fetch alimenta **3 sitios**: 🔵 total en el menú «Inquilinos», por fila en la lista de clientes, y la pestaña «Mensajes» de la ficha; el chat-tab invalida el resumen al abrir → marca leídos → baja). **(C) Badges en el portal del inquilino**: `GET /portal/me/messages/unread-count` (respuestas del staff sin leer, se limpia al abrir Mensajes vía `Tabs onValueChange`) + counts derivados del estado ya cargado (solicitudes `pending` en «Cambio de trastero», incidencias abiertas en «Incidencias»). **(D) Soporte tenant↔super admin** (los tickets ya existían en `/support` ↔ `/admin/support` pero `/support` **no estaba en el menú** del tenant): item **«Soporte»** (`LifeBuoy`) en el footer del menú del tenant + badge azul `SupportTicketsService.countWaitingForTenant` (tickets `waiting_user` = el admin respondió, te toca; `GET /support/tickets/waiting-count`); y en el **panel admin**, badge en el nav «Soporte» con `countOpenForAdmin` (tickets `open` cross-tenant = la pelota está en el admin; `GET /admin/support/tickets/open-count`). El state machine de tickets ya alterna `open`⇄`waiting_user` según quién responde, así que cada badge cuenta "lo tuyo". **(E) Registro automático en el histórico de conversaciones del tenant** (panel admin, automatiza lo que antes era manual): migración `20260630180000` añade `tenant_interactions.link` (TEXT nullable) + nuevo tipo `support` en `TenantInteractionTypeEnum`; `AdminTenantInteractionsService.create` acepta `link`. Cuando el super admin **envía un email** a un tenant (`AdminCommsService.emailTenant`) se crea una interacción `type:'email'` (`content`=asunto+cuerpo, autor=super admin). Cuando un tenant **abre un ticket** (`SupportTicketsService.createForTenant`) se crea una interacción `type:'support'`, `superAdminId:null`, `link:/admin/support/<id>` → la card de conversaciones la muestra con icono `LifeBuoy` + enlace **«Ver y gestionar el ticket →»**. Ambos best-effort (no rompen el envío/creación si el registro falla). **(F) Header del portal limpio**: `PublicHeader` oculta los CTA «Iniciar sesión»/«Crear cuenta» del staff cuando `pathname.startsWith('/portal')` (el inquilino entra por magic link, no los necesita). e2e: `unit-change-requests`, `portal-incidents`, `customer-chat` (+ `unread-count`/`unread-summary`), `support-waiting-count`, `support-ticket-interaction`, `admin-comms` (email→interacción). PRs #130–#142. **Gotcha**: tras tocar el enum de shared, **rebuild `@storageos/shared`** o el typecheck del web falla (`Record` exige la clave nueva); estos badges no llevan migración salvo `tenant_interactions.link`.

- **🐛 Fix de raíz — `apiFetch` se comía los headers del caller → el portal del inquilino daba 401 (2026-06-30):** **el portal del inquilino, los pagos desde el portal (Redsys/GoCardless) y la página pública de firma NUNCA funcionaron en navegador.** `apiFetch` (`apps/web/src/lib/auth/api.ts`) construye su `init` con `method`/`body`/`signal` pero **no copia `options.headers`**; `executeFetch` leía los headers de `init.headers` (siempre vacío) en vez de `options.headers`. El portal manda el token del inquilino como **header manual** (`headers: { Authorization: 'Bearer <portalToken>' }` + `requiresAuth: false`, porque NO usa el auth store del staff) → ese header **se perdía** → todas las `/portal/me/*` iban **sin `Authorization`** → **401**. Síntoma exacto: el `POST /portal/login/consume` (sin auth) devolvía la sesión, pero **todas** las llamadas siguientes daban 401 (en incógnito y siempre). **Los e2e NO lo detectaban** porque usan `supertest` (mandan el header directo), no `apiFetch` → **el flujo real del navegador del portal no tenía cobertura**. Fix (1 línea): en `executeFetch`, `const headers = new Headers(options.headers)` (antes `init.headers`). Arregla de golpe portal + Redsys/GoCardless del portal + `/sign/[token]`. **Gotcha permanente**: cualquier `apiFetch(path, { headers: {...} })` (header manual, p. ej. flujos `@Public` con token de portal) depende de esto; el panel staff usa el token del store vía `requiresAuth` (no headers manuales), por eso nunca se notó. typecheck 8/8 + lint + build web. **Nota**: los fixes previos de esta tanda (sesión persistente + 48 h, logout) eran correctos pero atacaban un síntoma equivocado (single-use/recarga); el 401 de fondo era este.

- **Fix + sesión del portal de 48 h (2026-06-30):** **bug**: el portal del inquilino guardaba el `accessToken` **solo en memoria** y el magic link es **single-use** (GETDEL en Redis) → al **recargar** (o en un segundo render), el `useEffect` reintentaba consumir el token **ya gastado** de la URL → **401 en todos los `/portal/me/*`** (síntoma reportado: «falta credencial» tras abrir un magic link del staff). **Fix (frontend)**: la sesión del portal se **persiste en `localStorage`** (`storageos.portal.session`, con `expiresAtMs` = ahora + `expiresIn`) tras consumir; al cargar, si hay sesión guardada **válida** se **restaura** (no se reconsume), y tras el primer consumo se **limpia el token de la URL** (`history.replaceState` → `/portal/consume`). Guard `useRef` (una sola ejecución por montaje, evita el doble-consumo). Si el server rechaza la sesión guardada → se descarta (`clearStoredPortalSession`) + mensaje «Enlace inválido o caducado. Pide uno nuevo a tu gestor.». **Backend**: la sesión del portal pasa de **30 min → 48 h** (`PORTAL_SESSION_TTL_SECONDS` en `consumeMagicLink`); con `localStorage` (sobrevive al cierre del navegador) el inquilino puede **volver a entrar durante 48 h** reabriendo el enlace o `/portal/consume` sin pedir otro. El **enlace** sigue siendo single-use y caduca en **7 días** para el primer acceso (no cambia). typecheck 8/8 + lint + build web. **Nota seguridad**: sesión de 48 h en `localStorage` accesible por JS — aceptable para el bajo riesgo del portal; sin botón de logout en v1 (caduca sola a las 48 h). El `sessionStorage` de `gc_portal_mandate` (flujo GoCardless) es independiente y se mantiene.

- **Magic link del portal generado por el staff (2026-06-30):** el staff genera desde la ficha del inquilino un **enlace de acceso al portal** y lo copia para repartirlo a mano (WhatsApp, SMS…) — pensado para inquilinos mayores que no saben pedir el enlace por email. **Sin migración.** `PortalService.createMagicLinkForCustomer(tenantId, customerId)` reutiliza la mecánica del magic link (tokenId hex + secret base64url + hash argon2id en Redis, single-use vía GETDEL) pero **devuelve la URL** en vez de enviarla por email, con **TTL largo `STAFF_MAGIC_LINK_TTL_SECONDS` = 7 días** (vs 30 min del flujo por email — lo abren cuando pueden); `storeMagicLink` se parametrizó con `ttlSeconds`. **No requiere email del customer** (el token guarda `{customerId, tenantId}`; el email solo se usaba para enviar). Endpoint **autenticado** (no `@Public`): `PortalLinkController` (`@Controller('customers/:customerId/portal-link')`, en `PortalModule` para reusar `PortalService` sin ciclos) `POST` → `PortalMagicLinkDto {url, expiresAt}` (`@RequirePermission('customers:write')`). Frontend: botón **«Enlace de acceso al portal»** (`portal-link-button.tsx`) en el header de la ficha del cliente (`/customers/[id]`, gated `<Can permission="customers:write">`) → diálogo con la URL (input readonly + copiar al portapapeles + caducidad; «de un solo uso»). Hook `useCreatePortalLink`. e2e `staff-portal-link` 2/2 (genera → URL `/portal/consume?token=` → el inquilino la consume (200, `customerId`) → replay 401 single-use → **audit log `portal.magic_link_generated` presente**; cliente inexistente 404, sin auth 401). typecheck 8/8 + lint + build web + regresión `portal-transparency` 2/2. **Nota**: el `consume` del portal devuelve **200** (no 201). **Auditado**: `createMagicLinkForCustomer` inyecta `AuditService` (de `AuthModule`, que `PortalModule` ya importa) y escribe `audit_logs` action `portal.magic_link_generated` (entityType `Customer`, `changes:{expiresAt}`, **sin** token/secreto, con ip/ua del staff); el controller pasa `user.sub` + `{ipAddress: req.ip, userAgent}`.

- **Centro de ayuda / FAQ (2026-06-30):** el staff define **preguntas frecuentes** que el inquilino consulta en su portal (autoservicio que reduce consultas). Migración `20260630160000_faq_entries`: tabla `faq_entries` (RLS; `question`, `answer`, `position`, `is_published`). `FaqModule`: `FaqService` con `withTenant` (sirve staff + portal) — `list` (todas, staff), `listPublished` (solo `is_published`, portal), `create`/`update`/`remove`. Staff: `GET`/`POST /faq-entries` + `PATCH`/`DELETE /faq-entries/:id` (`settings:read` GET / `settings:manage` mutaciones). Portal: `GET /portal/me/faq` (`@Public` + sesión de portal → publicadas) — `PortalModule` importa `FaqModule`. Shared `faq/index.ts`: `FaqEntryDto` + `CreateFaqEntrySchema`/`UpdateFaqEntrySchema`. Frontend: pestaña **«Centro de ayuda»** en `/settings/faq` (alta pregunta+respuesta con `Textarea` + lista con ocultar/publicar `Eye`/`EyeOff` + borrar; gated `settings:manage`, i18n `settings.nav.faq`) + card **«Centro de ayuda»** (`faq-card.tsx`) en `/portal/consume` (**acordeón** `useState`, no muestra la card si no hay FAQs). e2e `faq` 2/2 (crea publicada + oculta → staff ve 2, inquilino ve 1 → publicar la oculta → inquilino ve 2 → borrar → ve 1; 401 portal/staff). typecheck 8/8 + lint + build web + db 15/15. **Decisión v1**: sin categorías (lista por `position`), sin búsqueda. Cierra el follow-up «FAQ/centro de ayuda» — **el portal del inquilino queda completo** (8 mejoras + chat + FAQ).

- **Chat bidireccional inquilino ↔ staff (2026-06-30):** comunicación en tiempo (casi) real entre el inquilino (portal) y el staff (panel) — antes solo había envíos salientes (`communications`) e incidencias. Migración `20260630140000_customer_messages`: tabla `customer_messages` (RLS; `customer_id`, `sender_type` customer/staff, `sender_user_id?` FK→users SET NULL, `body`, `read_at?`). **Un hilo por cliente.** `CustomerMessagesModule` (importa `NotificationsModule` + `PushModule`, exporta el service): `CustomerMessagesService` con `withTenant` (RLS, sirve a staff **y** portal pasando el `tenantId`) — `list(tenantId, customerId, viewer)` (marca leídos los del lado contrario con `updateMany` + devuelve el hilo asc), `sendFromStaff` (crea `staff` + **push al inquilino** vía `PushService.sendToCustomer`), `sendFromCustomer` (crea `customer` + **notificación in-app al staff** vía `NotificationsService.create` type `customer.message` → `/customers/<id>`). Staff: `GET`/`POST /customers/:customerId/messages` (`customers:read`/`customers:write`). Portal: `GET`/`POST /portal/me/messages` (`@Public` + sesión de portal). Shared `CustomerMessageDto` + `SendCustomerMessageSchema` (en `customers`). Frontend: card **«Mensajes con tu gestor»** (`chat-card.tsx`) en `/portal/consume` (burbujas, sondeo 20 s, Enter envía) + pestaña **«Mensajes»** (`chat-tab.tsx`) en la ficha del cliente (`refetchInterval` 20 s; burbujas del staff a la derecha). e2e `customer-chat` 2/2 (inquilino escribe → `senderType:customer`/`senderName:null` → staff lo ve (len 1) → responde `senderType:staff`/`senderName` set → el inquilino ve los 2, el del staff con `readAt` set al listar; 401 portal y staff). typecheck 8/8 + lint + build web + db 15/15. **Decisión v1**: un hilo por cliente (sin múltiples conversaciones/asuntos), sin adjuntos, polling en vez de websockets; el aviso es notificación in-app (staff) + push (inquilino, si activó). Cierra el follow-up «comunicación bidireccional» del portal.

- **Portal del inquilino — white-label / marca del operador (2026-06-30):** el portal del inquilino muestra el **logo y color de marca del operador** (no "StorageOS"/genérico). Migración `20260630120000_portal_branding`: `tenants` += `portal_brand_color` (hex) + `portal_logo_url` (TEXT, nullable). `PortalSessionDto` gana `brandColor`/`logoUrl` (aditivo), rellenados en `consumeMagicLink` desde el tenant. Config staff: `TenantSettingsService.getBranding`/`updateBranding` + endpoints `GET /settings/tenant/branding` (`settings:read`) / `PATCH` (`settings:manage`, audit `tenant.branding.settings_changed`); `''` = borrar. Shared `UpdateTenantBrandingSchema` (color valida `^#[0-9a-fA-F]{6}$`, logo `url()`) + `TenantBrandingResponse`. Frontend: (1) pestaña **«Marca del portal»** en `/settings/branding` (color picker nativo + hex + URL de logo + vista previa; `useTenantBranding`/`useUpdateTenantBranding`); (2) el header de `/portal/consume` muestra el **logo + nombre del operador** y una barra de acento con el `brandColor` (el email pasa bajo «Hola, X»). **Decisión v1**: logo por **URL pública** (el operador la pega), no upload; el color aplica como acento del header (no se reescribe el tema `--primary`); el `/portal/login` queda genérico (no conoce el tenant hasta consumir el enlace). e2e `portal-branding` 2/2 (sin marca → `{null,null}` → configurar `#ff6600`+logo → el `consume` del portal devuelve `brandColor`/`logoUrl` → `''` borra el color sin tocar el logo; color no hex → 400). typecheck 8/8 + lint + build web. **Mejora #8 del portal — cierra las 8 mejoras del portal del inquilino** (transparencia, perfil, seguro, tienda, white-label). Follow-ups: upload del logo al bucket público, branding pre-login, aplicar el color al tema completo.

- **Portal del inquilino — tienda de accesorios (2026-06-30):** el inquilino **compra candados/cajas/accesorios** desde el portal; la compra genera una **factura emitida** que paga en «Tus facturas» (flujo de pago existente). **Sin migración.** Reutiliza `ProductSalesService.create`, cuya firma se **relaja a `userId: string | null`** (flujo público, como `invoices`/`contracts`; todos los usos del `userId` —`soldByUserId`, `invoices.create/issue`, `audit`— ya aceptan null). `ProductsModule` ahora **exporta** `ProductsService` + `ProductSalesService`; `PortalModule` lo importa. `PortalService` inyecta ambos: `listProducts` (productos `isActive` del tenant con `totalStock > 0`) + `purchaseProducts` (resuelve el **local del contrato activo** del inquilino → 404 `no_active_contract` si no tiene → `productSales.create({userId: null, input: {facilityId, customerId, items}})`; la venta crea+emite la factura F1 con Veri\*Factu y decrementa stock atómico). Endpoints `GET /portal/me/products` + `POST /portal/me/purchases {items:[{productId, quantity}]}` (`@Public` + sesión de portal). Shared `PortalPurchaseSchema`; reutiliza `ProductDto`/`ProductSaleDto`. Frontend: card **«Tienda»** (`shop-card.tsx`) en `/portal/consume` — lista productos (precio **con IVA** = `price × (1+taxRate/100)`, stock) + cantidad + «Comprar» → recarga facturas y stock. e2e `portal-shop` 3/3 (producto+stock+contrato → portal lista → compra 2× (`total 24.2` = 2×10×1.21, `invoiceId` set, factura visible en `/portal/me/invoices`, stock 5→3); sin contrato activo → 404 `no_active_contract`; 401). typecheck 8/8 + lint + build web. **Mejora #7 del portal**; queda white-label (#8).

- **Portal del inquilino — seguro/protección self-service (2026-06-30):** el inquilino ve los planes de protección que ofrece el negocio y **contrata/quita el seguro** en sus contratos desde el portal (palanca de ingresos recurrentes; `InsuranceModule` ya existía para staff). **Sin migración.** `PortalService` inyecta `ContractsService`: `listInsurancePlans` (planes `isActive` del tenant, vía `PrismaAdminService`) + `setMyContractInsurance` (valida que el contrato es del customer → 404 si no → delega en `ContractsService.setInsurance`, que congela el snapshot del precio + gating por plan `assertInsuranceFeature`; devuelve la lista de `PortalContractDto` actualizada). Endpoints `GET /portal/me/insurance-plans` + `PUT /portal/me/contracts/:id/insurance {planId|null}` (`@Public` + sesión de portal). `PortalContractDto` gana `insurancePlanId` (aditivo, para preseleccionar). Shared `PortalSetInsuranceSchema`; reutiliza `InsurancePlanDto`. Frontend: card **«Protección de contenido»** (`insurance-card.tsx`) en `/portal/consume` — lista los planes (precio/cobertura/descripción) + por cada contrato activo un `Select` (Sin protección / planes) que aplica al instante y recarga los contratos. La prima la factura la recurrente como una línea más (mecánica existente). e2e `portal-insurance` 2/2 (plan staff + contrato firmado → portal lista el plan → contratar (`insurancePlanId`/`insurancePlanName`/`insurancePrice:5`) → quitar (`insurancePlanId:null`); contrato ajeno → 404). typecheck 8/8 + lint + build web. **Mejora #6 del portal**; siguientes: tienda de accesorios (#7), white-label (#8).

- **Portal del inquilino — editar perfil y datos de facturación (2026-06-30):** el inquilino actualiza desde el portal sus datos de contacto y fiscales. **Sin migración.** `PortalService.getMyProfile`/`updateMyProfile` (vía `PrismaAdminService`, scoped a tenant+customer del JWT de portal): edita `firstName`/`lastName`/`companyName`, `phone`, `address`/`city`/`postalCode`/`country`, `documentType`/`documentNumber`; **el email NO se edita** (es el identificador del magic-link; lo cambia el staff). `''` = borrar (null), campo ausente = no tocar (spreads condicionales por `exactOptionalPropertyTypes`); `country` se normaliza a mayúsculas. Endpoints `GET`/`PATCH /portal/me/profile` (`@Public` + sesión de portal). Shared `PortalProfileDto` + `PortalUpdateProfileSchema`. Frontend: card **«Mis datos»** (`profile-card.tsx`, fichero aparte) en `/portal/consume` — form por tipo de cliente (razón social para business / nombre+apellidos para individual), email deshabilitado, dirección + datos fiscales. e2e `portal-profile` 2/2 (lee perfil → actualiza contacto+fiscales, `country` 'es'→'ES', email inmutable → `phone:''` lo borra sin tocar el resto; 401 sin sesión). typecheck 8/8 + lint + build web. **Mejora #5 del portal** (de la lista de 8); siguientes: seguro self-service, tienda de accesorios, white-label.

- **Portal del inquilino — transparencia y autoservicio (2026-06-30):** mejoras rápidas de valor (exponer datos que ya existían). **Sin migración.** (1) **Contrato enriquecido**: `PortalContractDto` gana `depositAmount`/`depositStatus`, `discountAmount`/`freeMonthsRemaining`, `insurancePlanName`/`insurancePrice` y `hasSignedPdf` — `ContractsService.toPortalDto` + `listForCustomer`/`requestEndByCustomer` añaden `insurancePlan` al include. (2) **Descargar contrato firmado**: `PortalService.getMyContractPdf` → `FilesService.presignFromPublicUrl('uploads', signedPdfUrl)` (nuevo helper: el `signed_pdf_url` se guardó como URL pública de un bucket **privado**, así que se reconstruye la key y se firma una GET temporal de 5 min; 404 `signed_pdf_not_available` si no hay) → `GET /portal/me/contracts/:id/signed-pdf` → `{url}`. (3) **Historial de pagos**: `listMyPayments` (transacciones `payment` del customer + nº de factura) → `GET /portal/me/payments`. (4) **Datos del local**: `listMyFacilities` (locales de sus contratos activos: dirección, CP, contacto, toque de queda) → `GET /portal/me/facilities`. `PortalService` inyecta `FilesService`. Shared `PortalPaymentDto`/`PortalFacilityDto`/`PortalDownloadDto`. Frontend (`/portal/consume`): la card «Mis contratos» muestra fianza (estado), seguro, descuento/meses gratis y botón **«Descargar contrato firmado»**; cards nuevas **«Tu local»** (dirección + horario de acceso + contacto) e **«Historial de pagos»** (importe/método/estado/fecha); cargas best-effort. e2e `portal-transparency` 2/2 (contrato con `depositAmount:100`/`hasSignedPdf:false` + local con `accessCurfewEnabled` + pagos array + `signed-pdf` 404 `signed_pdf_not_available`; 401 sin sesión) + regresión `move-out` 2/2. typecheck 8/8 + lint + build web. **Origen**: análisis del portal (24 endpoints, 9 cards) → estos eran los huecos de "datos que ya existen, solo exponerlos". Pendientes mayores: editar perfil/datos de facturación, seguro self-service, tienda de accesorios, white-label.

- **Seguimientos / recordatorios sobre tenants (panel admin, 2026-06-29):** CRM ligero — el super admin crea **tareas con fecha de recordatorio** sobre un tenant ("llamar para renovación el día X") y las marca hechas. Migración `20260629180000_tenant_followups`: tabla `tenant_followups` (FK→tenants CASCADE + super_admins SET NULL; `title`, `note?`, `due_date` DATE, `status` pending/done, `completed_at`). Réplica del patrón `tenant_interactions` (autor = super admin, vía `PrismaAdminService`). `AdminTenantFollowupsService`: `listForTenant` (pendientes primero), **`listPending`** (bandeja global de todos los tenants por `due_date`), `create`, `setStatus` (done → fija `completedAt`; reabrir → null), `remove`. Endpoints: en `AdminTenantsController` `GET`/`POST /admin/tenants/:id/followups` (crear audita `admin.tenant.followup_created`); controller nuevo `AdminFollowupsController` (`@Controller('admin/followups')`) `GET /` (bandeja) + `PATCH /:id {status}` + `DELETE /:id`. Shared `CreateTenantFollowupSchema` (`title`, `note?`, `dueDate` YYYY-MM-DD) + `UpdateTenantFollowupSchema` + `TenantFollowupStatusEnum` + `TenantFollowupDto` (con `tenantName`/`tenantSlug` para la bandeja). Frontend: página **`/admin/followups`** (item nav «Seguimientos», icono `CalendarClock`): bandeja de pendientes con badge de vencimiento (vencido rojo / hoy ámbar) + marcar hecho + borrar; **pestaña «Seguimientos»** nueva en el detalle del tenant (`TenantFollowupsCard`: form título+fecha+nota, default fecha hoy+7d, lista con hecho/reabrir/borrar). Hooks `useAdminFollowupsPending`/`useAdminTenantFollowups`/`useCreateFollowup`/`useUpdateFollowup`/`useDeleteFollowup`. e2e `admin-followups` 2/2 (crear → aparece en lista del tenant + bandeja → marcar hecho (`completedAt` set, sale de la bandeja) → borrar 204; 401). typecheck 8/8 + lint + build web + regresión admin-tenant-interactions 1/1 + db 15/15. **Última de las mejoras de admin propuestas** (health score, MRR movements, adopción/upsell, cohortes de retención, seguimientos).

- **Cohortes de retención (panel admin, 2026-06-29):** matriz de retención de tenants por mes de alta en `/admin/metrics`. **Sin migración** (solo lectura). `AdminMetricsService.getRetention(months)` → `AdminRetentionDto`: agrupa los tenants (no borrados) por su mes de alta (`createdAt`) y, por cada offset de mes, calcula el % que seguía **vivo** (`status` ≠ `cancelled`/`suspended`; la fecha de baja se aproxima por `updatedAt`, mismo criterio que el resto de métricas). **M0 = 100% por construcción** (`vivo en K = churnMonth == null || churnMonth >= alta+K`); offsets futuros para una cohorte = `null`. `maxOffset` = meses desde la cohorte más antigua. Endpoint `GET /admin/metrics/retention?months=` (`AdminGuard`, clamp 1-24). Shared `AdminRetentionDto`/`AdminRetentionCohortDto`. Frontend: card **«Cohortes de retención»** en `/admin/metrics` (`RetentionCohortsCard`): **heatmap** (filas = cohortes, columnas M0..Mn, celdas coloreadas verde→rojo por % + "Altas"). Hook `useAdminRetention`. e2e `admin-retention` 3/3 (estructura + M0 de la cohorte actual = 100; un tenant con alta hace 2 meses y baja hace 1 (vía SQL raw para sortear el `@updatedAt`) → su cohorte da `[100, 100, 0]`; 401). typecheck 8/8 + lint + build web + regresión admin-metrics/mrr-movements 4/4. **Limitación**: el churn por `updatedAt` es aproximado (consistente con `cancellationsThisMonth`); para precisión exacta harían falta snapshots de estado (follow-up).

- **Adopción / upsell por tenant (panel admin, 2026-06-29):** vista `/admin/adoption` — por cada tenant, qué **features premium** usa de verdad + **uso vs límites** del plan + **candidato a upgrade**. **Sin migración** (solo lectura/agregación cross-tenant). `AdminTenantsService.getAdoption()` → `AdminAdoptionDto`: señal de uso por feature (existencia de filas/flag) — `ai_assistant`=`aiConversation`, `sepa`=`sepaSettings.enabled`, `bank_reconciliation`=`bankStatement`, `rent_increases`=`rentIncrease`, `insurance`=`insurancePlan` activos, `access_control`=`accessCredential`/`accessDevice`, `automations`=`automationRule` activos (todo `groupBy` por tenantId). Conteos `units`/`facilities`(deletedAt null)/`users` vs `plan.maxUnits/maxFacilities/maxUsers`. **Plan recomendado** = el plan **activo más barato** (`featuresForPlan(slug)`) que cubre **todas** las features en uso **y** los límites; **candidato** si ese plan es más caro que el actual (engloba "usa feature fuera del plan" Y "topa límites"). `featureAdoption` = por feature, nº de tenants que la usan sobre los que la tienen incluida. Endpoint `GET /admin/tenants/adoption` (**antes de `@Get(':id')`**, `AdminGuard`). Shared `AdminAdoptionDto`/`AdminTenantAdoptionDto`/`AdminTenantAdoptionFeatureDto`/`AdminFeatureAdoptionDto` (importan `TenantFeature` de `../features`). Frontend: página `/admin/adoption` (item nav «Adopción», icono `TrendingUp`): barras de adopción por feature + KPI de candidatos + filtro "solo candidatos" + lista de tenants (chips de features usadas — **ámbar = fuera de su plan**, verde = en plan; uso vs límites en rojo si topa; badge «Subir a {plan}» + «Ver tenant» → `/admin/tenants/[id]` donde está el change-plan). Hook `useAdminAdoption`. e2e `admin-adoption` 3/3 (estructura 7 features; tenant `starter` con `aiConversation` → `isCandidate`/`recommendedPlanSlug:'pro'`/`usesFeatureOutsidePlan`/feature ai_assistant `used:true,inPlan:false`; 401). typecheck 8/8 + lint + build web + regresión admin (health/at-risk/tenant-users) 8/8.

- **MRR movements (panel admin, 2026-06-29):** desglose mensual del cambio de MRR del SaaS — **new / expansion / reactivation** (suman) vs **contraction / churn** (restan) + **Net New MRR** + **NRR**. **Decisión clave**: calcularlos con fiabilidad necesita el MRR de cada tenant en cada mes pasado, y la suscripción solo guarda el estado actual (el churn por `updatedAt` es ruidoso, la reactivación no es derivable) → se implementan con **snapshots mensuales**. Migración `20260629160000_mrr_snapshots`: tabla `mrr_snapshots` (1 fila por tenant y mes: `month` DATE, `plan_slug`, `status`, `mrr`; único `(tenant_id, month)`). `MrrModule` (módulo ligero sin controllers, en `admin/`, importado por `AdminModule` **y el worker**): `MrrSnapshotService` — **`captureMonth(monthStart)`** (foto del estado actual: 1 fila por sub `active`, MRR = `plan.priceMonthly`; upsert idempotente), **`backfillFromPayments(months)`** (reconstruye meses pasados desde `tenant_subscription_payments` — cada pago `planSlug`+`periodStart..periodEnd` implica MRR active en los meses que cubre; solo crea los que faltan → da histórico inmediato cuando hay cobros), **`getMovements(months)`** (asegura el mes en curso + backfill, carga snapshots de `months+1` meses, compara mes a mes por tenant manteniendo un `everSeen` para distinguir **new** de **reactivation**; `nrr = (base − churn − contraction + expansion)/base`). **Cron** `MrrSnapshotCron` (`0 3 1 * *`, gated `WORKERS_ENABLED_IN_API`) captura el mes en curso; el endpoint también lo asegura on-demand. Endpoint `GET /admin/metrics/mrr-movements?months=` (`AdminGuard`). Shared `AdminMetricsMrrMovementMonthDto`/`AdminMetricsMrrMovementsDto` (con `warmingUp` = aún sin histórico). Frontend: card **«Movimientos de MRR»** en `/admin/metrics` (Recharts `ComposedChart`: barras apiladas con signo verde arriba/rojo abajo + línea Net New + KPIs Net/NRR/expansión/churn; mensaje de "calentando" si `warmingUp`). El **worker importa `MrrModule`** para el cron (verificado: `worker-bootstrap` cierra el grafo DI). e2e `admin-mrr-movements` 3/3 (estructura; inserta 2 snapshots del mismo tenant 30→50 → detecta expansión 20; 401). typecheck 8/8 + lint + build web + regresión admin-metrics 1/1 + worker-bootstrap 2/2 + db 15/15. **Nota operativa**: sin Stripe live aún no hay pagos SaaS, así que los movimientos arrancan `warmingUp` y se poblarán con los cierres mensuales (o con el backfill cuando haya cobros).

- **Health score por tenant (panel admin, 2026-06-29):** ranking de salud de cada tenant para priorizar customer success / retención — mejora el "en riesgo" binario con un **score 0-100 gradual + nivel**. **Sin migración** (solo lectura/agregación cross-tenant vía `PrismaAdminService`). `AdminTenantsService.getTenantsHealth()` (todos los tenants `active`/`trial`, ordenados de menor score = más urgente) + `getTenantHealth(tenantId)` (404 si no existe) + helper puro **`scoreTenant`**: 4 factores ponderados — **engagement 35%** (recencia del último login del equipo: ≤7d=100…>90d/nunca=0), **facturación 25%** (facturas emitidas en 30d: ≥5=100…0=0), **suscripción 25%** (past_due=10, cancelAtPeriodEnd=20, trial>7d=70/≤7d=40, active=100), **adopción 15%** (contratos activos + locales). Score → **nivel** `healthy`≥75 / `warm`≥50 / `at_risk`≥25 / `dormant`<25; cada factor devuelve `{key,label,score,weight,detail}` (desglose legible). Señales con `groupBy` cross-tenant (users `_max(lastLoginAt)`, invoices/contracts/facilities `_count` por tenantId). Endpoints `GET /admin/tenants/health` (lista; **declarado antes de `@Get(':id')`**) + `GET /admin/tenants/:id/health` (`AdminGuard`). Shared `AdminTenantHealthDto`/`AdminTenantHealthFactorDto`/`AdminTenantHealthLevel`. Frontend: nueva página **`/admin/health`** (item de nav «Salud», icono `HeartPulse`; el label evita la palabra "Tenants" para no chocar con el `getByRole('link', {name:'Tenants'})` del smoke test 04) con 4 contadores por nivel clicables (filtro) + búsqueda + lista con barra de score, badge de nivel y desglose de factores; **card «Salud»** en la pestaña General del detalle del tenant (`TenantHealthCard` + `useAdminTenantHealth`, score grande + nivel + 4 factores con barra). Hooks `useAdminTenantsHealth`/`useAdminTenantHealth`. e2e `admin-tenant-health` 3/3 (lista con score 0-100/nivel/4 factores + orden ascendente; detalle + 404 ghost; 401). typecheck 8/8 + lint + build web + regresión admin (at-risk/tenant-users/metrics) 6/6.

- **GoCardless — Fase 3: cobro de facturas + webhook (2026-06-29):** tercera y última fase — **GoCardless queda completo** (config → mandato → cobro). El cobro crea un Payment contra el mandato; queda `processing` (liquidación SEPA en días) y el webhook (`payments.confirmed`) marca la factura pagada. **Sin migración.** `GoCardlessClient.createPayment` (POST /payments `{ links.mandate, amount, currency }` + idempotency; stub `PM-stub-…` con status `pending_submission`). **Multi-gateway sin ciclo**: se extrae `GoCardlessCoreModule` (cliente + settings + el nuevo **`GoCardlessChargeService`**, **sin deps de Payments**); `PaymentsModule` lo importa (unidireccional) y `GoCardlessModule` también (para mandato/settings/webhook) → se rompe el ciclo Payments↔GoCardless sin `forwardRef`. `GoCardlessChargeService.charge(tenantId, mandateId, amount…)` → `ChargeResult` (resuelve el access token del tenant + `createPayment`; mapea estados iniciales a `processing`, `confirmed/paid_out`→`succeeded`, `failed/cancelled`→`failed`; si no está activo o falla la API → `failed`). **`PaymentsService.chargeInvoice`**: **branch por `pm.gateway`** — si `gocardless` llama `goCardlessCharge.charge` (queda `processing`, NO marca la factura), si no el `StripeGateway` de siempre; el resto del flujo (crea `payment`, audit) intacto. Como `chargeInvoice` ramifica solo, el **auto-charge y el botón "Cobrar"** cobran por GoCardless sin cambios. **Webhook** (`GoCardlessWebhookController`, ahora inyecta `PaymentsService`): por cada evento `resource_type:'payments'`, `confirmed`/`paid_out` → `syncFromWebhook(succeeded, paidAt)` (idempotente; marca la factura `paid`), `failed`/`cancelled`/`customer_approval_denied` → `syncFromWebhook(failed)`; **`charged_back`** (devolución SEPA tras un cobro confirmado) → **`syncDisputeFromWebhook`** (revierte el payment `succeeded`→`failed`, resta `amountPaid`, devuelve la factura a `overdue`/`issued`; reusa el mismo flujo idempotente que los disputes de Stripe). unit `payments.service.spec` 14/14 (constructor con el nuevo 5º arg `goCardlessCharge`) + e2e `gocardless` 4/4 (activar+mandato → emitir factura → `charge` → payment `processing`/`gateway:gocardless`/`PM-stub-…` → factura **no** pagada → webhook `payments.confirmed` firmado → factura **`paid`** → webhook `charged_back` → factura revertida + `amountPaid 0`). typecheck 8/8 + lint + build web + regresión unit api 132/132 + portal-payments 4/4 + auto-charge 2/2.

- **GoCardless — Fase 2: mandato vía Billing Request Flow (staff + portal) (2026-06-29):** segunda fase. El staff o el inquilino autorizan una **domiciliación SEPA gestionada por GoCardless** y queda guardada como `PaymentMethod` (`gateway: gocardless`, `type: sepa_debit`). **Sin migración** (el flujo lo lleva el frontend con `sessionStorage`; el `mandate.id` se cifra en `gatewayTokenEncrypted`). `GoCardlessClient` gana los métodos del flujo (`createBillingRequest` POST /billing_requests `mandate_request.scheme=sepa_core` → BRQ; `createBillingRequestFlow` → `authorisation_url`; `getBillingRequest` → status+links; `getMandate`; `getCustomerBankAccount` → últimos 4 del IBAN) + **modo stub** determinista gated por `NODE_ENV=test`/`GOCARDLESS_MODE=stub` (e2e/CI sin red ni credenciales, como `AeatClient`/`AiProvider`). `PaymentMethodsService.registerResolved` (nuevo): registra un PM **ya resuelto** sin pasar por `gateway.getPaymentMethodDetails` (el flujo Stripe no aplica). `GoCardlessMandatesService` (orquestador): `startFlow` (crea BR+flow → URL, `redirect_uri`/`exit_uri` desde `WEB_BASE_URL`) y `completeFlow` (verifica `fulfilled` → `getMandate`+`getCustomerBankAccount` → `registerResolved` con `isDefault:true`; 400 `mandate_not_authorised` si aún no autorizado) + `isEnabled`. Endpoints staff `POST /settings/gocardless/mandate/{start,complete}` (`payments:charge`); portal `POST /portal/me/gocardless/mandate/{start,complete}` + `GET /portal/me/gocardless/enabled` (`@Public`+sesión de portal). `GoCardlessModule` importa `PaymentsModule`; `PortalModule` importa `GoCardlessModule`. **Frontend**: botón "Domiciliar con GoCardless" en la pestaña Pagos del cliente (`payment-methods-tab`, si `gcSettings.enabled`) y en el portal (`consume`, si `/gocardless/enabled`) → guarda `{billingRequestId, customerId/portalToken}` en `sessionStorage` → redirige a la `authorisation_url`; páginas de retorno `/(public)/pay/gocardless/complete` (staff) y `/(public)/portal/gocardless/complete` (portal) que llaman `complete` y registran el PM (envueltas en `<Suspense>` por `useSearchParams`). **Decisión**: sin tabla de flujos — el billing request queda acotado al tenant porque se consulta con SU access token. e2e `gocardless` 3/3 (añade: start sin activar 400 `gocardless_not_enabled` → activar → start → complete → PM SEPA `gateway:gocardless`/`last4:0001`/`isDefault` + aparece en los métodos del cliente). typecheck 8/8 + lint + build web + regresión portal-payments 4/4. **Pendiente Fase 3**: cobro de facturas (Payment GoCardless + factory multi-gateway en `chargeInvoice` + despacho de eventos del webhook → marca factura pagada) + auto-charge.

- **GoCardless — Fase 1: config por tenant + webhook base (2026-06-29):** primera fase de la integración de **GoCardless** (domiciliación SEPA gestionada; alternativa a las remesas DIY pain.008 del `SepaModule`, más barata que Stripe para domiciliar). **Decisión**: por fases (PR1 config+base, PR2 mandato vía Billing Request, PR3 cobro de facturas+auto-charge); mandato autorizable por staff **y** portal; **sin SDK** (`fetch` como Redsys/Holded/Anthropic). Migración `20260629140000_gocardless_settings`: tabla `gocardless_settings` (1:1 por tenant, `access_token_encrypted` + `webhook_secret_encrypted` cifrados AES-256-GCM como Redsys/Holded, `environment` sandbox/live, `enabled`). `GoCardlessModule` (en `payments/gocardless/`, registrado en `app.module`): `GoCardlessClient` (cliente HTTP `fetch` — base URL por environment, `Authorization: Bearer` + `GoCardless-Version: 2015-07-06` + idempotency key; `testConnection` lista `creditors`; **`verifyGoCardlessSignature`** función pura = HMAC-SHA256 hex del raw body, timing-safe), `GoCardlessSettingsService` (get/update/getResolved, patrón `RedsysSettingsService`; activar exige token+secret → 400 `gocardless_credentials_required`), `GoCardlessController` (`/settings/gocardless` GET `settings:read` / PUT+POST test `billing:configure`), `GoCardlessWebhookController` (`POST /webhooks/gocardless/:tenantId` — **la URL lleva el tenantId** porque GoCardless no manda el tenant en el payload y el secret es por tenant; verifica firma, despacho de eventos en fase de cobro). Raw body: `app.use('/webhooks/gocardless', raw(...))` en `main.ts` **y en el test factory** (faltaba; ahora también `/webhooks/stripe`). El enum `PaymentGatewayProvider` ya incluía `gocardless`; `Payment`/`PaymentMethod` ya tienen `gateway`+`gatewayCustomerId`+`mandateReference`. Shared: `UpdateGoCardlessSettingsSchema`+`GoCardlessEnvironmentEnum`+`GoCardlessSettingsDto`/`GoCardlessTestResultDto`. Frontend: card "Domiciliación SEPA — GoCardless" en `/settings/billing` (entorno + token + webhook secret + activar + probar conexión) + hooks `useGoCardlessSettings`/`useUpdate`/`useTest`. **Sin variables de entorno globales** (todo config por tenant). unit `gocardless-signature` 2/2 + e2e `gocardless` 2/2 (config cifrada no devuelve secretos + webhook firma válida 200 / inválida 400 + activar sin credenciales 400). typecheck 8/8 + lint + build web + regresión redsys/portal-payments 8/8. **Pendiente Fase 2/3**: mandato (Billing Request Flow) + factory multi-gateway en `chargeInvoice` + cobro de facturas + despacho de eventos del webhook.

- **Gating por plan: cerrado el hueco del seguro vía contrato (2026-06-29):** los 7 módulos premium ya tenían `@RequireFeature` a nivel de controller, pero la **asignación de seguro** vivía fuera del `InsuranceController` (en `ContractsController`: alta con `insurancePlanId` + `PUT /contracts/:id/insurance`), sin gate — un tenant que perdió la feature `insurance` (downgrade) podía seguir asignando seguros existentes. **Sin migración.** `ContractsService` gana un helper `assertInsuranceFeature(tenantId)` (consulta el plan del tenant con `withTenant` + `featuresForPlan(slug).includes('insurance')` → 403 `feature_not_in_plan`), invocado **solo al ASIGNAR** un plan: en `create` (si `input.insurancePlanId`) y en `setInsurance` (si `planId != null`). **Decisión**: se valida en el service, no con `@RequireFeature` en el controller, para que **quitar** un seguro (`planId: null`) siga permitido tras un downgrade. e2e `insurance` 2/2 (nuevo: starter crea plan+contrato con seguro → downgrade a `free` → asignar/alta con seguro 403, quitar 200). typecheck 8/8 + lint.

- **Promociones `free_months` aplicadas en la facturación (2026-06-29):** cierra el follow-up — las promociones de tipo **`free_months`** se creaban/gestionaban pero `validate`/`apply` las rechazaban con `unsupported_type` y no se facturaban. Ahora se aplican end-to-end. Migración `20260629120000_contract_free_months`: `contracts.promotion_id` (FK→promotions **SET NULL**) + `contracts.free_months_remaining` (Int default 0, contador de meses gratis pendientes). El `discountValue` de una promo `free_months` = **nº de meses** (entero; nuevo refine en `CreatePromotionSchema`). `PromotionsService.validate` ahora devuelve `valid:true` para `free_months` con `discountAmount:0`, `effectivePrice` = precio normal y **`freeMonths`** (campo nuevo en `ValidatePromotionResultDto`); `applyToContractTx` acepta `free_months` y devuelve `{discountAmount:0, freeMonths, promotionId}` (incrementa `usedCount`). `ContractsService.create` guarda `promotionId` + `freeMonthsRemaining`; `ContractDto` gana `freeMonthsRemaining`. **Facturación recurrente** (`BillingJobsService.processGenerateRecurring`): si `contract.freeMonthsRemaining > 0`, la línea de **alquiler sale a 0 €** (descripción "— mes gratis (promoción)"; el seguro/extras se siguen cobrando) y se **decrementa** el contador tras emitir → las primeras N facturas son gratis y luego vuelve al precio normal. **Decisión**: los meses gratis aplican al alquiler, no al seguro; no se amplió el enum `ContractEventType` (la factura a 0 € es la evidencia). Frontend: el wizard (`/contracts/new` paso 3) muestra "N meses gratis: las primeras facturas saldrán a 0 €" al validar el código. e2e `promotions` 3/3 (validate free_months válido + valor no entero 400; contrato con 2 meses → `freeMonthsRemaining:2` → factura recurrente con alquiler 0 € + contador a 1) + regresión contracts/insurance 9/9. typecheck 8/8 + lint + build web + db 15/15.

- **Facturación del tenant en el admin (clic en «Contratos», 2026-06-26):** en la card «Uso» del detalle del tenant, el número de **Contratos** es **clicable** → abre un diálogo con la **facturación del negocio del tenant** (las facturas que el tenant emite a SUS inquilinos; **no** confundir con los pagos de la suscripción SaaS de la pestaña Pagos). Solo lectura, **sin migración**. Shared: `AdminTenantInvoicingDto` (`currency`, `totalInvoiced`, `totalCollected`, `totalPending`, `invoiceCount`, `overdueCount`, `avgInvoice`, `monthly: {label, invoiced, collected}[]` últimos 12 meses) + `AdminTenantInvoicingMonthDto`. Backend: `AdminTenantsService.getInvoicing(tenantId)` (vía `PrismaAdminService`, misma lógica que `AnalyticsService.getMonthlyRevenue` pero cross-tenant: invoices contables —`issued/paid/overdue/refunded/partially_refunded`— por `issueDate`, pagos `succeeded` por `paidAt`; totales con `aggregate`, pendiente = Σtotal − ΣamountPaid − ΣamountRefunded de issued/overdue, ventana UTC de 12 meses) + endpoint `GET /admin/tenants/:id/invoicing` (`AdminGuard`, 404 si no existe). Frontend: `<TenantInvoicingDialog>` (6 KPIs + serie mensual con **barras CSS** facturado/cobrado, sin Recharts) cargado on-demand (`useAdminTenantInvoicing(id, enabled=open)`). e2e `admin-tenant-users` ahora 2/2 (añade: tenant nuevo → totales 0 + 12 meses; 401; 404). typecheck 8/8 + lint + build web.

- **Locales y trasteros del tenant en el admin (clic en «Locales», drill-down, 2026-06-26):** la card «Uso» gana un **4º Stat «Locales»** (`AdminTenantDto.facilityCount` nuevo, vía `_count.facilities` filtrado `deletedAt: null` en list+detail) **clicable** → diálogo **drill-down de dos niveles**: nivel 1 lista los **locales** (nombre, dirección/ciudad, nº de trasteros + ocupados + %), cada uno clicable → nivel 2 lista los **trasteros** de ese local (código, tipo, **m²**, **precio/mes**, estado con badge) + botón «← Locales». Solo lectura, **sin migración**. Shared: `AdminTenantFacilityDto` (`name`, `city`, `address`, `unitCount`, `occupiedCount`) + `AdminTenantUnitDto` (`code`, `unitTypeName`, `areaM2`, `basePriceMonthly`, `status`). Backend: `AdminTenantsService.listFacilities` (facilities `deletedAt:null` + `_count.units` + `groupBy` de units `occupied` por `facilityId`) + `listUnits(tenantId, facilityId)` (valida que el local es del tenant → 404 `facility_not_found`; include `unitType.name`, orden por `code`); endpoints `GET /admin/tenants/:id/facilities` y `GET /admin/tenants/:id/facilities/:facilityId/units` (`AdminGuard`). Frontend: `<TenantFacilitiesDialog>` (estado interno `selected` para el drill-down; hooks `useAdminTenantFacilities(id, open)` + `useAdminTenantFacilityUnits(id, facilityId)` on-demand). e2e `admin-tenant-users` 3/3 (añade: `facilityCount` en detail, `/facilities` vacío en tenant nuevo, `/facilities/:ghost/units` 404, 401). typecheck 8/8 + lint + build web.

- **Métricas del admin ampliadas + gráficos (2026-06-26):** la página `/admin/metrics` era mínima (5 cards de tenants + 4 de negocio, **MRR hardcoded a 0**, sin gráficos). Ahora calcula y muestra mucho más, **sin migración** (solo lectura cross-tenant vía `PrismaAdminService`). `AdminMetricsService.getOverview` (reescrito, ~15 queries en `Promise.all`) añade: **MRR real** (Σ `priceMonthly` de las suscripciones `active`) + **ARPU** (MRR ÷ tenants activos) —antes ambos 0—; **`trialsExpiringSoon`** (trials con `trialEndsAt` en ≤7 días) y **`openSupportTickets`** (open/in_progress/waiting_user) como alertas; **`platform`** (totales de plataforma: locales/trasteros/inquilinos/contratos/usuarios sumando todos los tenants); **`tenantsByPlan`** (distribución + MRR por plan, agrupado en JS desde `tenantSubscription` con su `plan`); **`monthlyGrowth`** (altas vs bajas de tenants por mes, 12 meses, bucketing por `createdAt`/`updatedAt`) y **`monthlySaasRevenue`** (ingresos de suscripción cobrados por mes, de `tenant_subscription_payments` status `paid` por `paidAt`). DTO ampliado en `packages/shared/src/admin/types.ts` (`AdminMetricsPlanSliceDto`, `AdminMetricsGrowthMonthDto`, `AdminMetricsRevenueMonthDto` + campos nuevos en `AdminMetricsDto`); el endpoint `GET /admin/metrics` y el hook `useAdminMetrics` no cambian. Frontend (`metrics/page.tsx` rediseñado con **Recharts**, ya usado en el panel del tenant): 5 KPIs tenants + 6 KPIs negocio (MRR/ARPU/altas/churn/trials/tickets, con acento ámbar en alertas) + **2 donuts** (tenants por estado, tenants por plan con MRR en la leyenda) + **2 gráficos de barras** (altas vs bajas, ingresos cobrados) + 5 totales de plataforma. e2e `admin-metrics` 1/1 (overview con `platform`, `tenantsByPlan` incluye `starter`, series de 12 meses, alta del mes contada; 401). typecheck 8/8 + lint + build web.
