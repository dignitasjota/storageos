/**
 * @storageos/database — cliente Prisma + tipos generados.
 *
 * Las apps consumen `PrismaClient`, enums y tipos directamente desde aquí:
 *   import { PrismaClient } from '@storageos/database';
 *
 * El cliente se genera en `node_modules/.prisma/client` al ejecutar
 * `pnpm db:generate` (o `pnpm db:migrate`).
 */
export * from '@prisma/client';
